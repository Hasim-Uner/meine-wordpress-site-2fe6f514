# Live Status

## Über Haşim, Abschluss-Ausgänge und ein Tracking-Ziel, Repository-Stand 2026-09-22

Dieser Abschnitt hat Vorrang vor der älteren Beschreibung von `/hasim-uener/`
(„Über Haşim – Methodikseite, 2026-09-13") und vor allen älteren Aussagen zum
Ziel des Tracking-Wegs im Kopf.

- **`/hasim-uener/` trägt die Copy aus `docs/briefings/ueber-mich/copy.md`.** Die Seite erzählt eine Sache: Die Website führt Gespräche, bei denen der Betreiber nicht dabei ist, und könnte zuhören, wenn man sie lässt. Aufbau: Kopf, 01–03 Geschichte, 04 Methode, 05 Wandel, 06 Haltung, 07 Zusammenarbeit, 08 Kurz zu mir, Abschluss. Die H1 lautet „Ihre Website führt Gespräche, bei denen Sie nicht dabei sind."
- **Die öffentlichen Anker bleiben vergeben.** `#arbeitsweise` (mit `#schwaechstes-glied` und `#defekt-oder-stellschraube`) trägt jetzt die Methode, `#werkzeuge` den Wandel, `#gestaltung` und `#ueberzeugen` die Haltung, `#zugaenge` die Zusammenarbeit, `#hintergrund` die Kurzbiografie. Die IDs sind weiterhin fest vergeben und nicht aus Überschriften erzeugt. Neu und ohne Außenbindung: `#geschichte`, `#erstes-gespraech`, `#zuhoeren`.
- **Entfallen: die Sektion „Orientierung", der dunkle Solar-Kasten und der eigene Übergabe-Absatz.** Die drei Besucherwege aus „Orientierung" (`details`-Block mit Blog-Link) sind ersatzlos weg; ihre beiden kommerziellen Ziele stehen als zwei leise Textlinks unter dem Abschluss-Button (Leistungen und Preise, Arbeiten und Ergebnisse). Der Beleg zum PV-Fall ist von der dunklen Tafel auf einen Absatz mit Haarlinie geschrumpft; Kennzahlen und Zeitraum kommen unverändert aus `hu_e3_metric()`, die Antwortzeit aus `hu_response_promise()`. Der Einordnungssatz zum Gesamtsystem entfällt mit der Tafel — der Fall bleibt über die verlinkte Fallstudie prüfbar. Der Absatz zur Übergabe nach dem Launch ist in einen Satz in Abschnitt 07 zusammengezogen.
- **Die Meta-Description der Route ist neu** und bildet die Gesprächs-These ab; der Title bleibt unverändert. Person-/ProfilePage-Bezug, die drei sichtbaren `rel="me"`-Profile und alle `data-track-*`-Werte bleiben erhalten; `about_view_results` und `link_about_freelancer` sind mit der Sektion umgezogen (`data-track-section="about_cta"`).
- **Kein Ausgang am Abschluss auf drei Routen.** Die laute Wegewahl im Fuß („Welcher Weg passt?") entfällt jetzt neben der Startseite auch auf `/kontakt/` und `/solar-waermepumpen-leadgenerierung/`: Kontakt ist das Ziel jedes Wegs, die Money Page endet in ihrem eigenen Marktcheck. Auf `/kontakt/` entfällt zusätzlich der Header-Button „Projekt anfragen", weil er auf dieselbe Seite zeigt. Der Navigations-Contract behält den CTA unverändert — er speist auch das gespeicherte WordPress-Menü (`inc/menu-setup.php`); ausgeblendet wird nur die Ausgabe in `template-parts/site-header.php`.
- **Ein Ziel für „Tracking".** Der Menüpunkt zeigte im Kopf auf `/ga4-tracking-setup/` und im Fuß auf `/server-side-tracking-b2b/`, weil `template-parts/site-header.php` den Routenschlüssel lokal über ein nicht existierendes `$routes['tracking_setup']` überschrieb. Die Überschreibung ist entfallen; beide Oberflächen lesen `tracking_b2b` aus `hu_get_commercial_route_map()`. `aria-current` bleibt auf beiden Tracking-Seiten gesetzt, und der Tracking-Weg wird im Fuß auf beiden ausgeblendet — dafür gibt es `hu_is_tracking_route_context()` in `inc/commercial-routing.php` statt derselben Vier-Wege-Prüfung an drei Stellen. `/ga4-tracking-setup/` bleibt als eigener Query-Owner bestehen und verlinkt weiterhin auf die Server-Side-Seite.
- **Geprüft:** Architektur-, PHP-, Canon-, Canon-Drift-, E3-, German-Copy-, Motion-, Spacing-, Entity-Signal-, Asset- und beide CSS-Audit-Guards grün, dazu die Funnel-Routing- und Lead-Path-Smoke-Tests. **Nicht geprüft:** visuelle Abnahme im Browser und Lighthouse. Beides steht vor der Veröffentlichung aus.

## Kanon erzwungen – Kontakt und Antwortzeit, Repository-Stand 2026-09-18

Dieser Abschnitt hat Vorrang vor allen älteren Aussagen zu Antwortzeit,
Marktcheck-Frist und sichtbarer Kontaktadresse.

- **Eine Antwortzeit, sitewide: „innerhalb von 24 Stunden werktags".** `hu_response_promise()` in `inc/canon/messaging-canon.php` liefert sie in sechs Fassungen (`value`, `window`, `badge`, `compact`, `sentence`, `phrase`); `HU_RESPONSE_HOURS` (24) ist die Zahl dahinter, `hu_response_promise_short()` und `hu_response_promise_sentence()` sind die benannten Kurz- und Satzform. `HU_RESPONSE_BUSINESS_DAYS` ist entfallen.
- **Der Marktcheck-Befund hat keine eigene Frist mehr.** `hu_marketcheck_reply_label()` bleibt als eigener Getter bestehen — es ist ein eigener Vorgang und die Copy rahmt ihn mit „Befund" —, liest den Wert aber aus dem Messaging-Canon. `HU_MARKETCHECK_REPLY_HOURS` und `HU_MARKETCHECK_REPLY_MAX_DAYS` sind entfallen, ebenso die Canon-Keys `marketcheck_reply_hours` und `marketcheck_reply_max_days` (neu: `marketcheck_reply_promise`).
- **Die berechnete SLA-Frist folgt derselben Konstante.** `nexus_compute_intake_response_deadline()` in `inc/review-crm.php` zählte 48 Arbeitsstunden, während die Copy eine andere Frist nannte: die Bestätigungsmail versprach das eine, der darin genannte Termin das andere. Beide lesen jetzt `HU_RESPONSE_HOURS`.
- **Impressum und Datenschutzerklärung lesen den Kontakt-Canon.** Die frühere Ausnahme für rechtlich benannte Kontaktwege ist aufgehoben; `page-impressum.php` (2×) und `page-datenschutz.php` nutzen `hu_get_contact_email()`, `hu_get_contact_mailto()` und `hu_get_contact_phone()`. Neu im Canon: `HU_CONTACT_EMAIL_MAILTO` / `hu_get_contact_mailto()`.
- **Zwei Shortcodes für Editor-Inhalte:** `[nx_email]` (`as="link|text|mailto"`, optional `label`) und `[nx_antwortzeit]` (`as="kurz|satz|kompakt"`) in `inc/shortcodes.php`. Sie sind der Zugriff für Gutenberg-Blöcke, ACF-Felder und Widgets, die nicht im Repo liegen.
- **`scripts/canon-guard.sh` blockt verbotene Werte repo-weit.** Anders als `lint-canon-drift.sh` (Diff-basiert) prüft er den ganzen Arbeitsbaum. Die Sperrliste steht als eigene Datei in `scripts/canon-forbidden-values.txt` und ist ohne Skriptänderung erweiterbar. Einzige inhaltliche Ausnahme ist `blocksy-child/inc/canon/**`; Historien-Pfade (CHANGELOG, `docs/audits`, `docs/decisions`, `docs/references`, `docs/briefings`, `seo-research`, `plans`, `automations/n8n`) stehen als begründete `ignore`-Zeilen in derselben Datei. Eingebunden in `.github/workflows/ci.yml` und in `scripts/build-theme-dist.sh` — letzteres läuft auch im `workflow_dispatch`-Deploy, also geht kein Deploy mit Verstoß durch. Lokal: `npm run lint:canon`.
- **Die Rechenannahmen des Money-Page-Vergleichsrechners stehen im E3-Canon.** `HU_E3_CALC_CPL_CONSERVATIVE` und `HU_E3_CALC_SALES_CONVERSION_CONSERVATIVE` waren nackte Literale im Template; die angesetzte Quote war dort von der gemessenen Abschlussquote des Falls (`HU_E3_SALES_CONVERSION_PERCENT`, 15 %) nicht zu unterscheiden und las sich als Widerspruch dazu. Der Rechner startet weiterhin bewusst unter den Fallwerten, sagt das jetzt aber im Text und liest beide Annahmen aus dem Canon. Unverändert: `HU_E3_LEAD_CONVERSION_PERCENT` ist die **Lead-Conversion-Rate** des Falls — eine eigene Kennzahl, die zufällig denselben Zahlenwert wie die Rechenannahme trägt und deshalb nie als Abschlussquote gelesen werden darf.
- **Das WordPress-Menü wird über `hu_retired_cta_labels()` normalisiert.** Die abgelösten CTA-Label standen in `inc/header.php` aus Fragmenten zusammengesetzt, um an der Fristen-Sperre vorbeizukommen. Sie liegen jetzt als Liste im Canon, wo ein abgelöster Wert im Klartext stehen darf.

## Startseite – Hero serverseitig konsolidiert, Repository-Stand 2026-09-16

Dieser Abschnitt hat Vorrang vor älteren Beschreibungen des Startseiten-Runtimes,
insbesondere über `startseite.js` und über das fehlende FAQ-Schema.

- **`assets/js/startseite.js` ist entfallen.** Das Skript hat den vom Server gerenderten Hero nach dem Parsen überschrieben: Eyebrow, H1-Struktur, Aufriss, Trust-Zeile, Porträt-Caption sowie Vorspann und Fall-Text in Abschnitt 02. Dazu injizierte es rund 140 Zeilen CSS mit durchgehendem `!important` in den `head`. Markup und Regeln liegen jetzt statisch in `front-page.php`, `startseite.css` und `startseite-rest-v1.css`. Das Hero-Diagramm ist statisches Markup statt DOM-Bau; die Bewegung bleibt CSS hinter `prefers-reduced-motion`. Die Route lädt auf dieser Ebene kein eigenes JavaScript mehr.
- **Die Antwortzusage im Hero kommt wieder aus dem Canon.** Das Skript trug die Antwortzusage als Literal und hatte das Canon-Element `.home-reply` ersetzt. Jetzt `hu_response_promise( 'compact' )` in der Trust-Zeile. Das E3-Case-Label stand ebenfalls als Literal im Skript und liegt jetzt als gebeugte Akkusativ-Fassung (`HU_E3_CASE_LABEL_ACCUSATIVE`) im E3-Canon.
- **`scripts/lint-canon-drift.sh` prüft jetzt auch Fristen-Literale.** Zeilen, die den Canon selbst aufrufen, dürfen ihren Fallback im Wortlaut tragen; repo-weit gilt seit 2026-09-18 zusätzlich `scripts/canon-guard.sh`, der auch diese Fallbacks nicht mehr durchlässt. Der Bruch oben lief grün an der CI vorbei, weil das Muster bis dahin nur Preise kannte.
- **`inc/homepage-wow.php` berührt die Startseite nicht mehr.** `hu_output_inline_home_system_visual()` hat über `wp_footer` rund 9 KB Inline-CSS und ein Inline-SVG für `.home-system-visual` in die Produktions-Startseite gelegt; `startseite.js` hat denselben Knoten anschließend entfernt. Die Funktion ist entfallen. Die Testseite hat selbst keinen `.home-hero` und war nie betroffen.
- **Drei tote Hero-Generationen sind aus dem CSS entfernt.** Der `::before`-Hero mit `home-hero-system-copper.webp` und der komplette V6-Block in `startseite.css` griffen nicht mehr, weil das Skript die Klasse entfernt hat; der V9.2-Block in `startseite-rest-v1.css` wurde von der V9.3-Fassung im Skript überstimmt und sah nur live aus. Übrig bleibt eine Definition je Regel.
- **Die Startseite hat jetzt ein eigenes `FAQPage`-Schema.** Sie war die einzige Money Page ohne, obwohl `inc/org-schema.php` den Editor-FAQ-Cache dort mit der Begründung unterdrückt, die Fragen seien template-owned. Der Knoten entsteht aus demselben Array wie die sichtbaren Antworten und hängt über `publisher` am `#organization`-Knoten. Dies ersetzt die Aussage „kein eigenes JSON-LD/FAQ-Schema" im Abschnitt zur Orientierungs-Startseite weiter unten.
- **Der Hero-Eyebrow trägt den Ortsbezug wieder** (`WordPress · Tracking · CRM · Pattensen bei Hannover`). Die JS-Fassung hatte ihn auf `WordPress · Tracking · CRM` verkürzt; `/` ist Query-Owner für `wordpress freelancer hannover`.
- **Die Referenzen im Nachweis-Block sind `h4` statt `h3`.** Sie standen vorher als Geschwister ihrer eigenen Gruppenüberschrift in der Outline.
- **Geprüft:** Architektur-, PHP-, Canon-Drift-, E3-, German-Copy-, Motion-, Spacing- und beide CSS-Audit-Guards grün. Die gerenderte Startseite wurde über eine PHP-Vorrichtung gegen `HEAD` verglichen: Tag-Balance sauber, alle 22 `data-track-`Hooks unverändert, FAQ-Schema wortgleich zu den sichtbaren Antworten, Überschriften-Outline ohne Sprung. **Nicht geprüft:** visuelle Abnahme im Browser und Lighthouse. Beides steht vor der Veröffentlichung aus.

## Meta Ads – Legacy-Service konsolidiert, Repository-Stand 2026-09-15

- `/meta-ads/` besitzt keine veröffentlichte WordPress-Seite mehr und wird als Legacy-Einstieg explizit auf den aktiven Beitrag `meta-ads-fuer-b2b` geführt.
- Das Ziel wird in `nexus_get_legacy_offer_redirect_map()` über `get_permalink()` aufgelöst statt auf die aktuelle Autoren-Suffix-Struktur fest verdrahtet. Der Live-/REST-Check vom 15.09.2026 lieferte für Post 14605 aktuell `/meta-ads-fuer-b2b/hasim/`.
- `page-meta-ads.php` und `assets/css/meta-ads.css` sind retired und entfernt; der aktive Beitrag, seine SEO-Metadaten und die normale Single-/Editorial-Architektur bleiben bestehen.
- `meta-ads` ist nicht länger als eigenständige `noindex, follow`-Route klassifiziert. Historische Audit-/WGOS-Dokumente bleiben historische Snapshots und werden nicht in aktuelle Routing-Verträge umgedeutet.

## GA4 – direkter Anfrageweg, Repository-Stand 2026-09-14

Die drei Hauptbuttons auf `/ga4-tracking-setup/` heißen jetzt
„Tracking-Projekt anfragen“ und führen zu
`/kontakt/?type=project&focus=tracking`. Begleittexte und Antwortzusage folgen
dem Projektweg; der Marktcheck-Hinweis im Kennzahlenband entfällt auf GA4.
Die optionale CTA-Fokusangabe verwendet den bestehenden Kontakt-URL-Helfer.
Die Performance-Marketing-Seite, Kontaktverarbeitung und Event-Namen bleiben
unverändert. PR-Stand; Veröffentlichung und Live-Versand nicht vorausgesetzt.

## Ergebnisse – Arbeitsbelege, Repository-Stand 2026-09-14

- `/ergebnisse/` zeigt WordPress-Arbeiten zuerst, ergänzt um eine technische
  Projektgeschichte zur Homepage-Konsolidierung mit stabilen Quellverweisen.
  Öffentliche Referenzfakten und Solar-Metriken bleiben in ihren Canons.
- Solar-Zahlen haben Kostenabgrenzung und getrennte Definitionen. Die angenommene
  frühere Abschlussquote, undatierte Lighthouse-Kacheln und die unbestätigte
  Live-Tracking-Anzeige entfallen. Der separate Case behält seine Sperre.
- White-Label wird durch ein ausdrücklich gekennzeichnetes Übergabemuster am
  eigenen Projekt erklärt; keine neue Kundenreferenz oder Messung erfunden.
- Layout: `system.css` plus gekapseltes `ergebnisse.css`, keine NX-Tokens,
  keine Scroll-Reveals und kein Seiten-JavaScript. Die Legacy-CSS-Baseline
  verliert den Ergebnisse-Verbraucher. Native Details bleiben ohne JS nutzbar.
- Motion Brief: Intensität 0, kein fokales Bewegungselement; Text und Kennzahlen
  statisch, Auf-/Zuklappen unmittelbar, Tastatur/Touch über native Elemente.
  Fokus und reduzierte Bewegung verwenden den gemeinsamen Core. Keine neue
  Laufzeitabhängigkeit; reale CWV wurden nicht erhoben.
- Die gemeinsame TOC folgt der neuen Abschnittsreihenfolge. Hero-Links führen
  zu Belegen, der Abschluss zu den drei bestehenden Anfragewegen. Blocksy
  behält das einzige `main`; keine doppelte Hauptbereich-ID im Template.
- Titel/Beschreibung angepasst; FAQ-Caches aus nicht sichtbarem Editor-Inhalt
  werden auf dieser Template-Seite nicht ausgegeben. Keine neue URL oder
  Commercial-Query-Ownership.
- Umsetzung im PR; Veröffentlichung nicht vorausgesetzt. Die Browserrichtlinie
  blockiert die visuelle Prüfung in dieser Sitzung. Die Prüfung von Desktop,
  Mobil und Live-Kontakt bleibt offen; die lokalen Code-/DOM-Prüfungen sind
  kein Ersatz dafür. Frontend-Grundlage: Modern Web Guidance `html`.


## Über Haşim – Methodikseite, Repository-Stand 2026-09-13

- `/hasim-uener/` übernimmt den abgestimmten persönlichen Methodiktext in
  verdichteter Form: Hintergrund, Engpass, Defekt/Gestaltung/Hypothese,
  Besucherwege, Gestaltung und langfristige Übergabe.
- Das Layout verwendet `system.css`: heller Lesebereich, nummerierte Kapitel,
  bestehendes responsives Porträt und dunkler Projektbeleg/Abschluss. Die alte
  Stationsanimation und das gesamte separate Seiten-JavaScript entfallen.
- Pauschale Behauptungen über Formularlänge, Kosten und Abschlüsse entfallen.
  Der Solar-Fall liest Zahlen und Zeitraum aus dem Canon; die öffentliche
  WordPress-Arbeit bleibt davon getrennt. Biografische Aussagen zu Studium,
  Vater und eigenem Werbebudget stammen aus dem Betreibertext.
- Titel/Beschreibung bilden die Methodikseite ab. Der bestehende ProfilePage-
  und Person-Bezug bleibt erhalten; unsichtbare Editor-FAQ-Caches werden auf
  dieser Template-Seite nicht ausgegeben. Commercial Query-Ownership bleibt
  auf Homepage und Fachseiten.
- Anfrage-, Profil- und Mail-Verweise behalten ihre Ziele und bestehenden
  Actions. Neue Links erklären die drei unterschiedlichen Besucherwege.
- PR-Stand; Veröffentlichung nicht vorausgesetzt. Browser-URL-Richtlinie
  verhindert die visuelle Prüfung in dieser Sitzung. Desktop/Mobil und der
  Kontaktabschluss bleiben vor Freigabe visuell zu prüfen.


## Homepage-Konsolidierung – Repository-Stand 2026-09-13

Die Startseite übernimmt die Freelancer-Inhalte und Suchintention. Vier direkte
Leistungsangebote, öffentliche Referenzen und der bestehende Kontaktablauf
ersetzen den reinen Drei-Wege-Verteiler. White-Label und Solar/Wärmepumpe bleiben
Spezialangebote. Die alte Freelancer-URL erhält einen 301 auf `/`; interne
Links, Service-Schema und Query-Ownership ziehen mit. Alte CSS-/Formularskripte
entfallen, der WordPress-Datensatz wird nicht automatisch gelöscht.

PR #345 ist gemergt; der Betreiber hat die Veröffentlichung bestätigt.
Eine eigene visuelle Liveprüfung sowie der Live-Redirect-/Kontakt-Smoke sind
in dieser Sitzung wegen der Browser-URL-Richtlinie weiterhin nicht bestätigt.
Entscheidung und Nachkontrolle: `../decisions/homepage-freelancer-konsolidierung.md`.
Die älteren Homepage-/Freelancer-Abschnitte unten dokumentieren den Vorgängerstand.


Statusbasis: Repo-Inhalt zum jeweiligen Commit; kein händisch gepflegtes Tagesdatum.

Basis dieses Status:

- Repo-Inhalt
- bestehende Deploy-Workflows
- vorhandene Template- und Funnel-Logik

Nicht verifiziert:

- exakte Live-Konfiguration in WordPress-Admin
- exakte n8n-, GTM-, GA4-, Consent- und CRM-Setups

## White-Label: Entwurf im Review, 2026-09-12

- `/whitelabel-retainer/`: heller Einstieg und dunkle Angebots-/Anfrageflächen nach dem aktuellen Gutachten-Design der Solar-Seite; Farben aus dem gemeinsamen `system.css`. Leistungen, illustriertes Übergabemuster und öffentliche Portfolioverweise erklären die Zusammenarbeit. Das Muster ist ausdrücklich kein Kundenbeleg; öffentliche Referenzen erhalten keine anonymisierten Solar-Kennzahlen.
- Primärziel ist die konkrete Aufgabe im bestehenden Formular. Der bezahlte WordPress-Test-Sprint steht vor dem Retainer; größere Einstiegsprojekte sind aufklappbar. Preise bleiben im Canon. Die unzutreffende Gleichsetzung von Preisdifferenz und Agenturmarge entfällt; Antwortfenster und verfügbare Umsetzungskapazität werden getrennt erklärt.
- Formular mit zwei Pflichtfeldern, sichtbarem Angebotsphasen-Kontext, optionalen Zugangsangaben, Fehlerfokus, Schutz vor gleichzeitigem Doppelversand und E-Mail-Ausweg ohne JavaScript. Endpoint und Erfolgs-Event bleiben erhalten. Die zentrale Start-FAQ beschreibt denselben Ablauf wie das Template und das FAQ-Schema.
- Motion Brief: Intensität 1; statische Inhalte, unmittelbare Formularzustände, native Details und dezente Button-Farbwechsel. Keine Reveal-Kette, kein zusätzlicher Animations- oder Tracking-Runtime. Mobiler CTA verschwindet vor dem Formular und ist im versteckten Zustand nicht fokussierbar; reduzierte Bewegung deaktiviert weiches Scrollen. PHP-gerendertes Template mit DOM-basierten Formularprüfungen. Die Browserrichtlinie blockiert die Live-Seite und das Öffnen der lokalen Vorschau; die visuelle Prüfung bleibt vor Veröffentlichung offen. Keine Aussage zu gemessenen Live-CWV oder zugestellten Live-Anfragen. Veröffentlichung erfolgt erst nach Review und Freigabe.

## Designsystem, Kopf, Fuß und Startseite: Umsetzung 2026-09-12, Veröffentlichung noch ausstehend

Dieser Abschnitt hat Vorrang vor älteren Beschreibungen von Startseite, Kopf und Fuß weiter unten.

- **Das Designsystem liegt jetzt in `assets/css/system.css` und ist global geladen.** Tokens stehen auf `:root` (Farbe, Abstandsskala `--s0` bis `--s6`, drei Mono-Stufen, Bewegung, Maße), Elementregeln unter `.doku`, Kopf und Fuß unter `.leiste` und `.fuss`. Einen Farbwert gibt es nur an zwei Stellen: in `:root` und in `.tafel`, das die Tokens für dunkle Flächen lokal umdefiniert statt Kindregeln zu überschreiben. Kein Dark Mode, `color-scheme: light`. `assets/css/anfragestrecke.css` behält vorerst seine eigene Kopie derselben Tokens unter `.strecke-doc`: die Money Page ist damit unverändert und zieht in einem eigenen Schritt nach.
- **Die alte Header-Schicht ist vollständig abgelöst.** `site-header-premium.css`, `site-header.css`, `site-header.js` und `site-footer.css` sind entfernt. Der Standardkopf läuft über `.leiste` in `system.css` plus `leiste.js`; Blog- und Energy-Kontexte behalten ihre eigenen, separat gerouteten Header. Der retired Growth-Audit-Pfad besitzt keinen Sonderheader mehr.
- **Der Kopf ist eine Leiste statt einer schwebenden Pille.** Schlanke Zeile, Wortmarke links, Navigationsziele und `Projekt anfragen` rechts, eine Haarlinie als Abschluss, `aria-current="page"` auf der laufenden Route. `assets/js/leiste.js` steuert Klappe, Escape, Klick daneben und die gemessene Höhe als `--leiste-h`. Der retired Growth-Audit-Sonderkopf ist entfernt.
- **Der Fuß steht auf demselben System.** Zwei Lautstärken bleiben. Die Absenderzeile ist jetzt das `.protokoll`-Muster: zwei Spalten, eine Zeile je Angabe in Mono, Label links, Wert rechts — vorher lagen Anschrift, Antwortzeit und Messhinweis als eine lange, durch Mittelpunkte getrennte Zeile gleich schwer nebeneinander. Die Anschrift bleibt zeichengleich mit dem `Organization`-Knoten in `inc/org-schema.php`. Alle `cta_footer_*`-Werte sind unverändert.
- **Die Startseite hat sieben Abschnitte statt elf.** Reihenfolge: Dokumentkopf mit Definition und Metadatenzeile · Prüfstand (Lighthouse, von Platz vier auf Platz zwei) · 01 Drei Wege · 02 Die Strecke · 03 Der Fall · 04 Eignung · Kontakt als Tafel. Ersatzlos entfallen: die Funnel-Grafik mit vier Begriffen (der Inhalt steckt in „Die Strecke“), das Auffangbecken „Sie wissen noch nicht, welcher Weg passt?“ und die Schlussliste „Was trifft zu?“ im Fuß. Die drei Wege standen vorher dreimal auf der Seite und in drei verschiedenen Reihenfolgen; jetzt stehen sie im Seiteninhalt genau einmal, mit Preisrahmen in der Zeile. Details zu Hooks und Preisherkunft: `docs/architecture/CONVERSION_ROUTING.md`.
- **Die 12-%-Lead-Conversion-Rate steht nicht mehr auf der Startseite.** Sie ist kein Fehler — `inc/canon/e3-proof-canon.php` führt sie als eigene Kennzahl neben der Abschlussquote (15 %), und die Startseite hatte sie mit dem Zusatz „auf der Anfragestrecke“ auch benannt. Zwei Prozentzahlen aus demselben Fall erklären sich auf einer Verteilerseite aber nicht in der Zeit, die sie bekommt. Der Canon bleibt unverändert; die ausführliche Fallstudie zeigt weiterhin beide.
- **Fünf Stylesheets und ein Skript laden auf der Startseite nicht mehr:** `homepage.css`, `homepage-redesign.css`, `homepage-art.css`, `homepage-art-v3.css`, `homepage-flow.css` und `homepage-redesign.js`. An ihre Stelle treten `system.css` und `startseite.css` plus `startseite.js` (nur die laufende Kapitelmarke). `homepage.css` und `homepage-redesign.css` bleiben im Repo, weil Agentur-, WGOS- und Case-Routen daran hängen; `homepage-art.css`, `homepage-art-v3.css`, `homepage-art-v3-base.css`, `homepage-flow.css`, `homepage-cro.css` und `homepage-redesign.js` waren danach verwaist und sind entfernt.
- **Antwortzeit sitewide vereinheitlicht.** Startseite, Fuß, `/kontakt/`, White-Label, Freelancer und die WGOS-Cluster tragen seither dieselbe Zusage aus `hu_response_promise()`; drei Stellen in `page-whitelabel-retainer.php` nannten sie vorher hart im Markup. **Den konkreten Wert dieses Eintrags ersetzt der Abschnitt „Kanon erzwungen“ ganz oben** — die hier beschriebene Vereinheitlichung gilt weiter, die Frist selbst nicht mehr.
- **Title und Description der Startseite standen im Revier der Money Page.** Sie lauteten „Anfragesysteme für Solar & Wärmepumpe | Haşim Üner“ und nannten den Solar-CPL-Fall, obwohl die Startseite in `docs/seo/query-ownership.csv` keine Query besitzt und `/solar-waermepumpen-leadgenerierung/` diese Intention tatsächlich hält. Jetzt: „Haşim Üner — WordPress, Tracking und Conversion“ und eine Beschreibung ohne Kennzahl. Die Startseite holt damit selbst weiterhin nichts — für einen Verteiler normal.
- **Geprüft:** PHP-Lint und `pre-deploy-smoke` grün, Spacing-, Motion- und Canon-Drift-Guard grün. Die gerenderte Startseite wurde über eine PHP-Vorrichtung plus Playwright bei 360, 390, 768, 1024 und 1440 Pixeln gemessen: kein waagerechter Überlauf, kein am Rand klebender Text, kein Tap-Ziel unter 24 Pixeln, `--leiste-h` gesetzt, Klappe ab 1081 Pixeln aus. **Nicht geprüft:** Lighthouse. Die 100/100/99 auf dem Prüfstand stammen aus dem Canon und sind vor der Veröffentlichung neu zu messen — sonst wird der Prüfstand zum Eigentor.

## Freelancer-Release: Main-Freigabe 2026-09-09

- **Freelancer-Nachbesserung, 2026-09-09:** Normale Textlinks behalten bei Hover, Tastaturfokus und Aktivierung die passende Farbe auf hellen und dunklen Flächen; eine später geladene Blocksy-Hover-Regel kann sie nicht mehr weiß färben. Das Inhaltsverzeichnis läuft auf Desktop ab 1280 × 600 Pixeln mit feinem Zeiger in einer reservierten linken Randspalte mit: 64 Pixel breit, 8 Pixel Randabstand, vollständige Beschriftungen bei Hover oder Tastaturfokus. Auf kleinen, flachen oder Touch-Ansichten gibt es eine kompakte aufklappbare Navigation unter dem Header. Native Sprunglinks, Abschnittsmarkierung und Tastaturziele bleiben erhalten. Ohne JavaScript sind die Links sichtbar; ohne Intersection Observer entfällt nur die automatische Abschnittsmarkierung. Älteres HTML im Cache unterbricht die separate Formularinitialisierung nicht. Grundlage: Modern Web Guidance `user-experience/scroll-position-aware-elements`, CSS `sticky` statt zusätzlicher Scrollanimation.
- **Freelancer Phase 2:** Umsetzung vom 2026-09-08/09 auf Basis von `origin/main` (`4a5db01`, Arbeitszweig `codex/freelancer-phase-2`). Der Nutzer hat den Push nach `main` am 2026-09-09 ausdrücklich freigegeben; die Auslieferung folgt über den bestehenden CI- und Production-Deploy-Workflow. Auf `/wordpress-freelancer-hannover/` führen der gerenderte Header-CTA (Leiste und Menü) und der Footer-Link „Kontaktformular“ zum seitenlokalen `#anfrage`. Der Footer lässt dort die erneute Drei-Wege-Wahl und damit den Selbstlink weg; direkte Kontaktwege, Verzeichnis und Absender bleiben. Globale URL-Helper, andere Routen und bestehende Tracking-Aktionsnamen bleiben unverändert. Diese Freelancer-Ausnahme ersetzt mit diesem Release die unten beschriebene identische Footer-Ausgabe auf allen Seitentypen.
- **Freelancer-Seitenaufbau:** Das Template führt vom Hero unmittelbar zum begrenzten Solar-Proof und danach durch Leistungen/Preise, Anfrageweg, Zusammenarbeit, FAQ und Projektanfrage. Die vier Service-Anker `#angebot-website`, `#angebot-funnel`, `#angebot-tracking` und `#angebot-weiterentwicklung` bleiben erhalten; Preise kommen aus dem bestehenden Canon. Die breite Vergleichstabelle und die separate Preisrechtfertigung sind durch eine kurze aufklappbare Entscheidungshilfe ersetzt. Öffentliche, undatierte Lighthouse-Zahlen entfallen; überprüfbare Links führen zum Projektverlauf und den automatisierten Repository-Prüfungen. Die persönliche Einordnung verlinkt `/hasim-uener/`. Das Template erzeugt keinen zweiten `main`-Bereich innerhalb des Blocksy-Hauptbereichs.
- **Freelancer-Anfrage und SEO:** Angebotslinks übernehmen ihren Kontext in die vorhandene Formularauswahl. Der Zweischritt wird erst nach erfolgreicher JavaScript-Initialisierung eingeblendet; direkte E-Mail und Telefon bleiben verfügbar, ohne JavaScript erscheint zusätzlich ein erklärter E-Mail-Ausweg. Endpoint `nexus/v1/contact-request`, Payload-Feldnamen, Einwilligung und serverseitiger CRM-Pfad bleiben unverändert. Es wird keine Laufzeitmessung ergänzt. Title und Query-Zuordnung bleiben; die Meta-Description beschreibt die direkte Entwickler-Zusammenarbeit. `inc/org-schema.php` schließt die Freelancer-Route bzw. ihr Template vom generischen Editor-FAQ-Cache aus, damit keine unsichtbaren, veralteten Fragen ausgegeben werden. Das bestehende Service-Schema mit vier Angeboten bleibt zentral; kein zusätzliches FAQ-Schema entsteht im Template.
- **Freelancer-Layout und Navigation:** Die zwei bisherigen Seiten-Stylesheets sind in einem Stylesheet mit gemeinsamen Abstands- und Font-Tokens zusammengeführt; der separate Polish-Layer entfällt. Lesbare Kontraste gelten auch für fokussierte Formulareingaben. Tabellen-Clipping, scrollabhängige Texttransparenz und Porträt-Effekte entfallen. Die Route nutzt native Sprunglinks mit Header-Abstand und Fokusziel; `nexus-core.js` nimmt ausschließlich diese Route vom globalen Smooth-Scroll-Handler aus. Auch Blocksys CSS-Scrollanimation wird nur hier überschrieben. Der Block-Library-Dequeue erfolgt nach den Core-Enqueues. GitHub-Arbeitsnachweise und ein PageSpeed-Prüflink ersetzen undatierte Score-Behauptungen.
- **Lokale QA, 2026-09-09:** Theme-Build, PHP-Syntax, vollständige PHPStan-Analyse, Architektur-, Lead-Path-, Canon-, Entity-, Copy-, CSS-Abstands- und Motion-Prüfungen bestanden. Browserprüfung bei 320, 375, 412, 768, 1024 und 1440 Pixeln ohne Text-Clipping oder horizontalen Overflow; genau ein Hauptbereich und eine H1. Formular-, Fehler-, Fokus- und Rückwegtests liefen mit simulierten Antworten, ohne echte Anfragen zu versenden. Skip-Link, Menü-CTA, Escape, Footer-Anker, FAQ, reduzierte Bewegung, 200%-Textvergrößerung und E-Mail-Ausweg ohne JavaScript geprüft. Axe meldet auf der Route einschließlich geöffneter Inhalte und Formularzustände keine WCAG-Verletzungen; der vorhandene Blocksy-Skip-Link außerhalb der Landmarks bleibt ein Best-Practice-Hinweis der Gesamtseite.
- **Performance-Abschluss, 2026-09-09:** Der Hero reserviert die feste Höhe des Premium-Headers über `--nx-site-header-height`, statt vom nachträglich gemessenen Wert `--nx-header-height` abzuhängen. Das ungenutzte Cal-Buchungsskript wird ausschließlich auf dieser Route abgemeldet; dort gibt es keinen Buchungsdialog. Anschließend ergab eine festgelegte Dreierreihe mit Lighthouse 13.4.1 auf dem gebauten, per gzip ausgelieferten lokalen Stand Performance **95 / 96 / 96** (Median **96**), jeweils **100 Accessibility, 100 Best Practices und 100 SEO**. FCP 1,69–1,72 s, LCP 2,32–2,51 s, TBT 82–182 ms, CLS 0,0006; keine Run-Warnungen. Frühere Läufe vor diesen Korrekturen schwankten zwischen 75 und 95; sie wurden nicht als Abschlusswert verwendet. Ein kontrollierter Versuch mit Footer-CSS im Head brachte keinen ausreichenden zusätzlichen Befund für eine Produktänderung; dessen bisheriger Ladeort bleibt bestehen. Die temporäre Vorschau ersetzt weder WordPress-Laufzeit/DB/REST-Prüfung noch einen produktiven Performance-Test; keine CrUX-/INP-Feldwerte und kein Conversion-Nachweis.
- **Aktualitätsdatum noch offen:** Das `WebPage.dateModified` der Freelancer-Route kommt weiterhin aus dem WordPress-Datensatz. Vor Veröffentlichung bzw. beim Live-Abgleich muss das tatsächliche redaktionelle Änderungsdatum synchronisiert werden; die Checkout-/Deploy-Zeit eines Templates wird nicht als redaktionelles Datum ausgegeben. Canonical, Query-Ownership und Schema-Owner bleiben unverändert.

## Live

- **`/stack-solar/` aufgelöst, ersatzlos auf die Money Page (2026-09-12).** 31 Impressionen, 0 Klicks in 90 Tagen (Export 2026-09-01), keine Query in `docs/seo/query-ownership.csv`. Anders als bei `/stack-agentur/` wurde **kein Inhalt übernommen**: Drei der fünf Schichten — Server-Side Tracking, CRM-Anbindung, Vorqualifizierung — führt die neu gebaute Anfragestrecke bereits als Stationen `station-messung`, `station-vertrieb` und `station-vorqualifizierung`, dort mit Gegenrechnung und konkreten Zahlen statt mit Schichtbeschreibung. Unikat blieben nur Frontend-Speed und Managed Hosting; Letzteres trug im Wesentlichen eine Affiliate-Platzierung. Entfallen sind `page-stack-solar.php`, der Forced-SEO-Eintrag in `seo-meta.php`, vier Einträge in `inc/enqueue.php` (darunter der Affiliate-Disclosure-Block), der `llms-txt.php`-Eintrag und die Zeile in der statischen `llms.txt`. Der Verweis auf `/server-side-tracking-b2b/` ist entfernt, der Link in `assets/content/blog/photovoltaik-leads-tco-rechnung.md` zeigt direkt auf die Anfragestrecke statt über die Weiterleitung. `/stack-solar/` liegt als 301 auf `/solar-waermepumpen-leadgenerierung/`. **Nebenwirkung, bewusst in Kauf genommen:** Die Seite war der einzige Aufrufer von `template-parts/affiliate-notice.php`; das Partial bleibt ohne Aufrufer bestehen, `affiliate-notice.css` wird nicht mehr geladen. Die HostPress-Partnerschaft selbst läuft unverändert über `inc/affiliate-links.php` und `inc/blog-pillar-posts.php` weiter. Dort werden Partnerlinks mit `rel="sponsored nofollow noopener"` ausgezeichnet, aber ohne sichtbaren Werbe-Hinweis — dieser Zustand bestand bereits vorher und ist zu prüfen. **Manual WP:** Seite im Backend löschen, sonst bleibt sie in der Sitemap.
- **`/stack-agentur/` aufgelöst, Inhalt in White-Label (2026-09-12).** Die Seite trug 16 Impressionen und 0 Klicks in 90 Tagen (Export 2026-09-01) und besitzt in `docs/seo/query-ownership.csv` keine Query. Ihr tragendes Argument fehlte auf der Zielseite und steht jetzt als eigener Absatz im Beleg-Abschnitt von `/whitelabel-retainer/`: Ein Infrastrukturwechsel ist keine Voraussetzung, Managed Hosting ist kein Nachteil, eigene Server lohnen erst bei Last, Sicherheit oder Integrationen. Dazu zwei FAQ-Einträge in `nexus_get_whitelabel_faq_items()` (`stack`, `bestand`). Alles Übrige — versionierter Code im Repository der Agentur, Zugänge in deren Accounts, dokumentierte Übergabe ohne Lock-in — stand dort bereits, teils klarer formuliert. Entfallen sind `page-stack-agentur.php`, der Forced-SEO-Eintrag in `seo-meta.php`, zwei Einträge in `inc/enqueue.php` sowie in `seo-subpage-cluster-links.php` der Forced-Meta-Filter und die per `blocksy:content:bottom` angehängte Verweis-Sektion, die den neuen Absatz gedoppelt hätte. `/stack-agentur/` liegt als 301 auf `/whitelabel-retainer/` in `nexus_get_legacy_offer_redirect_map()`; der Auslagerungs-Leitfaden bleibt verlinkt. **Manual WP:** Seite im Backend löschen, sonst bleibt sie in der Sitemap.
- **Methodenbibliothek von `/wordpress-agentur-hannover/` entfernt (Umsetzung 2026-09-12, Veröffentlichung noch ausstehend).** Der Abschnitt `#asset-uebersicht` mit Filter-Chips, sechs Akkordeons und 39 Baustein-Karten ist ersatzlos entfallen; `#methode` bleibt und beschreibt die sechs Kernbereiche weiterhin, jetzt ohne Baustein-Zählung und ohne Verweis in den Katalog. Die Karten sind keine Links mehr (`.wgos-steps__link--static`), weil ihre Ziele `#acc-<slug>` nicht mehr existieren. Anlass: Die 39 Karten verlinkten auf `wgos_asset`-Seiten, die seit `hu_get_noindex_follow_post_types()` `noindex, follow` tragen — öffentliche Linkfläche in einen Index-Sackgassen-Pfad. Das Template lädt die Registry nicht mehr (`nexus_get_wgos_asset_registry()`, `$asset_groups`, `$area_meta`, `$total_assets` entfallen); 108 Zeilen Inline-JavaScript für Akkordeon-Fabrik, `#acc-`-Deeplinks und Kategorie-Filter sind mit entfernt. `NexusCore.initFaqAccordion` betreibt das FAQ und ist nicht betroffen. Sechs Verweise auf `#asset-uebersicht` in `wgos-assets.php`, `wgos-cluster-pages.php`, `seo-cockpit-links.php` und `helpers.php` zeigen jetzt auf `#methode`. Der `wgos_asset`-CPT, seine Einzelseiten und der Hub `page-wgos-assets.php` bleiben unverändert bestehen. Template 84 → 72 KB. **Offen:** Die zugehörigen Regeln in `assets/css/agentur.css` (`#asset-uebersicht`, `.acc-*`, `.ag-lib-*`, `.asset-card`) sind jetzt tot und noch nicht entfernt; `.ag-counter` wird weiterhin gebraucht und muss bleiben.
- `blocksy-child/` ist der deploybare Website-Code.
- **Money Page `/solar-waermepumpen-leadgenerierung/`: Gutachten-Standard (Umsetzung 2026-09-11, Veröffentlichung noch ausstehend).** Die Route ist vollständig neu gebaut und trägt ein eigenes Designsystem in `assets/css/anfragestrecke.css`, gekapselt unter `.strecke-doc`: weißer Grund, Satoshi/Figtree im Satz, IBM Plex Mono für Marken und Zahlen, Randspalte mit laufender Kapitelnummer, Marginalie, Radius 3 px, vier dunkle Tafeln, kein Dark Mode. **Das Stylesheet trägt auch die Marktcheck-Strecke** (`sol-quiz-*`, `sol-sr`, `sol-mono`): die lag bis dahin in `solar-leadgenerierung-solara.css`, und als diese Datei von der Route genommen wurde, stand das Formular eine Deploy-Runde lang ungestylt da. Wer das Stylesheet ablöst, muss die Strecke mitbringen. Die Kapitelnavigation ist ein Register: unter 1280 px eine flache waagerechte Leiste, darüber (mit Zeiger und ab 620 px Höhe) eine schmale senkrechte Leiste am linken Rand, die bei Hover oder Tastaturfokus auf die Titel aufklappt. `homepage-redesign.css` und `solar-leadgenerierung-solara.css` laden auf dieser Route nicht mehr. `solar-leadgenerierung-solara.js` bleibt und trägt weiterhin den mehrstufigen Marktcheck (REST → CRM) am Mount `#sol-quiz-mount`; die Wurzelklasse `.solara-landing` bleibt dafür erhalten. Neu daneben: `assets/js/anfragestrecke.js` für Rechner, Kapitelmarke und genau zwei Aufbau-Bewegungen. Aufbau: Dokumentkopf mit Definition, Metadatenzeile, Messschrieb und Marktzeile · 01 Strecke (fünf Stationen) · 02 Ihr Anteil · 03 Rechnung (Cost-per-Order-Rechner) · 04 Fall · 05 Leiter mit Ausstiegspunkten · 06 Passung · 07 Marktcheck · 08 Fragen · 09 Verweise. Die Anker `#marktcheck`, `#ergebnisse` und `#einstieg` bleiben Abschnitts-IDs; `#fall` und `#leiter` hängen zusätzlich an den jeweiligen H2. Die alten Anker `#problem`, `#vergleich`, `#system`, `#capex`, `#fit`, `#garantie`, `#deeper`, `#faq` und `#final-cta` existieren nicht mehr — kein Repo-Link zeigte darauf.
- **Marktcheck ohne JavaScript: kein `mailto` mehr (2026-09-21).** Der Ausgangszustand des Mounts `#sol-quiz-mount` war ein `mailto:`-CTA mit der Aufschrift „Marktcheck starten“ — eine Zusage, die der Link nicht einlöst, und die einzige `mailto`-Stelle im Marktcheck-Pfad. An seiner Stelle steht jetzt ein Hinweis, dass die Strecke JavaScript braucht, plus ein Link auf das serverseitig gerenderte Formular auf `/kontakt/` (`nexus_get_contact_url()`). Die Kontaktadresse bleibt als Text sichtbar. Tracking-Hook: `cta_strecke_marktcheck_fallback_kontakt` statt `cta_strecke_marktcheck_mail`. Die mehrstufige Komponente selbst ist unverändert und mountet weiterhin aus `solar-marketcheck-compact.js`; die CTAs auf `/ergebnisse/` und `/case-study-solar-leadgenerierung/` zeigten bereits über `hu_get_request_marketcheck_url()` auf `#marktcheck`.
- **Marktzahlen nennen ihre Quellen auf der Seite (2026-09-21).** `hu_market_figures_disclaimer()` sagte „Quellen nenne ich auf Nachfrage“. Der Satz nennt sie jetzt: Anfragenfluss, pv magazine und A&M Beratung, mit `HU_MARKET_FIGURES_YEAR` als Erhebungsstand. Die Money Page ist der einzige Leser des Market-Canons.
- **Solar-Fall ohne Anonymisierungsbegründung (2026-09-21).** Auf der Money Page ist „Anonymisiert, weil ein Verfahren läuft — die Belege zeige ich im Gespräch“ aus Abschnitt 04 entfernt, und die Quellzeile der Kennzahlentabelle heißt „… in DACH.“ statt „…, DACH, anonymisiert.“. Beide Stellen folgen damit `docs/briefings/solar-money-page/referenz.html`.
- **Schrift: Hausschrift bleibt, nur Mono kommt dazu (Stand 2026-09-11, zweiter Durchgang).** Ein erster Durchgang setzte Newsreader als Fließtextschrift auf der Anfragestrecke sowie in Kopf und Fuß. Das ist zurückgenommen: die Serif lief auf dem Schirm weniger klar als Satoshi, und Klarheit schlägt Vorlagentreue. Newsreader ist samt seiner vier woff2-Dateien wieder entfernt. Geblieben ist **IBM Plex Mono** (sechs woff2, latin und latin-ext, `@font-face` in `style.css`, kein Google-CDN) für Ziffern, Marken, Labels und Tabellenspalten — dort trägt die Schrift tabellarische Ziffern, mit denen Rechner-, Leiter- und Fallspalten exakt untereinander stehen. Der Preload in `functions.php` ist wieder routenunabhängig Satoshi plus Figtree; Plex bekommt keinen Preload, weil es nicht das LCP-Element trägt.
- **Kopf und Fuß im selben Farbklima (2026-09-11).** `site-header-premium.css` und `site-footer.css` definieren ihre Tokens weiterhin lokal, zeigen jetzt aber auf den Gutachten-Standard: Grund `#17140f` statt `#0b0f12`, Akzent `#ef8b4d` statt `#e08a3c`. Die Textschrift bleibt in beiden Figtree; nur die Mono-Rolle liegt jetzt bei IBM Plex Mono statt bei der Systemschrift. Kontraste auf dem neuen Grund: Text 16,3:1, Mid 10,2:1, Dim 7,8:1, Akzent 7,4:1 — jeder Wert gleich hoch oder höher als vorher. Das ist der erste Schritt einer gestaffelten Umstellung: bis die übrigen Routen nachziehen, steht der neue Kopf bewusst auch über Seiten im alten Kleid.
- **Marktcheck-Zusage vereinheitlicht (2026-09-11).** `hu_marketcheck_reply_label()` ist die Canon-Quelle der Marktcheck-Frist. Frühere Growth-Audit-/CJA-Ausgaben, die diese Zusage duplizierten, sind inzwischen entfernt; aktive Marktcheck-Copy liest den Canon oder die aktuelle Funnel-Implementierung.
- **Startseite: Orientierung über drei Angebotswege (Umsetzung 2026-09-10, Veröffentlichung noch ausstehend).** `front-page.php` führt vom kompakten Hero mit bestehendem Porträt zu drei gleich gewichteten Einstiegen, dem begrenzten Solar-Projektbeleg, persönlicher Arbeitsweise und dem allgemeinen Kontaktabschluss. Die Solar-Karte führt zur Branchenseite `/solar-waermepumpen-leadgenerierung/` ohne Formularanker; deren Marktcheck bleibt der nächste Schritt im Energie-Funnel. Freelancer- und White-Label-Ziele bleiben erhalten. Die große Hero-Pipeline, undatierte Lighthouse-Zahlen, die nicht bestätigte Tracking-Aktivitätsbehauptung und die widersprüchliche Abschlussquote entfallen aus der Homepage. Anfragekosten, Anfragezahl und Zeitraum kommen weiterhin aus dem E3-Canon. `#lebender-beweis` bleibt als kleiner Bereich mit Links zu WordPress-Arbeitsproben, Tracking-Fachseite und PageSpeed erhalten; `#abgrenzung` trägt jetzt die Arbeitsweise. Die Seite hat weiterhin kein Formular und kein eigenes JSON-LD/FAQ-Schema. Ihre WebPage-Texte beziehen sich auf die gefilterten Homepage-Meta-Helper statt auf alte Editor-Texte. Die neuen CSS-Regeln gelten nur innerhalb `.hu-hp--orientation`; die gemeinsam geladenen Energy-Stile bleiben erhalten. Die Homepage-Footer-Zeile schließt Neubau und Weiterentwicklung ein. Hook-Wechsel und Abnahmeumfang: siehe folgenden Absatz. Dieser Eintrag ersetzt ältere Homepage-Architekturbeschreibungen weiter unten.
- **Homepage-Hooks und Browserbasis:** `home_door_energy` bezeichnet jetzt den Besuch der Energy-Angebotsseite und ersetzt den bisherigen Formular-Sprung `home_door_marktcheck`. Die vorhandenen Freelancer-/White-Label-, Case-, Tracking-, PageSpeed-, Hero-Sprung- und Abschluss-Hooks bleiben. Neu sind `home_proof_wordpress` und `home_about`; `home_fit_project` und `home_close_about` entfallen mit ihren alten Flächen. Es wird kein Analytics-Code und keine externe Event-Konfiguration ergänzt. Bei späterer Auswertung die neuen Auswahlklicks nicht als Marktcheck-Starts zählen. Frontend-Grundlage: lokal vorhandener Modern-Web-Guide `performance/optimize-image-priority`; der CLI-Abruf war nicht erfolgreich. Das bestehende WebP-Porträt erhält feste Maße, responsive Quellen und hohe Ladepriorität; Browser ohne Prioritätsunterstützung laden es regulär. Alle Inhalte und Links sind ohne JavaScript nutzbar; das vorhandene Sprunglink-Skript respektiert reduzierte Bewegung.
- **Der globale Footer hat zwei Lautstärken und 13 Links — eine einzige Variante für alle Seitentypen.** Dieser Punkt hat Vorrang vor älteren Footer-Beschreibungen weiter unten. Die früheren Varianten `ft--energy-minimal`, `ft--audit-minimal` und `ft--home` samt Routenblock (`ft__routes`) und CTA-Band sind zusammengelegt; `template-parts/site-footer.php` rendert nur noch `.ft`. Reihenfolge: Routen (Kicker „Was trifft zu?", drei Ich-Sätze, je ein kommerzieller Weg), Direktzeile (Mail, Telefon, Kontaktformular), Verzeichniszeile (Über Haşim · Fallstudie Solar · Blog · Glossar · Impressum · Datenschutz) und Absenderzeile (© · Sitz · Arbeitsweise · Antwortzeit aus `hu_response_promise( 'value' )` · Messung, rechts das LinkedIn-Icon). Entfallen sind die drei Verzeichnisspalten samt Spaltenköpfen, die Markenzeile mit Claim und das Messprotokoll als eigener Streifen: die Leistungsziele stehen sitewide im Hauptmenü, „Ergebnisse & Case Studies" und die Fallstudie waren zwei Links auf einen Fall, und die Protokollangaben sind Absenderangaben. Der Sammel-CTA „Projekt anfragen" entfällt in beiden Rollen — als CTA-Band und als Verzeichnis-Link —, weil die Routen den Weg oben schon benennen. `hu_get_contact_phone()` (`inc/canon/messaging-canon.php`) ist die Canon-Quelle der Telefonnummer; Impressum und Datenschutz tragen sie weiterhin je einmal hart im Template. Das Stylesheet `assets/css/site-footer.css` arbeitet mit komponentenlokalen `--ft-*`-Tokens ohne `--nx-*`-Abhängigkeit, führt genau eine Vollbreite-Trennlinie (über der Verzeichniszeile), keinen Flächenverlauf und keinen Radius außer am Fokusring. `.ft__sr` ist ersatzlos entfallen; die Navigationen tragen ihren Namen jetzt über ARIA statt über eine versteckte Überschrift, „Footer-Navigation" steht nirgends mehr im Dokument. Der Header der Energy- und Audit-Routen bleibt unverändert unterdrückt.
- **Historische Header-Farbabweichung geschlossen.** Die dafür verantwortlichen alten Header-Stylesheets sind entfernt; der aktuelle Standardkopf bezieht seine Farben aus `system.css`.
- Die wichtigsten Nicht-Blog-Routen sind repo-seitig als zusammenhängender Linkgraph geführt: Der Systemphasen-Link der Startseite führt in die Solar-/SHK-Money-Page statt in den Agentur-Nebenpfad, der globale Footer bindet `/glossar/` an (seit dem Zwei-Lautstärken-Umbau als kurzer Anker „Glossar" statt mit beschreibendem Anker — der Header-Menüpunkt trägt denselben Anker), `/hasim-uener/` führt vom E3-Proof zum dokumentierten Case und `/server-side-tracking-b2b/` verbindet Messkonzept, Solar-Case und technischen Gesamt-Stack.
- Das Theme laeuft jetzt ueber einen getrennten CI/CD-Pfad: `.github/workflows/ci.yml` prueft PHP-Syntax, Workflow-YAML und das deploybare Theme-Paket; `.github/workflows/deploy.yml` deployed erst nach erfolgreichem CI-Run fuer einen Push auf `main` oder optional manuell per Dry-Run bzw. `workflow_dispatch`.
- Zentrale Theme-Module fuer Assets, SEO-Meta, Schema, Shortcodes, Portal und Snippets sind versioniert.
- Die Product-Default-Logik führt kalten Solar-/SHK-Traffic zum Marktcheck auf `/solar-waermepumpen-leadgenerierung/#marktcheck`; die versionierte Homepage ist repo-seitig als SHK-Hauptlandingpage auf Marktcheck/Analyse ausgerichtet und führt E3-Proof-Zahlen über den zentralen Canon. Der Homepage-Hero priorisiert den Analyse-CTA vor den Proof-Zahlen und zeigt rechts nur noch eine reduzierte Lead-System-Skizze aus Nachfrage, eigener Strecke und passender Anfrage. Der Energy-Kontext und die wiederverwendbaren Energy-CTAs priorisieren den Marktcheck mit Fit-Entscheid; der globale Standard-Header hält die drei Geschäftswege gleichrangig und nutzt „Projekt anfragen“ als generischen Direkt-CTA. Der schriftliche Marktcheck-Befund trägt die kanonische Antwortzeit (`hu_marketcheck_reply_label()`); Success-/CRM-Texte nennen keine abweichende Frist mehr. Der wichtigste Proof-Link zeigt direkt auf `/case-study-solar-leadgenerierung/` statt auf den breiten Ergebnisse-Hub. Technische Nebenpfade wie Core Web Vitals und kostenlose Tools sind aus der Footer-Navigation entfernt. `/wordpress-agentur-hannover/` bleibt als sekundärer Footer-Link für lokale Money-Page-/SEO-Signale erhalten; White-Label steht repo-seitig als Geschäftsweg „Für Agenturen“ in der Hauptnavigation und ist als `index, follow` sowie Sitemap-Eintrag behandelt.
- Die Solar-/SHK-Money-Page bietet repo-seitig direkt nach dem transparent begrenzten Kostenbaustein-Vergleich den kleineren Einstieg `Sofortkontakt-Setup` für 790 EUR netto an (`#einstieg`, Einrichtung in fünf Werktagen); der CTA bleibt im bestehenden Marktcheck-Flow und die FAQ bildet den Einstieg mit ab. Aufbauwerte kommen aus dem Pricing-Canon, Portalwerte sind als Rechenbeispiel gekennzeichnet, und Media, Optimierung, Abschlussquote sowie Vertriebsaufwand werden nicht als Schein-Ersparnis verrechnet. Die Hero-Referenzgrafik zeigt nur die belegten E3-Werte Start 150 EUR, Aufbaukorridor 70 bis 100 EUR und Monat 6 mit 22 EUR statt eines interpolierten Monatsverlaufs. Die Seite verwendet keine Regions-Verknappung mehr. Auf dieser Route wird der globale Header vollständig unterdrückt; die seiteninterne Sprungnavigation übernimmt nach dem Hero sticky die Orientierung. Der projekt-eigene Header ist global die einzige serverseitig gerenderte Theme-Navigation; der zuvor nur per CSS versteckte Blocksy-Parent-Header wird über `blocksy:builder:header:enabled` abgeschaltet. Das WordPress-Menü normalisiert weiterhin die alten `page_id`-Ziele 13035 und 14283 auf die kanonischen SEO-/Ergebnisse-Routen, leitet die alten Query-URLs per 301 weiter, rendert reine Menügruppen als nicht klickbare Labels und entfernt die unklaren Leerziele `Performance` sowie `Tracking & CRO`.
- Das Gateway-Band direkt unter dem Homepage-Hero trägt nur noch die beiden Einstiege der Hero-Zielgruppe (Marktcheck, Case Study); Agentur und White-Label stehen vollständig im Final-Routing am Seitenende. Beide Sichten lesen dieselbe Kartenquelle `$home_routing_gateways`. Die sieben Homepage-FAQ liegen jetzt als `$home_faq_items` im Template und speisen sichtbaren Text und einen eigenen `FAQPage`-Knoten gemeinsam; die Startseite ist dafür in `$template_owns_faq_schema` (`inc/org-schema.php`) eingetragen, damit kein zweiter Knoten aus Editor-Inhalt entsteht. Die Antwortzeit auf den Marktcheck kommt aus `hu_marketcheck_reply_label()` und gilt für Startseite, Money Page und die geteilte Footer-/CTA-Microcopy; das Kurzlabel im Menü wird aus demselben Getter gebaut. Seit 2026-08-20 schreibt `nexus_energy_nav_cta_label()` Menü-CTAs nur noch im Energy-Kontext auf dieses Label um, und `nexus_get_site_header_fallback_items()` liest die Navigation aus `hu_get_primary_navigation_contract()` statt aus einer zweiten, handgepflegten Liste. `nexus_render_site_header_menu()` ist als zweiter Renderer ohne Aufrufer entfallen.
- Die Homepage-Sektionen sind repo-seitig erweitert um einen visuellen System-Flow (`06 / Das System — visuell`, Anker `#flow`, Tracking-Section `homepage_system_visual`): zwei Spalten — links Portal-Chaos mit ~4.800 € / Monat, rechts eigene Strecke (Money Page → Vorqualifizierung → Privacy-first Tracking) mit den E3-Canon-Zahlen als Ergebnis. About (`#about`) liegt jetzt vor FAQ (`#faq`), damit Personenmarke und Trust-Anker direkt vor der Einwand-Behandlung greifen; der CTA hinter dem FAQ-Block führt zusätzlich auf den Marktcheck.
- Repo-seitig existiert zusätzlich die noindex-Testroute `/startseite-wow/` für eine visuell stärkere Homepage-Variante. Die Route ist virtuell im Theme registriert, benötigt keinen WordPress-Editor-Eintrag, lädt `homepage-redesign.css` plus `homepage-wow.css/js`, führt weiter primär in den Marktcheck und gibt `noindex,nofollow` per Meta sowie `X-Robots-Tag` aus. Die kanonische Startseite `/` bleibt davon unverändert.
- Der Marktcheck trägt repo-seitig eine Trust-Microcopy „kostenlos & unverbindlich" direkt an den primären Einstiegen: als zusätzliche Hero-Note auf der Homepage (vor der bestehenden Analyse-Note) und in der Solar-Quiz-Card als erstes SSR-Bullet plus JS-Fineprint. Der aktive Intake fragt in vier Auswahlstufen Leistungsfokus, wirtschaftlichen Projekt-Fit, Vertriebsverantwortung und Umsetzungshorizont ab; erst danach folgen die geschäftlichen Kontaktdaten. Er speichert ausschließlich die gegebenen Antworten und leitet keine CPL-, Anfragevolumen- oder Engpasswerte daraus ab. Die anschließende Anfragesystem-Analyse bleibt davon sprachlich getrennt; die Freemail-Validierung begründet die Firmen-Domain-Anforderung und nennt einen direkten E-Mail-Ausweg. Wie lang der Intake ist, versprechen Startseite, E3-Case und die Testroute `/startseite-wow/` aus dem Diagnose-Canon (`HU_MARKETCHECK_STEPS`, `HU_MARKETCHECK_MINUTES`) statt als eigene Copy; der Lead-Path-Smoke bricht ab, sobald Canon und `QUIZ_STEPS` auseinanderlaufen. Ebenso kommt der Aufbaupreis auf Startseite, `/eigene-leadgenerierung-vs-portale/` und `/kunden-gewinnen-solarteure/` jetzt aus dem Pricing-Canon (`hu_foundation_price_display()`, `hu_foundation_total_display()`); die zurückgezogene Spanne `12.000 – 18.000 €` steht auf keiner Route mehr, und die Vergleiche nennen Media, Optimierung und Vertriebsaufwand ausdrücklich als nicht enthaltene Kostenbausteine.
- Die lokale Money Page `/wordpress-agentur-hannover/` folgt repo-seitig jetzt einem Spezialisten-Flow: Hannover bleibt Standort- und SEO-Anker, die Zielgruppe wird aber fachlich über anspruchsvolle B2B-Angebote bestimmt. Der Hero führt das Ziel-Keyword explizit in der H1, grenzt klassische Webdesign-/Relaunch-Agenturleistung direkt aus, positioniert WordPress als technische Basis für messbare B2B-Anfragen und routet die Projektprüfungs-CTAs in den scoped Kontaktflow `/kontakt/?type=project&focus=followup_scope`; sekundär führt er zum E3-Case. Für Solar-, Wärmepumpen- und Speicher-Kontext gibt es jetzt zusätzlich bereits im Hero einen direkten Textlink zum Marktcheck. Danach folgen Spezialisierung, Anfragesystem-Methode, Methodenbibliothek, E3-Proof, Abgrenzung, Wartung und laufende Betreuung, Solar-Brücke, Standort, FAQ und finale Projektprüfung. Der Abschnitt `#wordpress-wartung` stellt Pauschalvertrag, Stundenpaket, Hosting-inklusive und Eigenbetrieb ohne erfundene Marktpreise gegenüber, grenzt klassische Wartungsverträge mit zugesicherter Reaktionszeit oder 24/7-Verfügbarkeit aus und führt für planbare Weiterentwicklung zur Freelancer-Route; die bestehende FAQ und das Service-Schema spiegeln dieselbe Retainer-Grenze. Die konsolidierten Service-Anker `#technisches-seo` und `#wordpress-wartung` existieren im Template und werden im Service-Schema nur auf vorhandene Seitenbereiche gemappt. Der Standortblock zeigt zusätzlich die lokalen Signale Pattensen bei Hannover, Region Hannover/Niedersachsen und DACH-Remote; die lokale `areaServed`-Information liegt zentral am Organization-/LocalBusiness-Schema und wird nicht mehr in jedem Service-Schema dupliziert.
- Die Personenseite liegt auf `/hasim-uener/` (vorher `/uber-mich/`, 301 in `nexus_get_legacy_offer_redirect_map()`). Route statt Template-Auswahl: `page-hasim-uener.php`, gesetzt von `nexus_maybe_ensure_about_page()`. Sechs Blöcke — Hero, vier Stationen, Haltung, fünf Arbeitsregeln, CTA, Schlusszeile. Die persönliche D2C-Station steht auf unternehmerischem Risiko (`Eigenes Geld`, Onlineshop 2019–2023) statt auf einer zweiten Kennzahl; vom getrennten E3-Canon steht nur der Prozentwert, seit 2026-09 als `−85 %` mit Richtung statt als nackte Zahl, während Betrag, Zeitraum, Leadzahl und Region bewusst rausbleiben. Die E3-Station führt mit einem beschreibenden Trust-Link zum dokumentierten Solar-Case. Zwischen Haltung und CTA stehen fünf Arbeitsregeln (`#schwaechstes-glied`, `#defekt-oder-stellschraube`, `#zugaenge`, `#ueberzeugen`, `#werkzeuge`) statt einer zweiten Biografie: die Timeline trägt den Lebenslauf, die Regeln sagen, wonach entschieden wird. Sie haben die frühere Kompetenzleiste (WordPress · Tracking · Ads · CRO · Automatisierung) ersatzlos abgelöst, weshalb auch die Meta-Description der Route diese Aufzählung nicht mehr führt. Die Anker-IDs sind fest vergeben und nicht aus den Überschriften erzeugt, damit sie als öffentliche Sprungziele eine Textänderung überleben. Die Gliederung ist bewusst flach: H1 ist der Name, die fünf Regeln sind die einzigen H2 im Fließtext, dazu die CTA-Überschrift — die Stationen-Labels sind seit 2026-09 `<p><strong>` statt `<h2>`, weil sie eine Grafik beschriften und die Gliederung mit fast identischen Wörtern verdoppelt hatten. Der Abschnitt ist reiner Fließtext ohne Karten, Kästen oder Icons und trägt genau ein gestalterisches Element, die Akzent-Haarlinie des Zitatblocks, einmal für den ganzen Abschnitt. Belege in `docs/qa/hasim-uener-arbeitsregeln/`. Der Abschluss verzweigt nach Zielgruppe: Betriebe in die generische Projektanfrage (`hu_get_commercial_route( 'project_request' )`, nicht der Energy-Marktcheck), Agenturen in das White-Label-Angebot; LinkedIn und E-Mail bleiben leise Kontaktwege, die Schlusszeile verweist als einziger Außenlink auf `hasimuener.org`. Zwei Zusagen in den Karten sind jetzt einlösepflichtig: Ausfall wird am selben Tag gemeldet, und Scope und Preis stehen vor dem Start fest — laufende Kapazität erst nach dem Erstprojekt, passend zum „Kein Blind-Retainer"-Modell auf `/whitelabel-retainer/`.
- Service-Seiten, Blog, Kategorie-Archive, Footer-CTA und Trust-Bausteine sind im Repo. Der frühere Tools-Hub bleibt als Legacy-Code erhalten, wird öffentlich aber nicht mehr gerendert.
- Der Blog ist repo-seitig jetzt stärker als Solar-/Wärmepumpen-Supporting-Layer ausgerichtet: Blog-Index und Kategorie-Archive rendern als bildlose, typografische Editorial-Flächen mit echten Kategorie-Links, zentralen Kategorie-SEO-Metas, thematischen Vertiefungslinks, CollectionPage-/ItemList-Schema und Marktcheck-Führung. Der Blog-Index gruppiert die Beiträge zusätzlich frontendseitig in die vier Themencluster Portal-Kritik, CRO, Tracking und Performance; nach dem dritten Artikel sitzt eine Inline-Marktcheck-CTA-Card. Single-Artikel nutzen einen reduzierten Editorial-Hero, führen über eine Kontext-Brücke, eine strukturierte Next-Step-Zone und typografischen Related Content zu Kategorie, Money Page, Marktcheck und E3-Proof. Die alte Kategorie `wordpress-growth-agentur` bleibt als Datenbank-/URL-Slug stabil, wird öffentlich aber als `WordPress-Systeme` gelabelt; alte Autor-Bio-Copy mit Growth-/Shopify-Positionierung wird theme-seitig durch den kanonischen Personen-Text ersetzt. Artikel-Hero können weiter eine theme-seitig generierte Titelgrafik als Fallback nutzen.
- Der Beitrag `/checkfox-solar-waermepumpe-einordnung/` nutzt innerhalb des generischen `single.php` eine slug-spezifische, vollständig repo-eigene Präsentationsschicht. Sie ist auf eine kurze Entscheidungsseite reduziert: kompakter Hero, sticky Sprungnavigation (52 px, `<nav aria-label="Auf dieser Seite">`) und genau fünf Sektionen — Kostenrechner (Portal-CPO-Einzelszenario), Vertragscheck als standardmäßig geschlossenes `<details>` mit acht Prüfpunkten, Gegenüberstellung Portal-Modell vs. eigener Anfrageweg, Quellen-/Transparenzblock und ein abschließender Marktcheck-CTA. **Der gespeicherte WordPress-Editor-Inhalt wird auf dieser Route nicht mehr ausgegeben** (weder sichtbar noch im DOM); er bleibt in der Datenbank, ist aber für die Live-Seite ohne Wirkung — inhaltliche Änderungen laufen ab jetzt über `template-parts/checkfox-decision-cockpit.php`. Entfallen sind der Kurzantwort-Umschalter, die Sektion „Seriosität versus Wirtschaftlichkeit", der separate Prozessvergleich, die doppelten „Wann sinnvoll"-Blöcke und der angehängte Langartikel; die Kontextzeile des Blog-Headers ist auf dieser Route zugunsten der Sprungnavigation unterdrückt. Checkfox-Assets, personenbezogene Speicherung und provider-spezifische Preis-/Vertragsbehauptungen werden nicht eingeführt. URL, Canonical, Veröffentlichungsdatum, Indexierung und SEO-Metadaten bleiben im bestehenden Post-/SEO-Resolver-Pfad.
- Der Beitrag `/aroundhome-solar-einordnung/` nutzt ebenfalls eine slug-spezifische repo-eigene Präsentationsschicht und rendert den gespeicherten Editor-Langartikel nicht mehr. Die Seite beantwortet den Suchintent `Aroundhome Kosten für Handwerker` als Kosten- und Vertragsentscheid: kompakter Hero, Trennung von öffentlicher Anbieterangabe, individuellem Vertrag und Betriebsökonomie, CPO-Rechner ohne voreingestellte Aroundhome-/Branchenwerte, sieben schriftlich zu klärende Vertragspunkte, Evidenzleiter für öffentliche Erfahrungen, Portal-/Hybrid-/Eigenvergleich, klar abgegrenzter E3-Mechanismusbeleg, gemeinsame sichtbare FAQ-/FAQPage-Datenquelle und Quellenblock mit Anbieter- und Partner-AGB-Links. `single.php` unterdrückt auf der Route generischen Hero, Editor-Inhalt, Rating, Newsletter, Share-Rail, Related Content und Footer-CTA; eigene Aroundhome-Assets und Tracking-Bezeichner laden nur dort. URL, Canonical, Post-Metadaten, Breadcrumb und Article-/WebPage-Schema bleiben im zentralen Resolver-Pfad.
- Die `/system-diagnose/` ist repo-seitig retired und leitet per 301 auf `/solar-waermepumpen-leadgenerierung/#marktcheck`. Der `Growth Audit`, die generischen Audit-Aliasse und `/anfrage/` leiten als geschützte Legacy-Einstiege ebenfalls in diesen Marktcheck-Einstieg. Alte Tool-, ROI- und Service-Slugs werden nicht mehr hart per 301 erzwungen, sondern als `410 Gone` aus dem öffentlichen Crawl-Surface genommen; alte WGOS-Pfade bleiben noindex/access-protected und sitemap-excluded, solange sie intern noch genutzt werden.
- Repo-seitig gibt es jetzt einen E3-Proof-Canon in `blocksy-child/inc/canon/e3-proof-canon.php`: `150 €` vorher, `22 €` nachher, `über 85 %` Delta, `1.750+` qualifizierte Anfragen, `15 %` Abschlussquote und `6 Monate` Zeitraum.
- Der versionierte Ergebnisse-Hub bleibt als vorhandener Proof-Hub erreichbar, ist aber nicht mehr die primaere Proof-Fläche in Header oder Footer. Der kaufnahe Proof wird ueber `/case-study-solar-leadgenerierung/` direkt verlinkt und nutzt die kanonischen E3-Zahlen aus `blocksy-child/inc/canon/e3-proof-canon.php`. Ergebnisse-Hub und E3-Case geben jetzt zusätzlich ein sekundäres internes Signal auf `/wordpress-agentur-hannover/` für B2B-WordPress-Kontexte außerhalb des Solar-/Wärmepumpen-Fokus.
- Die anonymisierte Methodik-Case-Page läuft kanonisch über `/case-study-solar-leadgenerierung/`. Die Slug-Migration der editor-eigenen WordPress-Seite passiert repo-seitig automatisch (`nexus_maybe_ensure_case_study_page` in `inc/helpers.php`); die alten firmenbenannten Slugs `/e3-new-energy/`, `/case-e3/` und `/case-studies/e3-new-energy/` werden per 301 auf die anonymisierte URL geführt. Die Zielseite bleibt bis zur Freigabe des Anonymisierungs-Holds `noindex, follow`.
- Die frühere `Growth Audit`-Route `/growth-audit/` ist öffentlich deaktiviert und leitet per 301 auf den Marktcheck. Dasselbe gilt für die alten generischen Audit-Aliasse und `/wordpress-tech-audit/`; kostenlose Tool-Pfade liefern `410 Gone` statt als eigene Seiten oder erzwungene 301-Einstiege weiterzuleben.
- **Historischer Header-Eintrag superseded.** Die damalige Premium-/Sheet-Architektur wurde durch die aktuelle `.leiste`-Navigation ersetzt; Growth-Audit besitzt keine Sonderführung mehr. Maßgeblich ist der Abschnitt „Designsystem, Kopf, Fuß und Startseite“ weiter oben.
- Die frühere noindex-Kampagnenseite `/audit-linkedin/` ist repo-seitig retired. Eigenes Template, virtuelle Route und Assets sind entfernt; der Slug liefert `410 Gone` statt weiter als 301- oder noindex-Seite im öffentlichen Surface zu bleiben.
- Repo-seitig liegt zusätzlich eine B2B-Branchen-Landingpage für Solar-, Wärmepumpen- und Speicher-Anbieter kanonisch unter `/solar-waermepumpen-leadgenerierung/`. Sie bleibt SEO-, Kontext- und Lead-Zweig; der Hero nutzt einen fünfstufigen B2B-System-Intake mit vier echten Fit-Signalen und anschließendem geschäftlichem Datenpfad. Inhabergeführter Vertrieb gilt nicht mehr fälschlich als fehlende Vertriebsinfrastruktur; nur eine ausdrücklich fehlende Verantwortlichkeit oder ein Vermittlungs-/Kurzfristmodell führt im neuen Pfad zu `nurture`. Der Intake sendet als `B2B-System-Intake` über den bestehenden Audit-CRM-Endpoint.
- Repo-seitig liegt zusätzlich eine Intercept-Landingpage unter `/solar-leads-kaufen-alternative/` für den Kauf-Suchintent („Solar Leads kaufen", „Photovoltaik Leads kaufen"). Sie führt seit 2026-08-08 als zehnteiliger, strikt alternierender Hell-/Dunkel-Buying-Guide von Beschaffungsmodellen und Auftragsökonomie über Qualitätskriterien zur eigenen Anfrage-Infrastruktur. Der Hero qualifiziert Solar-, Wärmepumpen- und Speicher-Anbieter im DACH-Markt; der generische Wärmepumpen-Intent bleibt per Kontextlink bei `/waermepumpen-leads/`. Proof bleibt auf den E3-Canon (`150 € → 22 €`, `1.750+`, `15 %`, `6 Monate`) begrenzt. Primärziel ist durchgehend der Marktcheck mit Fit-Entscheid auf `/solar-waermepumpen-leadgenerierung/#marktcheck`; redaktionelle Sekundärziele sind Kostenstudie, TCO-Vergleich, CPL-Analyse und Case. Das Page-Delta `solar-leads-kaufen-alternative-page.css` lädt nur auf dieser Route über dem gemeinsam genutzten Intercept-CSS. Auto-Provisioning, Force-Template und SEO-Meta (`hu_get_forced_singular_seo_map()`) sind im Theme.
- Repo-seitig sind zwei weitere SEO-Sub-Pages versioniert, die auf demselben `.hu-intercept`-Grundsystem aufsetzen: `/b2b-solar-leads/` ist seit 2026-08-08 als eigenständiges Gewerbe-PV-Qualification-Gate gestaltet. Ein kompakter Split-Hero erklärt die Vorqualifizierung vor dem Termin; danach folgen Canon-Proof, vier Entscheidungssignale, eine semantische Vergleichstabelle, Pain-Economics, fünf Systembausteine, Fit/No-Fit, die sichtbare Leiter `Marktcheck → Anfragesystem-Analyse → Umsetzung`, FAQ und ein eindeutiger finaler Marktcheck-CTA. Das strikt gescopte Page-Delta `b2b-solar-leads-page.css` lädt nur auf dieser Route; Zahlen stammen aus E3- und Pricing-Canon, die eigene Fit-Schwelle `ab 50.000 €` ist ausdrücklich kein Marktmittelwert. `/eigene-leadgenerierung-vs-portale/` bleibt die Vergleichsseite mit 8-Kriterien-Matrix und TCO-Überschlag über 24/36 Monate. Beide routen primär in den Marktcheck und tragen Service- sowie FAQPage-Schema.
- `/server-side-tracking-b2b/` ist seit 2026-08-14 die transaktionale Money-Page für generisches Server-Side-Tracking; `/ga4-tracking-setup/` bleibt die vorgelagerte Event-, Consent- und Messkonzept-Grundlage. Die Route hat zwölf verdichtete Abschnitte: Split-Hero mit Fit-/Scope-Vorprüfung und überprüfbarem Rahmen (Preis, Paralleltest, Kundeneigentum), drei budgetrelevante Symptome, Client-/Server-Unterschied mit Hybrid-Hinweis, Fit und Non-Fit, Datenfluss-Grafik, sechs Umsetzungsergebnisse, drei Pakete, Tracking Care, vier Umsetzungsschritte, Sicherheit, acht FAQ und Anfrageformular. Die unbelegte Spanne „20 bis 40 Prozent mehr gemessene Conversions" ist aus sichtbarer Copy und FAQ-Schema entfernt; Proof bedeutet jetzt ausschließlich Paralleltest, benannte GTM-Versionen und Ergebnisse in den Konten des Kunden. Preise, Lieferfenster, enthaltene Korrekturzeit und Antwortzeit liegen zentral in `inc/canon/pricing-canon.php` und speisen Template, FAQ und Meta-Description. Das Formular schreibt unverändert über `nexus/v1/contact-request` mit `type=project` und `focus=tracking`; primär sichtbar sind Name, geschäftliche E-Mail, Website und Messproblem, optionale technische Angaben liegen in nativer Progressive Disclosure. Die mobile Sticky-CTA pausiert, sobald `#anfrage` sichtbar ist. Die Route ist kein Mitglied des Solar-Cluster-Footer-Moduls mehr; sie verlinkt intentklar auf `/ga4-tracking-setup/` und `/server-side-tracking-gtm/` sowie für den Solar-/SHK-Kontext auf den dokumentierten Case und `/stack-solar/`. Der Marktcheck bleibt der sekundäre Branchenpfad. Schema-Eigentum bleibt beim Template: BreadcrumbList, Service mit drei Offers und FAQPage.
- `/stack-solar/` bleibt eine indexierbare sekundäre Methodenroute, nicht ein eigenständiges Paket. Die fünf Schichten Frontend, Hosting, Server-Side Tracking, CRM und projektspezifische Vorqualifizierung werden erst nach Marktcheck und Anfragesystem-Analyse eingeordnet. Unbelegte CPL-, SQL-, Match-Rate-, Speed-, Lieferzeit- und Multiplikator-Claims sind aus sichtbarer Copy, FAQ und TechArticle-Schema entfernt; Ergebniswerte stammen ausschließlich fallgebunden aus dem E3-Canon. Die Route verlinkt zurück zur Solar-/SHK-Money-Page, zur Tracking-Money-Page, zum dokumentierten Case, zum Marktcheck und für Agenturen zum getrennten White-Label-Einstieg.
- Repo-seitig sind zwei zusätzliche Pillar-Pages für Topical Authority versioniert, die ebenfalls das `.hu-intercept`-Layout teilen: `/lead-funnel-solar/` (MOFU/BOFU-Pillar mit 5-Stufen-Funnel-Architektur für interne Marketing-/Vertriebsleiter) und `/kunden-gewinnen-solarteure/` (TOFU-Pillar mit Mythen-Aufklärung, 5 systematischen Hebeln und strikt B2B-qualifiziertem Fit-Check). Beide verlinken kontextuell auf die anderen SEO-Sub-Pages (Server-Side Tracking, Vergleich, Intercept) und routen primär in den Marktcheck.
- Die Legacy-Route `/roi-rechner/` ist kein erzwungener 301-Redirect mehr und liefert repo-seitig `410 Gone`. Die frühere automatische Papierkorb-Cleanup-Funktion ist entfernt.
- **`/solar-leads-kaufen-lohnt-sich/` leitet seit 2026-09-04 per 301 auf `/solar-leads-kaufen-alternative/`.** Grund ist belegte Kannibalisierung, nicht Stille: Der Pillar-Post rankte im GSC-Export 28d (2026-07-30) auf Pos. 45–81 für `leadgenerierung photovoltaik`, `wärmepumpen leads`, `solar leads` und `leads solar` — vier Queries, die `docs/seo/query-ownership.csv` anderen Seiten zuschreibt, während er selbst keine besitzt. Der alte Slug `/photovoltaik-leads-tco-rechnung/` zeigt jetzt ebenfalls direkt auf das Endziel, statt über `hu_blog_pillar_redirect_legacy_slugs()` (Priorität 6) eine Kette zu bilden; der Map-Eintrag läuft auf Priorität 2 und gewinnt. Beide Slugs sind sitemap-excluded. Der Byline-Link im Fachmedien-Gastbeitrags-Entwurf zeigt jetzt aufs Endziel — ein veröffentlichter Backlink ist nicht mehr korrigierbar.
- **Der Sitemap-Ausschluss greift jetzt auch für `post`, nicht nur `page`.** `nexus_get_sitemap_excluded_ids()` löst Slugs über beide Post-Typen auf, der `wp_sitemaps_posts_query_args`-Filter gilt für beide. Bis 2026-09-04 waren alle ausgeschlossenen Slugs zufällig Seiten, deshalb fiel die Lücke nicht auf; ein noindex- oder 301-Blogpost wäre in der Sitemap stehen geblieben und hätte genau das Mischsignal erzeugt, das der Filter verhindern soll.
- **Nicht umgesetzt und warum:** `/ga4-tracking-setup/` und `/server-side-tracking-gtm/` bleiben bestehen. Beide sind ausgebaute Cluster-Seiten in `inc/wgos/wgos-cluster-pages.php` mit eigenem Titel, Lead, Intro und Asset-Links; `/ga4-tracking-setup/` hat zusätzlich das Template `page-ga4.php` und ist der aufgelöste Wert des `tracking`-Schlüssels in `nexus_get_primary_public_url_map()`. Eine Umleitung wäre ein Umbau der Cluster-Registry, kein Redirect — und die Belege tragen ihn nicht: 0 bzw. 2 Impressionen bedeuten Stille, nicht Kannibalisierung. `seo-research/2026-07/reports/gsc-abgleich.md` sagt zu beiden „liegen lassen" beziehungsweise „später prüfen, bis der Solar-Kern steht".
- Der SEO-Sub-Page-Cluster ist um zwei Qualitäts-Pages erweitert, die das `.hu-intercept`-Layout teilen: `/cost-per-lead-photovoltaik/` (CPL-Analyse mit drei Szenarien-Vergleich Portal-Standard, Portal-Exklusiv, Eigenes System) und `/qualifizierte-pv-anfragen/` (Vier-Merkmale-Modell für hochwertige Anfragen plus Warnsignal-Block). Die Pillar-Seite `/solar-leads-kaufen-alternative/` ist um eine sachliche Markt­einordnungs-Sektion mit vier Anbieter-Modellen und einer Qualitäts-Kriterien-Box erweitert (juristisch konservatives Wording, keine Schmähkritik).
- Die vier sachlichen Lead-Anbieter-Markteinordnungen (Wattfox, Aroundhome, DAA, Leadfluss) liegen weiterhin als Publish-Pack-Drafts unter `content/blog-drafts/`; zusätzlich legt `blocksy-child/inc/blog-provider-posts.php` sie einmalig als veröffentlichte WordPress-Beiträge an, inklusive Kategorie, Tags, SEO-Meta, Marktcheck-CTA und interner Verlinkung zu E3 sowie dem Portalvergleich. Bestehende bereits veröffentlichte Beiträge mit gleichem Slug werden nicht inhaltlich überschrieben.
- Für die Anbieter-Einordnungen (Aroundhome, Checkfox, Wattfox, DAA, Leadfluss) rendert `blocksy-child/single.php` eine eigene Variante der bestehenden Article-Context-Box (`.nexus-article-context--provider`, Slug-Liste im Template). Sie ersetzt die Markteinordnungs-Variante an derselben Stelle — es entsteht keine zweite Box. Inhalt: die Suchfrage kurz beantwortet mit getrennter Aussage zu Unternehmensseriosität und wirtschaftlicher Eignung, danach ein Prüfraster aus fünf Fragen an den Anbieter (Exklusivität, Mindestabnahme, Storno, Übergabezeit, Kosten pro Auftrag). Bewusst als Fragen formuliert: Konditionen sind vertragsabhängig, eine Aussage über einen konkreten Anbieter wäre unbelegt. Primärer Anschluss ist der sachliche TCO-Vergleich `/eigene-leadgenerierung-vs-portale/`, sekundär der Marktcheck. Die `data-track-category` beider Kontext-Links folgt jetzt dem Ziel statt der Position — nur der Marktcheck zählt als `lead_gen`, redaktionelle Anschlussseiten als `internal_link`. Die veröffentlichten Beiträge selbst bleiben editor-owned; `blog-provider-posts.php` ist unverändert.
- Der Pillar-Blog-Artikel *„Solar-Leads kaufen: Warum die billigen Anfragen am Ende die teuersten sind"* (Slug `/solar-leads-kaufen-lohnt-sich/`) liegt als deployte Seed-Quelle unter `blocksy-child/assets/content/blog/photovoltaik-leads-tco-rechnung.md` (Dateiname historisch beibehalten). `blocksy-child/inc/blog-pillar-posts.php` legt ihn als veröffentlichten WordPress-Beitrag mit Slug, Kategorie, Tags, SEO-Meta und Featured Image aus `blocksy-child/assets/img/blog/photovoltaik-leads-kaufen-alternative-hero.png` an. **Stand 2026-07-01:** Inhalt von Marketer-Sprache (TCO/CPO/CPL) auf Handwerker-Klartext umgeschrieben, neue such-/schmerzorientierte Überschrift, Slug von `photovoltaik-leads-tco-rechnung` → `solar-leads-kaufen-lohnt-sich` geändert. Der alte Slug wird über die Retire-Liste als Draft aus der Indexierung genommen und per 301 (`hu_blog_pillar_redirect_legacy_slugs`, `template_redirect`) auf die neue URL geleitet. Der frühere Seed-Slug `/photovoltaik-leads-kaufen-alternative/` wird ebenfalls als Draft geführt.
- Die Solar-Money-Page `/solar-waermepumpen-leadgenerierung/` hat repo-seitig einen Themen-Hub `#deeper` zwischen Garantie- und FAQ-Sektion: vier thematische Cluster (Strategie & Vergleich, Lead-Qualität & CPL, Funnel & Tracking, Zielgruppen & Marktbild) mit zehn Inbound-Links auf die SEO-Sub-Pages, einschließlich `/waermepumpen-leads/`. Die System-Sektion führt zusätzlich zur gehärteten technischen Methodenroute `/stack-solar/`. Sticky-Section-Nav führt den `#deeper`-Anker mit; Tracking-Section `deeper` und Click-Action `deeper_cluster_link` für Attribution. Damit ist die interne Verlinkung von der Money-Page in das Topical-Authority-Cluster geschlossen.
- E-E-A-T-Hardening für die Solar-SEO-Sub-Pages: zentrale Helper in `seo-meta.php` (`hu_get_canonical_author_person`, `hu_get_solar_subpage_breadcrumb_schema`, `hu_get_subpage_last_updated_iso/label`) liefern Author-Schema mit `Person.@id` auf `/hasim-uener/#person`, BreadcrumbList-Schema und Last-Updated-Datum aus `filemtime()` des Templates. Jede Sub-Page rendert eine Author-Byline ("von Haşim Üner · Stand: …") direkt unter dem Hero-Lead über `template-parts/seo-subpage-byline.php`, Service-Schema enthält jetzt `author` zusätzlich zu `provider`, und ein zusätzliches BreadcrumbList-JSON-LD wird ausgegeben. `llms.txt` listet den Cluster "Solar- und Wärmepumpen-Leadgenerierung: Themen-Cluster" einschließlich der Wärmepumpen-Route für KI-Crawler.
- Conversion-Pack auf der Homepage und den Sub-Pages live: Die Homepage hat repo-seitig eine zusätzliche Sektion `#deeper` zwischen FAQ und Final-CTA mit denselben vier thematischen Clustern wie auf der Money-Page und zehn Inbound-Links in den SEO-Sub-Page-Cluster; CTA-Eyebrow ist auf `11 / Nächster Schritt` umnummeriert. Die Cluster-Subpages laden zusätzlich eine barrierefreie Sticky-CTA-Bar (`template-parts/seo-subpage-sticky-cta.php` + `assets/js/seo-subpage-sticky-cta.js`), die ausschließlich auf Viewports ≤ 760 px nach 240 px Scroll erscheint, einen Marktcheck-CTA mit Touch-Min-Size 44 px führt, einen Dismiss-Button mit `aria-label` bietet, die Entscheidung 24 h via `localStorage` merkt, `prefers-reduced-motion` respektiert und `role="region"` für Screenreader trägt. Body-Padding wird über `--hu-sticky-cta-h` dynamisch gesetzt, damit der CTA keinen Inhalt verdeckt.
- Die Solar-Brücke auf `/wordpress-agentur-hannover/` (Section 09 `#fokusmarkt`) ist repo-seitig zu einem Mini-Hub aufgewertet: unter dem primären Marktcheck-CTA liegt ein kompakter `.wp-agentur-deeper`-Block mit vier strategischen Sub-Page-Links (Solar Leads kaufen Alternative, Server-Side Tracking B2B, B2B Solar Leads, Cost per Lead Photovoltaik). Damit ist der interne Verlinkungs-Loop zwischen den beiden Money-Pages geschlossen und die Agentur-Page verteilt PageRank in den SEO-Sub-Page-Cluster. Styling im Light-Layout der Agentur-Page mit Hover-Lift und Accent-Border.
- Die Glossar-Registry ist repo-seitig als interner Verlinkungsmotor erweitert: `keywords_match`-Phrasen werden im Blog-Autolinker berücksichtigt, `lead-portale` routet als Alias auf `/solar-leads-kaufen-alternative/`, `solar-leadgenerierung` als Alias auf `/solar-waermepumpen-leadgenerierung/`, `wordpress-agentur-hannover` als Alias auf die lokale Money-Page und strategische Schreibweisen wie `Portal-Abhängigkeit`, `Leads kaufen`, `Aroundhome`, `Wattfox`, `DAA`, `eigene Leads`, `Solar-Leadgenerierung` und `Anzeigen-Anschluss` sind abgedeckt. Autoverlinkte Glossar-Begriffe erhalten die `short_definition` als Hover-Tooltip. Die Glossary-Sync-Version steht auf `2026-05-26-glossary-v4`.
- Die E3-Methodik-Case-Page `/case-study-solar-leadgenerierung/` bekommt repo-seitig eine zusätzliche Sektion `#vertiefung` zwischen `lessons` und `cta`: Section-Eyebrow „Methodik im Detail", H2 „Vier Bausteine, die das E3-Ergebnis möglich gemacht haben." und ein 2-Spalten-Grid mit Links auf `/cost-per-lead-photovoltaik/`, `/server-side-tracking-b2b/`, `/lead-funnel-solar/` und `/eigene-leadgenerierung-vs-portale/`. Stil im paper-2-Layout der Solar-Page mit Copper-Hover-Accent (`--solar-deep`). Damit ist auch der stärkste Proof-Hub in den SEO-Sub-Page-Cluster eingebunden.
- Das SEO-Cockpit klassifiziert die acht Sub-Pages des Solar-Clusters repo-seitig jetzt als eigene Page-Role `seo_subpage` mit Label „SEO-Sub-Page". Die Pfade sind zentral in `hu_get_solar_seo_subpage_paths()` (`inc/seo-meta.php`) hinterlegt, und `nexus_get_seo_cockpit_page_role()` erkennt sie über `hu_is_solar_seo_subpage_path()`. Business-Score 13, Funnel-Score 10 (zwischen Service und System), High-Value-Liste erweitert. Damit erscheinen die Sub-Pages im Cockpit-Reporting nicht mehr als generische `page` (Score 6/4), sondern werden bei Priorisierung, Insights und Lead-Attribution korrekt als Topic-Hub-Assets behandelt.
- Das SEO-Cockpit lädt `top_pages` und `top_queries` im Snapshot jetzt paginiert statt als starre Top-20-Auszüge (`top_pages` bis 150, `top_queries` bis 250) und die zentrale URL-Normalisierung erhält URL-Fragmente wie `#marktcheck`, damit Ankerziele nicht schon im Core zusammenfallen.
- Das SEO-Cockpit löst die effektiven Snippets für die konfigurierte Startseite und den Blog-Index jetzt anhand von `page_on_front` beziehungsweise `page_for_posts` über die zentralen Homepage-/Blog-Helper auf. Admin-Exporte verwenden damit dieselben forcierten Title-/Description-Werte wie das Frontend statt veralteter gespeicherter Post-Meta; andere Singular-Seiten bleiben im bestehenden Resolver-Pfad.
- Visueller Polish auf den Themen-Hubs: H2 auf der Homepage `#deeper` ist jetzt explizit dunkel (war versehentlich weiß auf cream und damit unsichtbar), Card-Titel auf den Themen-Cluster-Cards explizit weiß auf dunklem Card-Hintergrund. Cards bekommen einen warm getönten linearen Gradient, inset-Highlight und Drop-Shadow, Hover-State mit `translateY(-2px)`, glühender Accent-Border und Right-Arrow-Slide-In. `.hu-intercept__byline` auf den Sub-Pages als Pille mit Border und höherem Datums-Kontrast (0.55 → 0.75 Alpha), Sub-Page-Cards mit Hover-Lift und Underline-Animation auf Card-Titel-Links. `prefers-reduced-motion` durchgehend respektiert.
- Versionierte SEO-Title/Description sind repo-seitig auf Keyword-spitzer geschärft: `/solar-waermepumpen-leadgenerierung/` adressiert jetzt explizit `Leadgenerierung Photovoltaik & Wärmepumpe`, `/wordpress-agentur-hannover/` betont `WordPress Agentur Hannover` mit SEO/Tracking/CRO, `/server-side-tracking-b2b/` adressiert seit 2026-08-08 `Server-Side Tracking einrichten lassen` plus GA4/Ads/Consent — „Consent" steht bewusst im Title, weil die Seite laut `docs/seo/query-ownership.csv` vier DSGVO-Queries besitzt und CAPI in die Description ausweicht, `/b2b-solar-leads/` fokussiert ausschließlich Gewerbe-PV (`PV-Termine B2B: qualifizierte Gewerbe-PV-Anfragen`; das generische „Photovoltaik Leads" bleibt aus Title und H1 entfernt, weil es die Query der Solar-Money-Page hierher gezogen hat), `/solar-leads-kosten-studie/` fokussiert `Solar-Leads Kosten 2026` plus CPL/CPO und `/case-study-solar-leadgenerierung/` führt die CPL-Senkung `150 € auf 22 €` sowie die kanonischen E3-Kennzahlen im Title.
- Der historische Kategoriepfad `/category/owned-leads/` wird repo-seitig per 301 auf `/eigene-leadgenerierung-vs-portale/` geführt, weil der Pfad in Live-Logs weiterhin von Googlebot gecrawlt wurde, aktuell aber als 404 endete.
- Die Homepage-SERP-Signale sind repo-seitig auf die aktuelle Solar-/Wärmepumpen-Positionierung gezogen: Title und Description sprechen jetzt eigene Anfragesysteme, Marktcheck, Vorqualifizierung und Portal-Alternative an statt WordPress/B2B als Hauptpositionierung.
- `page-wgos.php` ist repo-seitig kein öffentlicher Sales-Funnel mehr, sondern ein internes `WGOS Client Dashboard` mit Login-/Capability-Schutz (`view_wgos_dashboard` oder `manage_options`), `noindex,nofollow`, Sprint-, Credit-, Aufgaben-, Asset-, Modul- und Dokumentations-Fallbacks. Nicht eingeloggte Nutzer gehen zum Login, eingeloggte Nutzer ohne Berechtigung zurück zur Startseite.
- Die aktiven Service-Clusterseiten für `GA4 Tracking Setup` und `Performance Marketing` sind als versionierte Routen im Theme hinterlegt. Die früheren SEO-, CRO-, Core-Web-Vitals- und Wartungs-Slugs sind nicht mehr öffentliche Primärziele; interne Links zeigen auf kanonische Routen, Assets oder Agentur-Anker, und die alten Service-Slugs liefern `410 Gone`.
- `inc/enqueue.php` nutzt für CSS- und JS-Assets in Live-Umgebungen die Version aus dem Header der deployten Child-Theme-`style.css` als verbindliche Cache-Busting-Version und fällt nur in `local`/`development` auf `filemtime()` zurück. Eine eventuell gesetzte `HU_THEME_VERSION`-Konstante greift nur noch, wenn der Theme-Header keine Version liefert, damit eine veraltete Server-Konstante neue Assets nicht festhält; der alte Instant-Results-Stack bleibt als Legacy-Code vorhanden, wird auf `/growth-audit/` aber durch den 301-Redirect nicht mehr öffentlich geladen.
- Die Service-Clusterseiten zeigen im Proof-Block jetzt bewusst belastbare System-Beweise (`100 % B2B-Fokus`, Diagnosezeit aus dem Canon, `3 Proof-Routen`) statt uebergreifend wiederholter Leistungszahlen, die als generischer Platzhalter gelesen werden koennten.
- Die kanonischen Cluster-Routen und die Kontaktseite `/kontakt/` werden repo-seitig jetzt zusaetzlich als veroeffentlichte WordPress-Seiten mit zugewiesenem Template abgesichert, damit oeffentliche URL, native Sitemap, Canonical-Logik und Admin-Aufloesung konsistent bleiben.
- Repo-seitige Primary-Links auf aktive Cluster- und Service-Ziele zeigen jetzt direkt auf ihre kanonischen Routen, Asset-URLs oder Agentur-Anker und fallen nicht mehr auf kuerzere Legacy-Slugs wie `/seo/` zurueck.
- Ergebnisse laufen repo-seitig weiterhin kanonisch über `/ergebnisse/`; alte Proof-Slugs bleiben Legacy-Redirects. Für die Hauptnavigation ist `/case-study-solar-leadgenerierung/` der direkte Proof-Link.
- `/ergebnisse/` ist seit 2026-09 als Vertrauensschicht für alle drei kommerziellen Wege neu gebaut und heißt repo-seitig `page-ergebnisse.php` (vorher `page-case-studies-e-commerce.php`, Route unverändert). Sechs Blöcke: Hero mit Belegübersicht, dokumentiertes Großprojekt als Vorher/Nachher-Bilanz samt Begriffsdefinition, drei öffentlich prüfbare Websites, technischer Eigenbeweis dieser Website mit Messkette und externen Prüf-Links, White-Label als erklärte Lücke statt erfundener Referenz, Abschluss mit drei getrennten nächsten Schritten. Der Hero trägt keinen Angebots-CTA mehr; der Marktcheck steht nur noch in der ausdrücklich benannten Energy-Karte des Abschlusses. Entfallen sind der Solar-Hero, der doppelte E3-Block „Laufender Proof" und die Agentur-Brücke als eigener Abschnitt. Sichtbare Kennzahlen kommen aus `inc/canon/e3-proof-canon.php`, die Referenzliste aus dem neuen `inc/canon/reference-canon.php`, das sich die Route mit `/wordpress-freelancer-hannover/` teilt. Die frühere Fehlbenennung „Sales-Conversion" ist raus; Lead-Conversion-Rate und Abschlussquote werden auf der Seite definiert. Stylesheet: `assets/css/ergebnisse.css` (vorher `results.css`, dessen tote `.wl-*`-Regeln entfallen sind).
- Der frühere `Growth Audit`-Shortcode-Flow bleibt repo-seitig als Legacy-Code vorhanden, ist aber nicht mehr der öffentliche Einstieg. Neue kalte Solar-/SHK-Besucher laufen in den Marktcheck auf der Solar-Seite; Agentur-Besucher laufen von der lokalen Money Page primär in den scoped Projektprüfungs-Flow auf `/kontakt/?type=project&focus=followup_scope` und nur bei explizitem Solar-/Wärmepumpen-Kontext in den unteren Marktcheck-Hinweis.
- Direkte Gespraechs-CTAs fuer Audit- und Kontakt-Kontexte nutzen theme-seitig zentral `https://cal.com/hasim-uener/30min?overlayCalendar=true`; öffentliche WGOS-/Asset-Hub-Sales-CTAs sind aus dem Funnel entfernt. Die Whitelabel-Seite nutzt fuer Partner- und Agentur-Fit zusaetzlich `https://cal.com/hasim-uener/whitelabel-fit-gesprach?overlayCalendar=true`.
- Diese Gespraechs-CTAs oeffnen bei verfuegbarem JavaScript ein eingebettetes Cal.com-Popup im Seitenkontext; der direkte Kalender-Link bleibt pro Event-Typ als robuster Fallback erhalten.
- Die Kontaktseite führt öffentlich weiterhin über die kaufnahen Anfragearten `Marktcheck`, `Umsetzung` und `Weiterentwicklung`; `Website-Analyse` und `Projektprüfung` werden zusätzlich als scoped Einstiege für `/kontakt/?type=analysis` und `/kontakt/?type=project` genutzt. Für die Agentur-Money-Page landet `type=project` als B2B-Projektanfrage im bestehenden Contact-REST-Vertrag und nicht als Solar-Marktcheck oder allgemeiner Kontaktpfad. Bestandskunden und allgemeine Rückfragen werden nicht mehr als Hauptauswahl gerendert. Die Frontend-Ausgabe von `/kontakt/` ist repo-seitig am nativen Theme-Template verankert und als kompakter Superflow reduziert. Neben-Routing, Social-Links und Erklär-Rail sind entfernt; Auswahlfragen springen automatisch weiter, `?type=implementation`, `?type=analysis` und `?type=project` starten direkt im passenden Thema und der bestehende REST-Submit-Vertrag bleibt rückwärtskompatibel.
- Der frühere warme Anfrage-Intake `/anfrage/` rendert kein eigenes Formular mehr. Repo-seitig ist `page-anfrage.php` nur noch ein Redirect-Template auf `/solar-waermepumpen-leadgenerierung/#marktcheck`; die alte `energy-intake.js`-Strecke und der dedizierte `/anfrage/`-CSS-Block sind aus dem Deploy-Code entfernt.
- Das Client Portal existiert technisch inklusive Login- und Upload-Logik.
- Repo-seitig existiert jetzt ein gemeinsames `Nexus CRM` fuer Audit-Anfragen, Projektanfragen und Blog-Abos; die Blog-Benachrichtigungen inkl. DOI, Abmeldung und manuell anstossbarem Artikelversand liegen im Theme.
- Repo-seitig existiert jetzt ein `SEO Cockpit` im WordPress-Admin: Search-Console-OAuth, Cache-Layer, Kernmetriken, Dashboard-Widget, Cron-Snapshot, optionaler Koko-Kontext und ein neuer Audit-Lead-Layer aus dem internen CRM sind im Theme vorbereitet.
- Das `SEO Cockpit` priorisiert repo-seitig jetzt nicht mehr nur nach Severity, sondern ueber eine business-aware Queue aus Page Role, Funnel-Naehe, Impressionen, Lead-Signal, Confidence und neuen Insight-Typen wie `Money Page Underperforming`, `Weak Funnel Bridge`, `Orphan Value Page` und `Indexing Mismatch`.
- Die Crawl-Signale für Search- und KI-Crawler werden repo-seitig jetzt über eigene Theme-Routen für `/robots.txt` und `/llms.txt` ausgeliefert: `robots.txt` antwortet als `text/plain`; `llms.txt` bündelt Marktcheck-, Solar-Cluster-, Proof- und Kontaktpfade in einem erweiterten Markdown-Signal und dient Agenten als kompakter Routen-/Positionierungsindex.
- **`robots.txt` ist seit 2026-09-04 nach Crawler-Zweck gruppiert statt nach Anbieter.** `nexus_get_robots_txt_agent_groups()` in `inc/robots-txt.php` trennt AI-Search/Retrieval (`OAI-SearchBot`, `Claude-SearchBot`, `PerplexityBot`), nutzerausgelöstes Abrufen (`ChatGPT-User`, `Claude-User`, `Perplexity-User`) und Trainingscorpora (`GPTBot`, `ClaudeBot`); alles Übrige inklusive Googlebot und Bingbot bleibt in der `*`-Gruppe. Die effektive Policy ist unverändert erlaubend — neu ist, dass sie pro Zweck entscheidbar ist. Vorher fehlten `Claude-SearchBot`, `Claude-User` und `Perplexity-User` (sie fielen auf `*` zurück) und Search-, User- und Training-Bots trugen denselben undifferenzierten Block. Google und Bing bedienen ihre AI-Oberflächen (AI Overviews, AI Mode, Copilot) aus dem normalen Crawl; es gibt dort keinen separaten AI-Crawler und es wird auch keiner erfunden. Die redundanten `Allow: /llms.txt` und `Allow: /wp-sitemap.xml` sind entfallen, weil `Allow: /` sie bereits abdeckt. **Regel bei Änderungen:** Eine benannte Gruppe lässt den Bot die `*`-Gruppe vollständig ignorieren — jede Gruppe muss deshalb den kompletten Regelsatz tragen.
- Die Datei `llms.txt` im Repo-Root ist ein Abzug der dynamischen Route, kein zweiter Pflegeort. `scripts/lint-entity-crawler-signals.php` rendert `nexus_get_llms_txt_content()` offline und vergleicht; `--write-llms` schreibt den Abzug neu. Der Abzug war zuvor gedriftet und veröffentlichte den veralteten Intake-Fokus `followup_scope`, während die Live-Route `implementation_scope` ausgibt.
- **Lokale Entitätsverankerung für KI-Antwortsysteme (2026-09-05).** Der `llms.txt`-Header nennt jetzt den Sitz (Pattensen bei Hannover, Region Hannover, Niedersachsen) plus Einzugsgebiet und verlinkt das Impressum als NAP-Quelle — vorher trug der Kopf der Datei, den ein Agent vor jeder Route liest, keinen Ortsbezug. Die Routing-Aussage (Freelancer / White-Label / Marktcheck) steht unverändert daneben. Am `Organization`/`LocalBusiness`-Knoten liefert `hu_get_business_area_served()` in `inc/org-schema.php` das `areaServed` jetzt zweischichtig: eine `GeoCircle` mit 100 km Radius um den Sitz für die Vor-Ort-Aussage (Midpoint aus `hu_get_business_geo_coordinates()`, derselben Quelle wie `geo`) und die benannten Orte mit **Wikidata-URI und** de-Wikipedia-Artikel im `sameAs`-Array. Die Q-IDs wurden über `pageprops`/`wikibase_item` aus genau den bereits gesetzten Wikipedia-Titeln aufgelöst; ungeprüft übernommene IDs aus einem Vorschlag zeigten auf Gießen (`Q3874`) und einen schwedischen Literaturpreis (`Q511874`). `DACH` bleibt ohne `sameAs`, weil es dafür kein eindeutiges Wikidata-Item gibt. `scripts/lint-entity-crawler-signals.php` prüft Shape und Konsistenz (ein GeoCircle, Midpoint = `geo`, je Ort genau eine Wikidata- und eine Wikipedia-URI, keine doppelt vergebene Q-ID, Ortsanker im `llms.txt`-Intro); die Bedeutung einer Q-ID kann nur der Wikidata-Abgleich selbst belegen. Das `areaServed` der Service-Knoten bleibt davon unberührt auf Länderebene. Schema und sichtbare Copy nennen jetzt dieselben Orte: Wolfsburg stand nur im Graph, Celle nur im Standortblock von `/wordpress-agentur-hannover/` — Celle ist als `City` (`Q3933`) ergänzt, Wolfsburg in der Tagesreichweiten-Aussage der Seite und in der `llms.txt`-Standortzeile. Der Lint prüft das jetzt wie bei `Person.sameAs` und dem Personen-Hub: jede `City` aus `areaServed` muss auf der lokalen Money Page namentlich vorkommen.
- **Aufnahmekriterium für `llms.txt`:** Routen mit eigener Rechen- oder Entscheidungsgrundlage plus die Hubs. Von den fünf Provider-Markteinordnungen aus `inc/blog-provider-posts.php` stehen deshalb nur `/aroundhome-solar-einordnung/` und `/checkfox-solar-waermepumpe-einordnung/` dort — beide tragen den CPO-Rechner auf Basis eigener Betriebswerte und gelten auch in `scripts/smoke-live.sh` als Money-Routen. Checkfox fehlte bis 2026-09-04, obwohl es in beiden Punkten mit Aroundhome gleichauf liegt. Wattfox, DAA und Leadfluss sind Markteinordnungen ohne eigene Rechengrundlage und bleiben über `/blog/` und die Sitemap auffindbar; `llms.txt` ist ein Routen-Index, kein Abzug des gesamten Bestands.
- Der Solar-Subpage-Cluster umfasst repo-seitig jetzt auch `/solar-leads-kosten-studie/` in der zentralen Cluster-/Cockpit-Erkennung und in der sichtbaren Kontextlink-Registry. Public/Legacy-`noindex`-Seiten werden zentral als `noindex, follow` geführt, geschützte/private Routen bleiben `noindex, nofollow`; SEO-Cockpit und Frontend-Meta nutzen denselben Resolver.
- Technische SEO-Hygiene ist repo-seitig weiter konsolidiert: `/sitemap_index.xml` leitet per 301 auf `/wp-sitemap.xml`, nicht kuratierte Tag-/Custom-Taxonomy-/Author-/Date-Archive sind `noindex, follow`, die Taxonomy-Sitemap bleibt auf Kategorien beschränkt und das Theme bleibt ohne SEO-Plugin die zentrale Quelle für Meta, Canonical, Robots, Social Meta und Schema. Für Robots gilt das seit 2026-08-06 auch tatsächlich: `hu_silence_core_robots_tag()` schaltet den Core-Tag über `wp_robots` ab, weil sonst zwei `<meta name="robots">` im Head standen (Core mit `max-image-preview:large`, Theme mit der eigentlichen Direktive). Die Preview-Direktive läuft jetzt im Theme-Tag mit, ausgenommen auf `noindex`-Routen. Document-Title-Overrides laufen über eine zentrale Resolver-Funktion; OG-Bildmaße kommen aus Attachment-Metadaten statt aus festen Defaults; doppelte globale Breadcrumb-Schema-Ausgaben werden auf Seiten mit route-spezifischen Breadcrumbs vermieden.
- FAQPage-Schema aus editorgetriebenen Posts/Pages wird repo-seitig nicht mehr per Regex auf jedem Frontend-Request extrahiert. `save_post` berechnet FAQ-Entities einmalig aus `<details>` bzw. `.faq-trigger`/`.faq-content`-Mustern und speichert sie als JSON in Post-Meta; das Frontend liest nur noch den Cache. Template-eigene FAQ-Schema-Ausgaben bleiben ausgenommen.
- Nicht-kritische Theme-Skripte werden zentral ueber `inc/enqueue.php` mit `defer` ausgeliefert; `nexus-core-js` und `nexus-site-header-js` bleiben fuer unmittelbare Boot- und Header-Interaktionen ausgenommen.
- Zusaetzliche INP-Haertung: `nexus-core.js` verschiebt nicht-kritische Observer/TOC/Progress-Initialisierung auf `requestAnimationFrame` bzw. Idle-Zeit, der Header verarbeitet Scroll- und Pointer-Bewegungen nur noch RAF-gebuendelt und das Cal.com-Enhancement bindet Preload-Listener direkt an passende Buchungslinks statt global am gesamten Dokument.
- Zusaetzliche Homepage-Performance: die Font-Faces liegen jetzt direkt in `style.css` statt in einer separaten `fonts.css`-Anfrage, `related-content.css` und `footer-cta.css` laden nur noch auf Blog-Singles und die Homepage zieht die Core-Block-Library nicht mehr in den kritischen CSS-Pfad.
- Zusaetzliche Homepage-Font-Optimierung: auf der Startseite wird fuer Figtree jetzt gezielt `figtree-600.woff2` als kritischer Schnitt vorgeladen; gleichzeitig sind Floating-Nav, Hero-Badge, Hero-CTA, Hero-Textlink und der fruehe Proof-Strip typografisch auf leichtere Figtree-Gewichte reduziert, damit `figtree-700.woff2` nicht mehr fuer den ersten Viewport notwendig wird.
- Zusaetzliche Homepage-JS-Optimierung: die Startseite zwingt jetzt auch Parent-Theme- und Core-Handles (`ct-scripts`, `nexus-core-js`, `nexus-site-header-js`) in den `defer`-Pfad; `nexus-core.js` initialisiert Smart-Nav-Scroll-Spy spaeter und bevorzugt `IntersectionObserver`.
- Das globale Theme ist repo-seitig jetzt fest auf das dunkle Farbschema gesetzt; der fruehere Desktop-Hell/Dunkel-Toggle rendert nicht mehr und der Frontend-Boot setzt `data-theme` bzw. `data-nx-theme` konsistent auf `dark`.
- Repo-seitige Branding-Ausgabe fuer Logo- und Favicon-Signale priorisiert jetzt das im WordPress-Admin gesetzte `custom_logo` bzw. `site_icon`; die Theme-SVGs unter `assets/brand/` bleiben nur noch Fallback, damit Frontend-Head-Tags und Organization-Schema dieselbe Quelle nutzen.
- **Person und Organization sind seit 2026-09-04 in `sameAs` sauber getrennt.** Vorher lieferte `hu_business_same_as_urls()` exakt die Personenprofile plus Maps-URL: Person und Organization trugen dieselbe `sameAs`-Liste unter zwei verschiedenen `@id`s. `sameAs` ist eine Identitätsaussage, und zwei Knoten mit identischer Liste behaupten, dieselbe Entität zu sein — was `worksFor`/`founder` zwischen ihnen bedeutungslos macht und die publizistischen Profile (Medienwissenschaft, Diaspora-Politik) an das WordPress-/Tracking-Geschäft hängt. Jetzt gilt: `Person.sameAs` = eigene Domain, LinkedIn, **`github.com/Hasim-hannover` (persönlicher Account)**, `hasimuener.org`, Facebook, ORCID, YouTube, X. `Organization.sameAs` = **`github.com/Hasim-Uner` (GitHub-Organisation, hält die Produktions-Repos)** und das Google Business Profile. Bewusst wenige, korrekte Einträge statt einer gespiegelten Liste; ORCID identifiziert eine Person und gehört nie an einen Organization-Knoten.
- **GitHub-Accounts nicht wieder zusammenlegen.** `Hasim-hannover` (User) und `Hasim-Uner` (Organisation) existieren beide. Ein früherer Eintrag im CHANGELOG bezeichnete `Hasim-Uner` als "den tatsächlichen Account" und ersetzte alle `Hasim-hannover`-Verweise — das war eine falsche Annahme. Gegenprobe 2026-09-04: Das GitHub-Profil `Hasim-hannover` nennt Haşim Üner und verlinkt hasimuener.de; der ORCID-Datensatz `0009-0008-7500-2015` (Vorname "Haşim", Nachname "Üner", Pattensen bei Hannover) führt `github.com/Hasim-hannover` selbst als Researcher-URL.
- Der Hard Ban `Architekt für eigene Anfragesysteme` stand bis 2026-09-04 noch als Quellwert in `org-schema.php` (Person `jobTitle`/`description`, Organization- und WebSite-`name`/`description`) und wurde erst beim Rendern durch `schema-positioning.php` überschrieben. Quelle und Ausgabe sind jetzt deckungsgleich; `hu_brand_map_url()` liefert die CID-URL des Google Business Profile statt der Place-URL, die den zurückgezogenen Rollen-Claim im Pfad trug.
- Schema-/Entity-Graph: Personenreferenzen laufen zentral auf `/hasim-uener/#person` (aufgeloest ueber `hu_person_profile_url()`, nicht mehr hart verdrahtet); `Person.address` nennt Pattensen/DE. Die Person-Entity trägt jetzt zusätzlich JobTitle, Autorenbeschreibung, `alumniOf` und fachliche `knowsAbout`-Signale. `Organization.hasMap`, Unternehmensname, Koordinaten und Oeffnungszeiten sind an das Google Business Profile angeglichen. `/wordpress-agentur-hannover/` gibt neben Service-, WebPage- und FAQPage-Schema jetzt zusaetzlich Article-Schema mit Haşim Üner als Autor aus.
- Der frühere EnergieFahrplan-Showroom ist repo-seitig aus dem Funnel entfernt. Beweisführung läuft über E3, Methodik und die direkte Achse Landingpage -> Branchen-Seite -> Marktcheck.
- Die alte WGOS-Video-/Preview-Route `wgos-preview` und ihre statischen HTML-/Template-Artefakte sind aus dem deploybaren Code entfernt. Nicht runtime-referenzierte statische HTML-Referenzen wie das alte WGOS-Proof-Snippet und das Brand-Sheet sind ebenfalls entfernt. Das interne `page-wgos.php` bleibt davon unberührt und bleibt ein geschütztes Client-Dashboard.

## In Arbeit

- `/system-diagnose/` ist repo-seitig kein eigener öffentlicher Funnel mehr. Die Route bleibt als Legacy-Einstieg erhalten und leitet per 301 auf den Marktcheck der Branchen-Landingpage. Der frühere React-Funnel unter `blocksy-child/readiness/` bleibt als Legacy-Code im Repo, ist aber nicht mehr der aktive Solar-Lead-Pfad. Der aktive Marktcheck nutzt `/wp-json/nexus/v1/audit-request`, speichert im Audit-CRM und nutzt die bestehende Mail-/Brevo-Schicht.
- Das Repo wird gerade von einem Theme-Repo zu einem Website Operating System erweitert; WGOS bleibt dabei internes Delivery-/Client-System und ist nicht mehr öffentlicher Verkaufsrahmen.
- `page-wgos.php` bleibt template-driven als internes Dashboard; eine spaetere Auslagerung dynamischer Projektwerte muss serverseitig nach `client_user_id = current_user_id` filtern.
- Systembaustein-Detailseiten laufen weiter ueber den hierarchischen CPT `wgos_asset` mit SCF-Meta und Single-Template; die redaktionelle Befuellung der Spokes passiert weiter im WordPress-Admin.
- Systembausteine sind jetzt zusaetzlich als versionierte Registry im Theme definiert: 39 Assets stehen repo-seitig auf `publish`, inklusive 4 KI-/Automatisierungs-Assets im Kernbereich `Weiterentwicklung`.
- Die neue Systembaustein-Struktur rendert versionierte SEO-Meta, Related-Links und `Service`-Schema aus derselben Registry.
- Die frühere Systemlandkarte wird bei Bedarf weiter als Baustein-Übersicht angelegt, ist aber kein öffentlicher CTA- oder Sales-Pfad mehr.
- Die eigentliche Post-Erstellung und Aktualisierung fuer Systembausteine haengt aktuell noch am neuen Theme-Sync und damit am naechsten Deployment auf die Live-Umgebung.
- Teile der Homepage- und Navigationslogik haengen noch an manuellen WordPress-Admin-Schritten oder editorgetriebenen Default-Seiten ausserhalb der route-forced Clusterpages.
- Editorgetriebene Seitentitel, Excerpts, Karten und `the_content()`-Bereiche koennen weiter alte Proof-, Tonalitaets- oder Du/Sie-Brueche enthalten und muessen im WordPress-Admin separat verifiziert werden.
- Blog-Artikel koennen jetzt theme-seitig passende Systembausteine als Anschlussblock ausgeben; weitere Post-Mappings bleiben ausbaubar.
- Der frühere schnelle Intake und der Instant-Results-Flow für `/growth-audit/` liegen repo-seitig weiter als Legacy-Code vor, sind aber nicht mehr der aktive Renderpfad dieser Route.
- CRM-, Mail- und Follow-up-Logik fuer den Audit sind jetzt im Theme sichtbar; `wp_mail`-basierte Transaktionsmails koennen repo-seitig zentral ueber die Brevo API geroutet werden, sobald die Live-Credentials ausserhalb des Repos gesetzt sind.
- Neue Audit-Anfragen speichern repo-seitig zusätzlich Formular-Landingpage, ersten internen Einstieg, vorherige interne Seite und Referrer für Lead-Attribution im SEO-Cockpit. Der gemeinsame Frontend-Helper übernimmt innerhalb der Browser-Session außerdem Kampagnenquelle und Suchbegriff aus `utm_source`/`utm_term`; vorhandene `gclid`-/`fbclid`-Parameter werden nur als Google-/Meta-Quellensignal erkannt, nicht als rohe Klick-ID gespeichert. Der Solar-B2B-Marktcheck sendet absolute interne URLs; für bestehende Energie-Landing-Leads ohne historische URL-Meta ordnet das SEO-Cockpit defensiv per `energy_systems_landing`-Source-Fallback auf `/solar-waermepumpen-leadgenerierung/` zu, statt sie als komplett unmapped zu behandeln. Cockpit und CRM verwenden für die Qualifizierung denselben Vertrag `qualified|nurture` und das Cockpit aggregiert die qualifizierte Anzahl explizit.
- Der aktive Marktcheck-REST-Endpoint `/wp-json/nexus/v1/audit-request` ist repo-seitig auf Contract `2026-05-26.audit-request.v1` gehärtet: Clients können `contract_version` mitsenden, Antworten tragen `contractVersion`, `traceId`, `X-Nexus-Contract-Version` und `X-Nexus-Trace-Id`, und Fehler liefern maschinenlesbare `error_code`-/`error_details.field`-Signale.
- Die Energie-/Branchen-Landingpage bleibt unter `/solar-waermepumpen-leadgenerierung/` als SEO-Zweig, Kontextseite und primärer Marktcheck-Einstieg bestehen; der alte Pfad `/website-fuer-solar-und-waermepumpen-anbieter/` wird weiter per Redirect auf den kanonischen Slug gefuehrt. Die großen CTA-Flächen der Seite führen zum eigenen `#marktcheck` im Hero; kein separater `/system-diagnose/`-Funnel ist dort Default.
- Das neue Blog-Notify-System ist repo-seitig implementiert. Der initiale Subscribe-Request fuer `Neue Artikel per E-Mail` ist live verifiziert: der Endpoint speichert zunaechst einen Pending-DOI-Intent und versendet die Bestaetigungsmail; der CRM-Kontakt entsteht bzw. wird aktualisiert erst nach Klick auf den DOI-Link.
- Das SEO-Cockpit ist repo-seitig implementiert; Google-OAuth/API-Livezustand bleibt admin-/credential-owned und wird nicht im Repo gespeichert. Koko bleibt ein optionaler lokaler Traffic-Layer und muss getrennt vom Search-Console-API-Zugriff bewertet werden.
- Das SEO-Cockpit ist repo-seitig jetzt umbaufest interpretiert: historische Search-Console-Zeilen für konsolidierte Legacy-Slugs werden für Insight-Scoring auf das kanonische Ziel gemappt, WooCommerce-/Shop-Queries werden nicht mehr als kaufnahe Zielaufgaben priorisiert, und die Top-Pages-Tabelle markiert Redirect-Reste als `Legacy` statt sie wie unklare WordPress-Objekte aussehen zu lassen.
- Der SEO-Cockpit-CSV-Export führt aktuelle und vorherige Query×URL-Zeilen jetzt als Union, kennzeichnet `previous_only`-Verluste und ergänzt getrennte Page-Totals. Gap-Auswertungen ignorieren nur vorherige Zeilen, Drift-Auswertungen melden sie als `VERSCHWUNDEN`; fehlende Positionen werden nicht als Rang `0` ausgegeben.
- Das SEO-Cockpit zählt interne Kontextlinks jetzt nicht mehr nur aus `post_content`, sondern zusätzlich aus den relevanten Laufzeit-Templates für Cluster-Seiten, Blog-Bridges, die Agentur-Seite und den Ergebnisse-Hub; zugleich sind forcierte SEO-Snippets für `/ergebnisse/` und ein keyword-first Title für `/wordpress-agentur-hannover/` im Theme hinterlegt. Nicht-Zielqueries wie reine Shop-/E-Commerce-Intents werden bei Prioritäts-Queue, Kannibalisierung, Quick-Wins und Query-Movern aus Handlungsempfehlungen herausgefiltert.
- Der SEO-Cockpit-Linkgraph zählt jetzt auch die hardcodierten Themen-Hub-Links aus Homepage, Solar-Money-Page, E3-Case und Agentur-Seite in den Solar-/SHK-Subpage-Cluster. Dadurch fallen `/kunden-gewinnen-solarteure/` und `/server-side-tracking-b2b/` nicht mehr fälschlich als kontextlinklose Pages auf, sobald der neue Linkgraph nach Deploy/Sync neu aufgebaut ist.
- Der Solar-Hauptfunnel fuehrt repo-seitig nicht mehr ueber `/kontakt/`, `/wordpress-agentur-hannover/` oder `/whitelabel-retainer/`; diese Nebenpfade bleiben in isolierten Kontexten bzw. im Footer erreichbar, nicht in der primaeren CTA-Logik. `/whitelabel-retainer/` hat als Agentur-Einstieg zusaetzlich einen eigenen Header-Navigationspunkt „Für Agenturen“, ist aber kein Schritt im Solar-Funnel.
- `/whitelabel-retainer/` ist seit 2026-09-10 auf zehn Sektionen umgebaut. Reihenfolge: Hero, Lieferfelder, Einstieg/Preise/Marge, Ablauf, Kontrakt, Belege, Wer liefert, Einordnung, FAQ, Abschluss. Die Aussage im Hero ist die Kombination aus Umsetzung und Messkette in einer Person; ein Kicker trägt „White-Label“ und „Agenturen“ für Suche und Entity-Erkennung. Ersatzlos entfallen sind die Problem-Sektion, die Test-Sprint-Detailsektion (Inhalt in Karte und FAQ 2), die eigene Arbeitsmodus-Sektion (Inhalt in „Wie es läuft“) und die Sektion „Technischer Unterbau“ (die beiden Belegseiten-Links stehen jetzt in „Belege“). Die Ablauf-Kette ist aus dem Hero in „Wie es läuft“ gewandert, der Nav-Punkt `#ablauf` zeigt dorthin. FAQ-Inhalt, White-Label-Kontrakt (nur die Überschrift ist ausgetauscht), die drei Live-Referenzen, „Eine Person. Kein Ticketsystem.“, der Code-Beleg-Block, Porträt und Background-Text sind unverändert.
- Die primäre Conversion-Aktion der Route ist ein vierfeldriges Formular (Aufgabe, gewünschter Zeitraum, Zugänge zu WordPress/GA4/GTM, E-Mail) mit eigenem REST-Endpunkt `nexus/v1/whitelabel-request` (`inc/whitelabel-request.php`), Honeypot, geteiltem Rate-Limit und Zustellung an die Canon-Adresse aus `hu_get_contact_email()`. Der frühere Fit-Check (Quiz, Ergebnis-Block, `#fit-check`) und der `mailto`-CTA sind aus Markup, JS und CSS entfernt. Das Erfolgs-Event `whitelabel_request_submit` feuert kein anderes Formular der Website; damit ist die Conversion dieser Route isoliert messbar. Der Abschluss bietet drei gleichrangige Wege: konkrete Aufgabe (`?case=aufgabe`), Angebotsphase (`?case=angebotsphase`) und der Cal.com-Termin. Der `case`-Wert wird clientseitig aus der URL gesetzt, nicht serverseitig gerendert — die Route ist gecacht.
- Preise der Route: `HU_WHITELABEL_SERVER_SIDE_MIN` liegt bei 900 € statt 1.290 €. Der Endkundenpreis für dieselbe Leistung steht öffentlich auf `/server-side-tracking-b2b/` und blieb bei `HU_TRACKING_STANDARD_SETUP`; bei identischem Betrag hatte die Agentur keinen Margenspielraum. Der Retainer trägt mit `HU_WHITELABEL_RETAINER_HOURS` ein Stundenkontingent statt einer nackten Untergrenze und steht als eigene Karte („Laufende Kapazität“) unter dem Margenblock. Kein Stundensatz auf der Seite. Angebotskarten, FAQ 1, Eckdaten-Block und `hasOfferCatalog` lesen dieselben Konstanten.
- Der Kennzahlen-Kasten (CPL-Kontrast, Anfragezahl, Abschlussquote) und jeder Link auf die Case Study sind von `/whitelabel-retainer/` entfernt. Ohne Zahlen entfällt auch der Übertragbarkeits-Disclaimer. An ihrer Stelle steht ein Absatz zur Herkunft der Messtechnik ohne Kennzahlen. Die Kennzahlen auf `/case-study-solar-leadgenerierung/` und den Solar-Routen bleiben unverändert.
- Seit 2026-09-21 steht auf `/whitelabel-retainer/` auch die namentliche Live-Referenz auf den Betrieb des Solar-Falls nicht mehr: `$proof_references` trägt nur noch `civaka-azad.org` und `hasimuener.org`. Der Fall heißt domainweit „ein mittelständischer PV-Installationsbetrieb“ (`HU_E3_CASE_LABEL`) — ohne Link, ohne Namen und ohne Begründung für die Anonymisierung. `inc/article-content-hygiene.php` schreibt den Namen ebenfalls nicht mehr in den Alt-Artikel `server-side-tracking-gtm`, sondern ersetzt ihn dort durch dasselbe Label.
- Schema der Route: neben dem bestehenden `AboutPage`- und `FAQPage`-Knoten (neun Fragen aus `nexus_get_whitelabel_faq_items()`) emittiert `inc/org-schema.php` jetzt einen `Service`-Knoten `#service` mit `areaServed` DE/AT/CH, `provider` als Referenz auf die bestehende Person-Entity und `hasOfferCatalog` über die vier Erstprojekte. Der Test-Sprint trägt `price`, die drei „ab“-Stufen `minPrice` — eine Untergrenze ist kein Preis. Die Beträge kommen aus `hu_whitelabel_pricing_canon()`. Der sitewide Organization-Katalog in `inc/schema-positioning.php` bleibt davon unberührt.
- `/whitelabel-retainer/` ist kein geschlossener Kreis: Die Wortmarke im seitenlokalen Header ist ein Link auf `/` (`nav_whitelabel_home`), der Block „Wer liefert“ verlinkt als Textlink auf `/hasim-uener/`, und „Belege“ führt auf `/stack-agentur/` und `/wordpress-projekte-auslagern/`. Der reduzierte Footer trägt nur noch Startseite, Impressum und Datenschutz; Freelancer- und Solar-Link sind entfallen, weil sie für einen Agenturbesucher den Wettbewerbsgedanken erzeugen, den der Margenblock gerade auflöst.
- Flankierend: `/stack-agentur/` trägt „White-Label“ nicht mehr in Title und Meta-Description (Forced-SEO-Map in `inc/seo-meta.php`), damit es nicht mit `/whitelabel-retainer/` um dasselbe Signal konkurriert; der bestehende Kontextlink auf die Agentur-Route bleibt. Auf `/wordpress-projekte-auslagern/` führen die drei generischen Marktcheck-Ausgänge in `single.php` (Nächster-Schritt-Liste, Inline-CTA, Autorenbox) auf `/whitelabel-retainer/` statt in die Energie-Vertikale; alle anderen Beiträge behalten den Marktcheck unverändert.
- Die Preisdarstellung beider indexierter Tracking-Seiten ist seit 2026-08-14 aufeinander abgestimmt. Die Beträge stehen ausschließlich in `inc/canon/pricing-canon.php` und werden hier bewusst nicht wiederholt — diese Datei ist keine zweite Preisquelle. Endkundenleiter auf `/server-side-tracking-b2b/`: `HU_TRACKING_STANDARD_SETUP`, `HU_TRACKING_PRO_SETUP` und `HU_TRACKING_CUSTOM_SETUP_MIN` sind angehoben; die Monatsbeiträge der Tracking Care, Leistungsumfänge, Paketnamen und Seitenstruktur bleiben unverändert. White-Label-Leiter auf `/whitelabel-retainer/`: `HU_WHITELABEL_TEST_SPRINT_PRICE` bleibt der einzige feste Betrag und wird auf der Karte als Festpreis ausgewiesen; `HU_WHITELABEL_TRACKING_AUDIT_MIN`, `HU_WHITELABEL_SERVER_SIDE_MIN` und `HU_WHITELABEL_LANDINGPAGE_MIN` sind neue Untergrenzen und liegen rund 30 % unter dem jeweils entsprechenden Endkundenpreis. Der Audit ist die günstigste Stufe, weil er als Türöffner das Setup verkauft. Die Reihenfolge ist bindend: sinken die Endkundenpreise, muss der White-Label-Block mitgeprüft werden, sonst zahlen Agenturen mehr als Endkunden. Kein Stundensatz auf der Seite. Die Angebots-FAQ liest dieselben Konstanten und geht so auch ins FAQPage-Schema.
- Kannibalisierungs-Hinweise im SEO-Cockpit zeigen jetzt direkt die staerksten konkurrierenden URLs mit Impressionen und Position an, statt nur auf den normalen URL-Drilldown der Primaer-URL zu verweisen.
- Das Client Portal zeigt keine erfundenen KPI-/Roadmap-Daten mehr. Ressourcen, Kennzahlen und Roadmap werden nur ausgegeben, wenn sie pro Nutzer im Meta-Payload `nexus_client_portal` hinterlegt sind; sonst erscheinen explizite Empty-States. Admins können diesen Payload im Benutzerprofil pflegen. Login und Upload-Logik bleiben aktiv.
- KI-Erweiterung: 4 Systembausteine (KI-Assistent/Chatbot 30 Cr, KI-Lead-Qualifizierung 20 Cr, RAG-Wissenssuche 25 Cr, LLM-Workflow-Automatisierung 20 Cr) stehen repo-seitig jetzt auf `publish` und sind dem Weiterentwicklungs-Layer zugeordnet. Dachseite `/ki-integration-wordpress/` als versioniertes Page-Template (`page-ki-integration.php`) mit Service- und FAQPage-Schema bleibt im Repo; die Live-Synchronisation der Asset-Posts ueber den Theme-Sync ist noch nicht verifiziert.

## Geplant

- n8n nur bei konkretem Workflow-Bedarf weiterfuehren; jedes aktive Artefakt braucht JSON, Doku und Flow-Map
- Agenten-Layer weiter ueber kurze Root-Regeln, lokale Kontexte und Skills fuehren; keine neue Root-Planablage
- weitere Systemdoku fuer Tracking, CRM-Routing, Offer-Logik und CTA-Inventar
- Ausbau des SEO Cockpits um echte Koko-Daten, Page-Level-Korrelationen und spaetere URL-Inspection-Sichten
- Ausbau des Decision Logs fuer strukturelle Repo- und Systementscheidungen
- Nach Deploys: Theme-Deploy prüfen, SEO-Cockpit manuell synchronisieren, Koko-Status im gewählten Zeitraum lesen, Sitemap prüfen und alte WordPress-Seiten/Menüeinträge über den Admin-Legacy-Cleanup bereinigen.

## Deprecated

- Legacy-Slugs `/system-diagnose/`, `/anfrage-system-analyse/`, `/readiness-diagnose/`, `/anfrage/`, `/growth-audit/`, `/audit/`, `/customer-journey-audit/`, `/360-audit/` und `/wordpress-tech-audit/` werden auf `/solar-waermepumpen-leadgenerierung/#marktcheck` umgeleitet.
- Die früheren Tool-, ROI- und Service-Slugs `/wordpress-seo-hannover/`, `/core-web-vitals/`, `/conversion-rate-optimization/`, `/wordpress-wartung-hannover/`, `/kostenlose-tools/`, `/tools/`, `/website-performance-analyse/`, `/roi-rechner/`, `/audit-linkedin/` und `/seo/` sind keine erzwungenen 301-Pfade mehr und liefern `410 Gone`. Alte WGOS-Pfade wie `/wordpress-growth-operating-system/` und `/wgos-systemlandkarte/` bleiben noindex/access-protected bzw. noindex, solange sie intern noch als Dashboard-/Asset-Hub-Pfade existieren.
- Legacy-Slugs `/case-studies/` und `/case-studies-e-commerce/` werden auf `/ergebnisse/` umgeleitet.
- Der Legacy-Slug `/wordpress-agentur/` bleibt als Schutz-Redirect auf `/wordpress-agentur-hannover/` erhalten.
- Der Legacy-Pfad `/alle-loesungen-im-detail/` wird auf die aktuelle Loesungs-Uebersicht umgeleitet.
- Sitemap-/Index-Hygiene: interne Lösungsseiten (`/alle-loesungen/`, `/loesungen/`), Showroom-/Legacy-Demos (`/energie-fahrplan-demo/`), Tool-/Audit-Legacy-Routen, alte Proof-/Kontakt-/Agentur-/Solar-Pfade und retired Service-Slugs werden repo-seitig aus der nativen Sitemap ausgeschlossen und je nach Pfad noindex markiert oder als `410 Gone` ausgeliefert. Relevante Sitemap-Ziele bleiben kanonische Funnel-, Proof-, Money-, Blog-, Glossar-, Kontakt- und veröffentlichte Asset-/Content-Detailseiten.
- Die Clusterseiten `/ga4-tracking-setup/` und `/performance-marketing/` bleiben aktive versionierte Routen. `/wordpress-wartung-hannover/` ist als alte Seite wieder konsolidiert: eigenes Template und Cluster-Registry-Eintrag sind entfernt, der Slug liefert `410 Gone`; interne Primärlinks zeigen nicht mehr auf den alten Slug.
- Im WordPress-Admin erscheint fuer Admins ein Legacy-Cleanup-Hinweis; der Ein-Klick-Flow setzt gefundene Altseiten auf `draft` und entfernt passende Menue-Eintraege.
- Der `Growth Audit`, die System-Diagnose-Seite und die alten kostenlosen Tools sind nicht mehr öffentliche Funnel-Stufen. Nur geschützte Legacy-Einstiege mit wahrscheinlicher externer Nutzung leiten noch auf den Marktcheck; interne Tool-/ROI-/Service-Altlasten werden als `410 Gone` beendet statt als Redirect-Netz betrieben. Der kaufnahe Einstieg für kalten Solar-/SHK-Traffic läuft repo-seitig über den Marktcheck auf der Solar-Seite; die Agentur-Money-Page nutzt für allgemeine B2B-Besucher den scoped Projektprüfungs-Flow auf `/kontakt/?type=project&focus=followup_scope` und nur für Solar-/Wärmepumpen-Kontext den unteren Marktcheck-Hinweis. Vertiefte Folgeanalysen oder technische Integrationen ergeben sich erst danach.
- Oeffentliche Kontakt- und CTA-Texte mit `Pilotprojekt`, `Proof-of-Value`, `3.000+ Leads`, `34x ROAS` ohne Kontext oder `Retainer` als kaufnaher Standardbegriff sind nicht mehr Zielbild.
- Ein WordPress-Editor-Shell als Source of Truth fuer den Audit ist nicht mehr Zielbild.
- Lose Root-Ablage fuer Playbooks, Referenz-Snippets und Content-Drafts ist nicht mehr Zielstruktur.
