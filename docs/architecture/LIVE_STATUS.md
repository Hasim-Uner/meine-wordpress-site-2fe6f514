# Live Status

Statusbasis: Repo-Inhalt zum jeweiligen Commit; kein händisch gepflegtes Tagesdatum.

Basis dieses Status:

- Repo-Inhalt
- bestehende Deploy-Workflows
- vorhandene Template- und Funnel-Logik

Nicht verifiziert:

- exakte Live-Konfiguration in WordPress-Admin
- exakte n8n-, GTM-, GA4-, Consent- und CRM-Setups

## Live

- `blocksy-child/` ist der deploybare Website-Code.
- **Die Startseite `/` ist repo-seitig Verteiler, keine Solar-Landingpage mehr.** Dieser Punkt hat Vorrang vor allen älteren Homepage-Beschreibungen weiter unten in diesem Abschnitt. `front-page.php` trägt sechs Blöcke: Hero ohne CTA (der Hero-Cue zeigt auf `#wege`), drei Türen (`#wege` → Solar-Marktcheck, `/wordpress-freelancer-hannover/`, `/whitelabel-retainer/`), lebender Beweis (`#lebender-beweis`, Lighthouse-Werte dieser Seite plus ruhigem PageSpeed-Insights-Link und Fachlink zur Server-Side-Tracking-Route), E3-Beleg genau einmal (`#beleg`), Selbstqualifizierung (`#abgrenzung`) mit Projektanfrage-Ausgang und Abschluss (`#cta`) auf der kanonischen `project_request`-Route. Der frühere Klick-Einstieg (`#einstieg`, eine Frage, fünf Antworten, `home_entry_*`-Events) ist seit 2026-08-20 entfallen: Er stellte dieselbe Routing-Entscheidung wie die drei Türen ein zweites Mal und fragte dabei nach dem Kanal („Woher kommen Ihre Anfragen heute?"), während er nach dem Segment routete. `home_door_*` bleibt und ist damit die einzige Wegewahl-Messung der Seite. Entfallen sind Gateway-Band, Verlustpunkte, Prozess-Kaskade, System-Phasen, Portal-Chaos-Vergleich, Kategorie-Block, Themen-Hub, About-Sektion und die siebenteilige FAQ; die Inhalte leben auf Solar- und Agentur-Seite weiter. Damit emittiert die Startseite kein `FAQPage` mehr — `is_front_page()` bleibt in `$template_owns_faq_schema` (`inc/org-schema.php`), damit aus altem Editor-Inhalt kein Schema für unsichtbare Fragen entsteht. Der Marktcheck bleibt vollständig auf `/solar-waermepumpen-leadgenerierung/#marktcheck`; die Startseite besitzt weiterhin kein eigenes Formular. `homepage-redesign.css` wird auch von der Solar-Money-Page geladen und daher nur additiv erweitert.
- **Der globale Footer ist seit dem Selbstauskunft-Umbau eine einzige Variante für alle Seitentypen.** Dieser Punkt hat Vorrang vor älteren Footer-Beschreibungen weiter unten. Die früheren Varianten `ft--energy-minimal`, `ft--audit-minimal` und `ft--home` samt Routenblock (`ft__routes`) und CTA-Band sind zusammengelegt; `template-parts/site-footer.php` rendert nur noch `.ft`. Reihenfolge: Selbstauskunft (drei Ich-Sätze, je ein kommerzieller Weg), Direktzeile (Mail und Telefon), Messprotokoll (Antwortzeit aus `hu_response_promise()`, Sitz, Arbeitsweise, Messung), Verzeichnis (Leistungen · Belege · Person & Kontakt) und Schlusszeile. Der Sammel-CTA „Projekt anfragen" entfällt in beiden Rollen — als CTA-Band und als Verzeichnis-Link —, weil die Selbstauskunft den Weg oben schon benennt. `hu_get_contact_phone()` (`inc/canon/messaging-canon.php`) ist die neue Canon-Quelle der Telefonnummer; Impressum und Datenschutz tragen sie weiterhin je einmal hart im Template. Das Stylesheet `assets/css/site-footer.css` löst `site-footer-modern.css` samt der inline Kontrast-Ebene in `footer.php` ab und arbeitet mit komponentenlokalen `--ft-*`-Tokens ohne `--nx-*`-Abhängigkeit. `.ft__sr` ist dort erstmals definiert; vorher stand die Überschrift „Footer-Navigation" auf jeder Seite sichtbar über dem Fuß. Der Header der Energy- und Audit-Routen bleibt unverändert unterdrückt.
- Die wichtigsten Nicht-Blog-Routen sind repo-seitig als zusammenhängender Linkgraph geführt: Der Systemphasen-Link der Startseite führt in die Solar-/SHK-Money-Page statt in den Agentur-Nebenpfad, der globale Footer bindet `/glossar/` mit beschreibendem Anker an, `/hasim-uener/` führt vom E3-Proof zum dokumentierten Case und `/server-side-tracking-b2b/` verbindet Messkonzept, Solar-Case und technischen Gesamt-Stack.
- Das Theme laeuft jetzt ueber einen getrennten CI/CD-Pfad: `.github/workflows/ci.yml` prueft PHP-Syntax, Workflow-YAML und das deploybare Theme-Paket; `.github/workflows/deploy.yml` deployed erst nach erfolgreichem CI-Run fuer einen Push auf `main` oder optional manuell per Dry-Run bzw. `workflow_dispatch`.
- Zentrale Theme-Module fuer Assets, SEO-Meta, Schema, Shortcodes, Portal und Snippets sind versioniert.
- Die Product-Default-Logik führt kalten Solar-/SHK-Traffic zum Marktcheck auf `/solar-waermepumpen-leadgenerierung/#marktcheck`; die versionierte Homepage ist repo-seitig als SHK-Hauptlandingpage auf Marktcheck/Analyse ausgerichtet und führt E3-Proof-Zahlen über den zentralen Canon. Der Homepage-Hero priorisiert den Analyse-CTA vor den Proof-Zahlen und zeigt rechts nur noch eine reduzierte Lead-System-Skizze aus Nachfrage, eigener Strecke und passender Anfrage. Der Energy-Kontext und die wiederverwendbaren Energy-CTAs priorisieren den Marktcheck mit Fit-Entscheid; der globale Standard-Header hält die drei Geschäftswege gleichrangig und nutzt „Projekt anfragen“ als generischen Direkt-CTA. Der schriftliche Marktcheck-Befund wird als Normalfall innerhalb von 48 Stunden versprochen, Success-/CRM-Texte nennen zusätzlich die Obergrenze von 2 Werktagen. Der wichtigste Proof-Link zeigt direkt auf `/case-study-solar-leadgenerierung/` statt auf den breiten Ergebnisse-Hub. Technische Nebenpfade wie Core Web Vitals und kostenlose Tools sind aus der Footer-Navigation entfernt. `/wordpress-agentur-hannover/` bleibt als sekundärer Footer-Link für lokale Money-Page-/SEO-Signale erhalten; White-Label steht repo-seitig als Geschäftsweg „Für Agenturen“ in der Hauptnavigation und ist als `index, follow` sowie Sitemap-Eintrag behandelt.
- Die Solar-/SHK-Money-Page bietet repo-seitig direkt nach dem transparent begrenzten Kostenbaustein-Vergleich den kleineren Einstieg `Sofortkontakt-Setup` für 790 EUR netto an (`#einstieg`, Einrichtung in fünf Werktagen); der CTA bleibt im bestehenden Marktcheck-Flow und die FAQ bildet den Einstieg mit ab. Aufbauwerte kommen aus dem Pricing-Canon, Portalwerte sind als Rechenbeispiel gekennzeichnet, und Media, Optimierung, Abschlussquote sowie Vertriebsaufwand werden nicht als Schein-Ersparnis verrechnet. Die Hero-Referenzgrafik zeigt nur die belegten E3-Werte Start 150 EUR, Aufbaukorridor 70 bis 100 EUR und Monat 6 mit 22 EUR statt eines interpolierten Monatsverlaufs. Die Seite verwendet keine Regions-Verknappung mehr. Auf dieser Route wird der globale Header vollständig unterdrückt; die seiteninterne Sprungnavigation übernimmt nach dem Hero sticky die Orientierung. Der projekt-eigene Header ist global die einzige serverseitig gerenderte Theme-Navigation; der zuvor nur per CSS versteckte Blocksy-Parent-Header wird über `blocksy:builder:header:enabled` abgeschaltet. Das WordPress-Menü normalisiert weiterhin die alten `page_id`-Ziele 13035 und 14283 auf die kanonischen SEO-/Ergebnisse-Routen, leitet die alten Query-URLs per 301 weiter, rendert reine Menügruppen als nicht klickbare Labels und entfernt die unklaren Leerziele `Performance` sowie `Tracking & CRO`.
- Das Gateway-Band direkt unter dem Homepage-Hero trägt nur noch die beiden Einstiege der Hero-Zielgruppe (Marktcheck, Case Study); Agentur und White-Label stehen vollständig im Final-Routing am Seitenende. Beide Sichten lesen dieselbe Kartenquelle `$home_routing_gateways`. Die sieben Homepage-FAQ liegen jetzt als `$home_faq_items` im Template und speisen sichtbaren Text und einen eigenen `FAQPage`-Knoten gemeinsam; die Startseite ist dafür in `$template_owns_faq_schema` (`inc/org-schema.php`) eingetragen, damit kein zweiter Knoten aus Editor-Inhalt entsteht. Die Antwortzeit auf den Marktcheck kommt aus dem Diagnose-Canon (`hu_marketcheck_reply_label()`, `in der Regel 48 Stunden, spätestens 2 Werktage`) und gilt für Startseite, Money Page und die geteilte Footer-/CTA-Microcopy. Kurzlabels wie `Marktcheck · 48 h` tragen weiterhin nur den Normalfall; seit 2026-08-20 schreibt `nexus_energy_nav_cta_label()` Menü-CTAs nur noch im Energy-Kontext auf dieses Label um, und `nexus_get_site_header_fallback_items()` liest die Navigation aus `hu_get_primary_navigation_contract()` statt aus einer zweiten, handgepflegten Liste. `nexus_render_site_header_menu()` ist als zweiter Renderer ohne Aufrufer entfallen.
- Die Homepage-Sektionen sind repo-seitig erweitert um einen visuellen System-Flow (`06 / Das System — visuell`, Anker `#flow`, Tracking-Section `homepage_system_visual`): zwei Spalten — links Portal-Chaos mit ~4.800 € / Monat, rechts eigene Strecke (Money Page → Vorqualifizierung → Privacy-first Tracking) mit den E3-Canon-Zahlen als Ergebnis. About (`#about`) liegt jetzt vor FAQ (`#faq`), damit Personenmarke und Trust-Anker direkt vor der Einwand-Behandlung greifen; der CTA hinter dem FAQ-Block führt zusätzlich auf den Marktcheck.
- Repo-seitig existiert zusätzlich die noindex-Testroute `/startseite-wow/` für eine visuell stärkere Homepage-Variante. Die Route ist virtuell im Theme registriert, benötigt keinen WordPress-Editor-Eintrag, lädt `homepage-redesign.css` plus `homepage-wow.css/js`, führt weiter primär in den Marktcheck und gibt `noindex,nofollow` per Meta sowie `X-Robots-Tag` aus. Die kanonische Startseite `/` bleibt davon unverändert.
- Der Marktcheck trägt repo-seitig eine Trust-Microcopy „kostenlos & unverbindlich" direkt an den primären Einstiegen: als zusätzliche Hero-Note auf der Homepage (vor der bestehenden Analyse-Note) und in der Solar-Quiz-Card als erstes SSR-Bullet plus JS-Fineprint. Der aktive Intake fragt in vier Auswahlstufen Leistungsfokus, wirtschaftlichen Projekt-Fit, Vertriebsverantwortung und Umsetzungshorizont ab; erst danach folgen die geschäftlichen Kontaktdaten. Er speichert ausschließlich die gegebenen Antworten und leitet keine CPL-, Anfragevolumen- oder Engpasswerte daraus ab. Die anschließende Anfragesystem-Analyse bleibt davon sprachlich getrennt; die Freemail-Validierung begründet die Firmen-Domain-Anforderung und nennt einen direkten E-Mail-Ausweg. Wie lang der Intake ist, versprechen Startseite, E3-Case und die Testroute `/startseite-wow/` aus dem Diagnose-Canon (`HU_MARKETCHECK_STEPS`, `HU_MARKETCHECK_MINUTES`) statt als eigene Copy; der Lead-Path-Smoke bricht ab, sobald Canon und `QUIZ_STEPS` auseinanderlaufen. Ebenso kommt der Aufbaupreis auf Startseite, `/eigene-leadgenerierung-vs-portale/` und `/kunden-gewinnen-solarteure/` jetzt aus dem Pricing-Canon (`hu_foundation_price_display()`, `hu_foundation_total_display()`); die zurückgezogene Spanne `12.000 – 18.000 €` steht auf keiner Route mehr, und die Vergleiche nennen Media, Optimierung und Vertriebsaufwand ausdrücklich als nicht enthaltene Kostenbausteine.
- Die lokale Money Page `/wordpress-agentur-hannover/` folgt repo-seitig jetzt einem Spezialisten-Flow: Hannover bleibt Standort- und SEO-Anker, die Zielgruppe wird aber fachlich über anspruchsvolle B2B-Angebote bestimmt. Der Hero führt das Ziel-Keyword explizit in der H1, grenzt klassische Webdesign-/Relaunch-Agenturleistung direkt aus, positioniert WordPress als technische Basis für messbare B2B-Anfragen und routet die Projektprüfungs-CTAs in den scoped Kontaktflow `/kontakt/?type=project&focus=followup_scope`; sekundär führt er zum E3-Case. Für Solar-, Wärmepumpen- und Speicher-Kontext gibt es jetzt zusätzlich bereits im Hero einen direkten Textlink zum Marktcheck. Danach folgen Spezialisierung, Anfragesystem-Methode, Methodenbibliothek, E3-Proof, Abgrenzung, Wartung und laufende Betreuung, Solar-Brücke, Standort, FAQ und finale Projektprüfung. Der Abschnitt `#wordpress-wartung` stellt Pauschalvertrag, Stundenpaket, Hosting-inklusive und Eigenbetrieb ohne erfundene Marktpreise gegenüber, grenzt klassische Wartungsverträge mit zugesicherter Reaktionszeit oder 24/7-Verfügbarkeit aus und führt für planbare Weiterentwicklung zur Freelancer-Route; die bestehende FAQ und das Service-Schema spiegeln dieselbe Retainer-Grenze. Die konsolidierten Service-Anker `#technisches-seo` und `#wordpress-wartung` existieren im Template und werden im Service-Schema nur auf vorhandene Seitenbereiche gemappt. Der Standortblock zeigt zusätzlich die lokalen Signale Pattensen bei Hannover, Region Hannover/Niedersachsen und DACH-Remote; die lokale `areaServed`-Information liegt zentral am Organization-/LocalBusiness-Schema und wird nicht mehr in jedem Service-Schema dupliziert.
- Die Personenseite liegt auf `/hasim-uener/` (vorher `/uber-mich/`, 301 in `nexus_get_legacy_offer_redirect_map()`). Route statt Template-Auswahl: `page-hasim-uener.php`, gesetzt von `nexus_maybe_ensure_about_page()`. Sechs Blöcke — Hero, vier Stationen, Haltung, Werdegang, CTA, Schlusszeile. Die persönliche D2C-Station steht auf unternehmerischem Risiko (`Eigenes Geld`, Onlineshop 2019–2023) statt auf einer zweiten Kennzahl; vom getrennten E3-Canon steht nur der Prozentwert, seit 2026-09 als `−85 %` mit Richtung statt als nackte Zahl, während Betrag, Zeitraum, Leadzahl und Region bewusst rausbleiben. Die E3-Station führt mit einem beschreibenden Trust-Link zum dokumentierten Solar-Case. Der Werdegang zwischen Haltung und CTA liefert in fünf Kapiteln (Herkunft, B2B-Vertrieb, Onlineshop, Medienwissenschaft, Arbeiten mit KI) die Belege zur Haltung; er hat die frühere Kompetenzleiste (WordPress · Tracking · Ads · CRO · Automatisierung) ersatzlos abgelöst, weshalb auch die Meta-Description der Route diese Aufzählung nicht mehr führt. Der Abschluss verzweigt nach Zielgruppe: Betriebe in die generische Projektanfrage (`hu_get_commercial_route( 'project_request' )`, nicht der Energy-Marktcheck), Agenturen in das White-Label-Angebot; LinkedIn und E-Mail bleiben leise Kontaktwege, die Schlusszeile verweist als einziger Außenlink auf `hasimuener.org`. Zwei Zusagen in den Karten sind jetzt einlösepflichtig: Ausfall wird am selben Tag gemeldet, und Scope und Preis stehen vor dem Start fest — laufende Kapazität erst nach dem Erstprojekt, passend zum „Kein Blind-Retainer"-Modell auf `/whitelabel-retainer/`.
- Service-Seiten, Blog, Kategorie-Archive, Footer-CTA und Trust-Bausteine sind im Repo. Der frühere Tools-Hub bleibt als Legacy-Code erhalten, wird öffentlich aber nicht mehr gerendert.
- Der Blog ist repo-seitig jetzt stärker als Solar-/Wärmepumpen-Supporting-Layer ausgerichtet: Blog-Index und Kategorie-Archive rendern als bildlose, typografische Editorial-Flächen mit echten Kategorie-Links, zentralen Kategorie-SEO-Metas, thematischen Vertiefungslinks, CollectionPage-/ItemList-Schema und Marktcheck-Führung. Der Blog-Index gruppiert die Beiträge zusätzlich frontendseitig in die vier Themencluster Portal-Kritik, CRO, Tracking und Performance; nach dem dritten Artikel sitzt eine Inline-Marktcheck-CTA-Card. Single-Artikel nutzen einen reduzierten Editorial-Hero, führen über eine Kontext-Brücke, eine strukturierte Next-Step-Zone und typografischen Related Content zu Kategorie, Money Page, Marktcheck und E3-Proof. Die alte Kategorie `wordpress-growth-agentur` bleibt als Datenbank-/URL-Slug stabil, wird öffentlich aber als `WordPress-Systeme` gelabelt; alte Autor-Bio-Copy mit Growth-/Shopify-Positionierung wird theme-seitig durch den kanonischen Personen-Text ersetzt. Artikel-Hero können weiter eine theme-seitig generierte Titelgrafik als Fallback nutzen.
- Der Beitrag `/checkfox-solar-waermepumpe-einordnung/` nutzt innerhalb des generischen `single.php` eine slug-spezifische, vollständig repo-eigene Präsentationsschicht. Sie ist auf eine kurze Entscheidungsseite reduziert: kompakter Hero, sticky Sprungnavigation (52 px, `<nav aria-label="Auf dieser Seite">`) und genau fünf Sektionen — Kostenrechner (Portal-CPO-Einzelszenario), Vertragscheck als standardmäßig geschlossenes `<details>` mit acht Prüfpunkten, Gegenüberstellung Portal-Modell vs. eigener Anfrageweg, Quellen-/Transparenzblock und ein abschließender Marktcheck-CTA. **Der gespeicherte WordPress-Editor-Inhalt wird auf dieser Route nicht mehr ausgegeben** (weder sichtbar noch im DOM); er bleibt in der Datenbank, ist aber für die Live-Seite ohne Wirkung — inhaltliche Änderungen laufen ab jetzt über `template-parts/checkfox-decision-cockpit.php`. Entfallen sind der Kurzantwort-Umschalter, die Sektion „Seriosität versus Wirtschaftlichkeit", der separate Prozessvergleich, die doppelten „Wann sinnvoll"-Blöcke und der angehängte Langartikel; die Kontextzeile des Blog-Headers ist auf dieser Route zugunsten der Sprungnavigation unterdrückt. Checkfox-Assets, personenbezogene Speicherung und provider-spezifische Preis-/Vertragsbehauptungen werden nicht eingeführt. URL, Canonical, Veröffentlichungsdatum, Indexierung und SEO-Metadaten bleiben im bestehenden Post-/SEO-Resolver-Pfad.
- Der Beitrag `/aroundhome-solar-einordnung/` nutzt ebenfalls eine slug-spezifische repo-eigene Präsentationsschicht und rendert den gespeicherten Editor-Langartikel nicht mehr. Die Seite beantwortet den Suchintent `Aroundhome Kosten für Handwerker` als Kosten- und Vertragsentscheid: kompakter Hero, Trennung von öffentlicher Anbieterangabe, individuellem Vertrag und Betriebsökonomie, CPO-Rechner ohne voreingestellte Aroundhome-/Branchenwerte, sieben schriftlich zu klärende Vertragspunkte, Evidenzleiter für öffentliche Erfahrungen, Portal-/Hybrid-/Eigenvergleich, klar abgegrenzter E3-Mechanismusbeleg, gemeinsame sichtbare FAQ-/FAQPage-Datenquelle und Quellenblock mit Anbieter- und Partner-AGB-Links. `single.php` unterdrückt auf der Route generischen Hero, Editor-Inhalt, Rating, Newsletter, Share-Rail, Related Content und Footer-CTA; eigene Aroundhome-Assets und Tracking-Bezeichner laden nur dort. URL, Canonical, Post-Metadaten, Breadcrumb und Article-/WebPage-Schema bleiben im zentralen Resolver-Pfad.
- Die `/system-diagnose/` ist repo-seitig retired und leitet per 301 auf `/solar-waermepumpen-leadgenerierung/#marktcheck`. Der `Growth Audit`, die generischen Audit-Aliasse und `/anfrage/` leiten als geschützte Legacy-Einstiege ebenfalls in diesen Marktcheck-Einstieg. Alte Tool-, ROI- und Service-Slugs werden nicht mehr hart per 301 erzwungen, sondern als `410 Gone` aus dem öffentlichen Crawl-Surface genommen; alte WGOS-Pfade bleiben noindex/access-protected und sitemap-excluded, solange sie intern noch genutzt werden.
- Repo-seitig gibt es jetzt einen E3-Proof-Canon in `blocksy-child/inc/canon/e3-proof-canon.php`: `150 €` vorher, `22 €` nachher, `über 85 %` Delta, `1.750+` qualifizierte Anfragen, `12 %` Abschlussquote und `6 Monate` Zeitraum.
- Der versionierte Ergebnisse-Hub bleibt als vorhandener Proof-Hub erreichbar, ist aber nicht mehr die primaere Proof-Fläche in Header oder Footer. Der kaufnahe Proof wird ueber `/case-study-solar-leadgenerierung/` direkt verlinkt und nutzt die kanonischen E3-Zahlen aus `blocksy-child/inc/canon/e3-proof-canon.php`. Ergebnisse-Hub und E3-Case geben jetzt zusätzlich ein sekundäres internes Signal auf `/wordpress-agentur-hannover/` für B2B-WordPress-Kontexte außerhalb des Solar-/Wärmepumpen-Fokus.
- Die anonymisierte Methodik-Case-Page läuft kanonisch über `/case-study-solar-leadgenerierung/`. Die Slug-Migration der editor-eigenen WordPress-Seite passiert repo-seitig automatisch (`nexus_maybe_ensure_case_study_page` in `inc/helpers.php`); die alten firmenbenannten Slugs `/e3-new-energy/`, `/case-e3/` und `/case-studies/e3-new-energy/` werden per 301 auf die anonymisierte URL geführt. Die Zielseite bleibt bis zur Freigabe des Anonymisierungs-Holds `noindex, follow`.
- Die frühere `Growth Audit`-Route `/growth-audit/` ist öffentlich deaktiviert und leitet per 301 auf den Marktcheck. Dasselbe gilt für die alten generischen Audit-Aliasse und `/wordpress-tech-audit/`; kostenlose Tool-Pfade liefern `410 Gone` statt als eigene Seiten oder erzwungene 301-Einstiege weiterzuleben.
- Der globale Standard-Header rendert ab 2026-09-04 auf allen Breiten dieselbe 64-px-Leiste und dasselbe Vollflächen-Menü; die frühere doppelte Desktop-/Mobile-Navigation ist entfallen. Drei Geschäftswege, drei Linkgruppen, Kontaktmetadaten, CTA und Tracking-Hooks kommen aus `hu_get_site_header_navigation_contract()`; der flache Kompatibilitätsvertrag sowie das SEO-Cockpit werden daraus abgeleitet. `site-header-premium.css` wird über WordPress mit `site-header.css` als Abhängigkeit geladen und besitzt zugleich ausschließlich komponentenlokale Tokens. Auto-Hide beim Abwärtsscrollen, Reveal beim Aufwärtsscrollen, Fokus, geöffnetem Menü oder Mausnähe an der oberen Viewport-Kante bleiben erhalten. Energy-, Audit- und Blog-Sonderführung bleiben unverändert beziehungsweise unterdrücken den Standard-Header weiterhin.
- Die frühere noindex-Kampagnenseite `/audit-linkedin/` ist repo-seitig retired. Eigenes Template, virtuelle Route und Assets sind entfernt; der Slug liefert `410 Gone` statt weiter als 301- oder noindex-Seite im öffentlichen Surface zu bleiben.
- Repo-seitig liegt zusätzlich eine B2B-Branchen-Landingpage für Solar-, Wärmepumpen- und Speicher-Anbieter kanonisch unter `/solar-waermepumpen-leadgenerierung/`. Sie bleibt SEO-, Kontext- und Lead-Zweig; der Hero nutzt einen fünfstufigen B2B-System-Intake mit vier echten Fit-Signalen und anschließendem geschäftlichem Datenpfad. Inhabergeführter Vertrieb gilt nicht mehr fälschlich als fehlende Vertriebsinfrastruktur; nur eine ausdrücklich fehlende Verantwortlichkeit oder ein Vermittlungs-/Kurzfristmodell führt im neuen Pfad zu `nurture`. Der Intake sendet als `B2B-System-Intake` über den bestehenden Audit-CRM-Endpoint.
- Repo-seitig liegt zusätzlich eine Intercept-Landingpage unter `/solar-leads-kaufen-alternative/` für den Kauf-Suchintent („Solar Leads kaufen", „Photovoltaik Leads kaufen"). Sie führt seit 2026-08-08 als zehnteiliger, strikt alternierender Hell-/Dunkel-Buying-Guide von Beschaffungsmodellen und Auftragsökonomie über Qualitätskriterien zur eigenen Anfrage-Infrastruktur. Der Hero qualifiziert Solar-, Wärmepumpen- und Speicher-Anbieter im DACH-Markt; der generische Wärmepumpen-Intent bleibt per Kontextlink bei `/waermepumpen-leads/`. Proof bleibt auf den E3-Canon (`150 € → 22 €`, `1.750+`, `12 %`, `6 Monate`) begrenzt. Primärziel ist durchgehend der Marktcheck mit Fit-Entscheid auf `/solar-waermepumpen-leadgenerierung/#marktcheck`; redaktionelle Sekundärziele sind Kostenstudie, TCO-Vergleich, CPL-Analyse und Case. Das Page-Delta `solar-leads-kaufen-alternative-page.css` lädt nur auf dieser Route über dem gemeinsam genutzten Intercept-CSS. Auto-Provisioning, Force-Template und SEO-Meta (`hu_get_forced_singular_seo_map()`) sind im Theme.
- Repo-seitig sind zwei weitere SEO-Sub-Pages versioniert, die auf demselben `.hu-intercept`-Grundsystem aufsetzen: `/b2b-solar-leads/` ist seit 2026-08-08 als eigenständiges Gewerbe-PV-Qualification-Gate gestaltet. Ein kompakter Split-Hero erklärt die Vorqualifizierung vor dem Termin; danach folgen Canon-Proof, vier Entscheidungssignale, eine semantische Vergleichstabelle, Pain-Economics, fünf Systembausteine, Fit/No-Fit, die sichtbare Leiter `Marktcheck → Anfragesystem-Analyse → Umsetzung`, FAQ und ein eindeutiger finaler Marktcheck-CTA. Das strikt gescopte Page-Delta `b2b-solar-leads-page.css` lädt nur auf dieser Route; Zahlen stammen aus E3- und Pricing-Canon, die eigene Fit-Schwelle `ab 50.000 €` ist ausdrücklich kein Marktmittelwert. `/eigene-leadgenerierung-vs-portale/` bleibt die Vergleichsseite mit 8-Kriterien-Matrix und TCO-Überschlag über 24/36 Monate. Beide routen primär in den Marktcheck und tragen Service- sowie FAQPage-Schema.
- `/server-side-tracking-b2b/` ist seit 2026-08-14 die transaktionale Money-Page für generisches Server-Side-Tracking; `/ga4-tracking-setup/` bleibt die vorgelagerte Event-, Consent- und Messkonzept-Grundlage. Die Route hat zwölf verdichtete Abschnitte: Split-Hero mit Fit-/Scope-Vorprüfung und überprüfbarem Rahmen (Preis, Paralleltest, Kundeneigentum), drei budgetrelevante Symptome, Client-/Server-Unterschied mit Hybrid-Hinweis, Fit und Non-Fit, Datenfluss-Grafik, sechs Umsetzungsergebnisse, drei Pakete, Tracking Care, vier Umsetzungsschritte, Sicherheit, acht FAQ und Anfrageformular. Die unbelegte Spanne „20 bis 40 Prozent mehr gemessene Conversions" ist aus sichtbarer Copy und FAQ-Schema entfernt; Proof bedeutet jetzt ausschließlich Paralleltest, benannte GTM-Versionen und Ergebnisse in den Konten des Kunden. Preise, Lieferfenster, enthaltene Korrekturzeit und Antwortzeit liegen zentral in `inc/canon/pricing-canon.php` und speisen Template, FAQ und Meta-Description. Das Formular schreibt unverändert über `nexus/v1/contact-request` mit `type=project` und `focus=tracking`; primär sichtbar sind Name, geschäftliche E-Mail, Website und Messproblem, optionale technische Angaben liegen in nativer Progressive Disclosure. Die mobile Sticky-CTA pausiert, sobald `#anfrage` sichtbar ist. Die Route ist kein Mitglied des Solar-Cluster-Footer-Moduls mehr; sie verlinkt intentklar auf `/ga4-tracking-setup/` und `/server-side-tracking-gtm/` sowie für den Solar-/SHK-Kontext auf den dokumentierten Case und `/stack-solar/`. Der Marktcheck bleibt der sekundäre Branchenpfad. Schema-Eigentum bleibt beim Template: BreadcrumbList, Service mit drei Offers und FAQPage.
- `/stack-solar/` bleibt eine indexierbare sekundäre Methodenroute, nicht ein eigenständiges Paket. Die fünf Schichten Frontend, Hosting, Server-Side Tracking, CRM und projektspezifische Vorqualifizierung werden erst nach Marktcheck und Anfragesystem-Analyse eingeordnet. Unbelegte CPL-, SQL-, Match-Rate-, Speed-, Lieferzeit- und Multiplikator-Claims sind aus sichtbarer Copy, FAQ und TechArticle-Schema entfernt; Ergebniswerte stammen ausschließlich fallgebunden aus dem E3-Canon. Die Route verlinkt zurück zur Solar-/SHK-Money-Page, zur Tracking-Money-Page, zum dokumentierten Case, zum Marktcheck und für Agenturen zum getrennten White-Label-Einstieg.
- Repo-seitig sind zwei zusätzliche Pillar-Pages für Topical Authority versioniert, die ebenfalls das `.hu-intercept`-Layout teilen: `/lead-funnel-solar/` (MOFU/BOFU-Pillar mit 5-Stufen-Funnel-Architektur für interne Marketing-/Vertriebsleiter) und `/kunden-gewinnen-solarteure/` (TOFU-Pillar mit Mythen-Aufklärung, 5 systematischen Hebeln und strikt B2B-qualifiziertem Fit-Check). Beide verlinken kontextuell auf die anderen SEO-Sub-Pages (Server-Side Tracking, Vergleich, Intercept) und routen primär in den Marktcheck.
- Die Legacy-Route `/roi-rechner/` ist kein erzwungener 301-Redirect mehr und liefert repo-seitig `410 Gone`. Die frühere automatische Papierkorb-Cleanup-Funktion ist entfernt.
- Der SEO-Sub-Page-Cluster ist um zwei Qualitäts-Pages erweitert, die das `.hu-intercept`-Layout teilen: `/cost-per-lead-photovoltaik/` (CPL-Analyse mit drei Szenarien-Vergleich Portal-Standard, Portal-Exklusiv, Eigenes System) und `/qualifizierte-pv-anfragen/` (Vier-Merkmale-Modell für hochwertige Anfragen plus Warnsignal-Block). Die Pillar-Seite `/solar-leads-kaufen-alternative/` ist um eine sachliche Markt­einordnungs-Sektion mit vier Anbieter-Modellen und einer Qualitäts-Kriterien-Box erweitert (juristisch konservatives Wording, keine Schmähkritik).
- Die vier sachlichen Lead-Anbieter-Markteinordnungen (Wattfox, Aroundhome, DAA, Leadfluss) liegen weiterhin als Publish-Pack-Drafts unter `content/blog-drafts/`; zusätzlich legt `blocksy-child/inc/blog-provider-posts.php` sie einmalig als veröffentlichte WordPress-Beiträge an, inklusive Kategorie, Tags, SEO-Meta, Marktcheck-CTA und interner Verlinkung zu E3 sowie dem Portalvergleich. Bestehende bereits veröffentlichte Beiträge mit gleichem Slug werden nicht inhaltlich überschrieben.
- Für die Anbieter-Einordnungen (Aroundhome, Checkfox, Wattfox, DAA, Leadfluss) rendert `blocksy-child/single.php` eine eigene Variante der bestehenden Article-Context-Box (`.nexus-article-context--provider`, Slug-Liste im Template). Sie ersetzt die Markteinordnungs-Variante an derselben Stelle — es entsteht keine zweite Box. Inhalt: die Suchfrage kurz beantwortet mit getrennter Aussage zu Unternehmensseriosität und wirtschaftlicher Eignung, danach ein Prüfraster aus fünf Fragen an den Anbieter (Exklusivität, Mindestabnahme, Storno, Übergabezeit, Kosten pro Auftrag). Bewusst als Fragen formuliert: Konditionen sind vertragsabhängig, eine Aussage über einen konkreten Anbieter wäre unbelegt. Primärer Anschluss ist der sachliche TCO-Vergleich `/eigene-leadgenerierung-vs-portale/`, sekundär der Marktcheck. Die `data-track-category` beider Kontext-Links folgt jetzt dem Ziel statt der Position — nur der Marktcheck zählt als `lead_gen`, redaktionelle Anschlussseiten als `internal_link`. Die veröffentlichten Beiträge selbst bleiben editor-owned; `blog-provider-posts.php` ist unverändert.
- Der Pillar-Blog-Artikel *„Solar-Leads kaufen: Warum die billigen Anfragen am Ende die teuersten sind"* (Slug `/solar-leads-kaufen-lohnt-sich/`) liegt als deployte Seed-Quelle unter `blocksy-child/assets/content/blog/photovoltaik-leads-tco-rechnung.md` (Dateiname historisch beibehalten). `blocksy-child/inc/blog-pillar-posts.php` legt ihn als veröffentlichten WordPress-Beitrag mit Slug, Kategorie, Tags, SEO-Meta und Featured Image aus `blocksy-child/assets/img/blog/photovoltaik-leads-kaufen-alternative-hero.png` an. **Stand 2026-07-01:** Inhalt von Marketer-Sprache (TCO/CPO/CPL) auf Handwerker-Klartext umgeschrieben, neue such-/schmerzorientierte Überschrift, Slug von `photovoltaik-leads-tco-rechnung` → `solar-leads-kaufen-lohnt-sich` geändert. Der alte Slug wird über die Retire-Liste als Draft aus der Indexierung genommen und per 301 (`hu_blog_pillar_redirect_legacy_slugs`, `template_redirect`) auf die neue URL geleitet. Der frühere Seed-Slug `/photovoltaik-leads-kaufen-alternative/` wird ebenfalls als Draft geführt.
- Die Solar-Money-Page `/solar-waermepumpen-leadgenerierung/` hat repo-seitig einen Themen-Hub `#deeper` zwischen Garantie- und FAQ-Sektion: vier thematische Cluster (Strategie & Vergleich, Lead-Qualität & CPL, Funnel & Tracking, Zielgruppen & Marktbild) mit zehn Inbound-Links auf die SEO-Sub-Pages, einschließlich `/waermepumpen-leads/`. Die System-Sektion führt zusätzlich zur gehärteten technischen Methodenroute `/stack-solar/`. Sticky-Section-Nav führt den `#deeper`-Anker mit; Tracking-Section `deeper` und Click-Action `deeper_cluster_link` für Attribution. Damit ist die interne Verlinkung von der Money-Page in das Topical-Authority-Cluster geschlossen.
- E-E-A-T-Hardening für die Solar-SEO-Sub-Pages: zentrale Helper in `seo-meta.php` (`hu_get_canonical_author_person`, `hu_get_solar_subpage_breadcrumb_schema`, `hu_get_subpage_last_updated_iso/label`) liefern Author-Schema mit `Person.@id` auf `/hasim-uener/#person`, BreadcrumbList-Schema und Last-Updated-Datum aus `filemtime()` des Templates. Jede Sub-Page rendert eine Author-Byline ("von Haşim Üner · Stand: …") direkt unter dem Hero-Lead über `template-parts/seo-subpage-byline.php`, Service-Schema enthält jetzt `author` zusätzlich zu `provider`, und ein zusätzliches BreadcrumbList-JSON-LD wird ausgegeben. `llms.txt` listet den Cluster "Solar- und Wärmepumpen-Leadgenerierung: Themen-Cluster" einschließlich der Wärmepumpen-Route für KI-Crawler.
- Conversion-Pack auf der Homepage und den Sub-Pages live: Die Homepage hat repo-seitig eine zusätzliche Sektion `#deeper` zwischen FAQ und Final-CTA mit denselben vier thematischen Clustern wie auf der Money-Page und zehn Inbound-Links in den SEO-Sub-Page-Cluster; CTA-Eyebrow ist auf `11 / Nächster Schritt` umnummeriert. Die Cluster-Subpages laden zusätzlich eine barrierefreie Sticky-CTA-Bar (`template-parts/seo-subpage-sticky-cta.php` + `assets/js/seo-subpage-sticky-cta.js`), die ausschließlich auf Viewports ≤ 760 px nach 240 px Scroll erscheint, einen Marktcheck-CTA mit Touch-Min-Size 44 px führt, einen Dismiss-Button mit `aria-label` bietet, die Entscheidung 24 h via `localStorage` merkt, `prefers-reduced-motion` respektiert und `role="region"` für Screenreader trägt. Body-Padding wird über `--hu-sticky-cta-h` dynamisch gesetzt, damit der CTA keinen Inhalt verdeckt.
- Die Solar-Brücke auf `/wordpress-agentur-hannover/` (Section 09 `#fokusmarkt`) ist repo-seitig zu einem Mini-Hub aufgewertet: unter dem primären Marktcheck-CTA liegt ein kompakter `.wp-agentur-deeper`-Block mit vier strategischen Sub-Page-Links (Solar Leads kaufen Alternative, Server-Side Tracking B2B, B2B Solar Leads, Cost per Lead Photovoltaik). Damit ist der interne Verlinkungs-Loop zwischen den beiden Money-Pages geschlossen und die Agentur-Page verteilt PageRank in den SEO-Sub-Page-Cluster. Styling im Light-Layout der Agentur-Page mit Hover-Lift und Accent-Border.
- Die Glossar-Registry ist repo-seitig als interner Verlinkungsmotor erweitert: `keywords_match`-Phrasen werden im Blog-Autolinker berücksichtigt, `lead-portale` routet als Alias auf `/solar-leads-kaufen-alternative/`, `solar-leadgenerierung` als Alias auf `/solar-waermepumpen-leadgenerierung/`, `wordpress-agentur-hannover` als Alias auf die lokale Money-Page und strategische Schreibweisen wie `Portal-Abhängigkeit`, `Leads kaufen`, `Aroundhome`, `Wattfox`, `DAA`, `eigene Leads`, `Solar-Leadgenerierung` und `Anzeigen-Anschluss` sind abgedeckt. Autoverlinkte Glossar-Begriffe erhalten die `short_definition` als Hover-Tooltip. Die Glossary-Sync-Version steht auf `2026-05-26-glossary-v4`.
- Die E3-Methodik-Case-Page `/case-study-solar-leadgenerierung/` bekommt repo-seitig eine zusätzliche Sektion `#vertiefung` zwischen `lessons` und `cta`: Section-Eyebrow „Methodik im Detail", H2 „Vier Bausteine, die das E3-Ergebnis möglich gemacht haben." und ein 2-Spalten-Grid mit Links auf `/cost-per-lead-photovoltaik/`, `/server-side-tracking-b2b/`, `/lead-funnel-solar/` und `/eigene-leadgenerierung-vs-portale/`. Stil im paper-2-Layout der Solar-Page mit Copper-Hover-Accent (`--solar-deep`). Damit ist auch der stärkste Proof-Hub in den SEO-Sub-Page-Cluster eingebunden.
- Das SEO-Cockpit klassifiziert die acht Sub-Pages des Solar-Clusters repo-seitig jetzt als eigene Page-Role `seo_subpage` mit Label „SEO-Sub-Page". Die Pfade sind zentral in `hu_get_solar_seo_subpage_paths()` (`inc/seo-meta.php`) hinterlegt, und `nexus_get_seo_cockpit_page_role()` erkennt sie über `hu_is_solar_seo_subpage_path()`. Business-Score 13, Funnel-Score 10 (zwischen Service und System), High-Value-Liste erweitert. Damit erscheinen die Sub-Pages im Cockpit-Reporting nicht mehr als generische `page` (Score 6/4), sondern werden bei Priorisierung, Insights und Lead-Attribution korrekt als Topic-Hub-Assets behandelt.
- Das SEO-Cockpit lädt `top_pages` und `top_queries` im Snapshot jetzt paginiert statt als starre Top-20-Auszüge (`top_pages` bis 150, `top_queries` bis 250) und die zentrale URL-Normalisierung erhält URL-Fragmente wie `#marktcheck`, damit Ankerziele nicht schon im Core zusammenfallen.
- Das SEO-Cockpit löst die effektiven Snippets für die konfigurierte Startseite und den Blog-Index jetzt anhand von `page_on_front` beziehungsweise `page_for_posts` über die zentralen Homepage-/Blog-Helper auf. Admin-Exporte verwenden damit dieselben forcierten Title-/Description-Werte wie das Frontend statt veralteter gespeicherter Post-Meta; andere Singular-Seiten bleiben im bestehenden Resolver-Pfad.
- Visueller Polish auf den Themen-Hubs: H2 auf der Homepage `#deeper` ist jetzt explizit dunkel (war versehentlich weiß auf cream und damit unsichtbar), Card-Titel auf den Themen-Cluster-Cards explizit weiß auf dunklem Card-Hintergrund. Cards bekommen einen warm getönten linearen Gradient, inset-Highlight und Drop-Shadow, Hover-State mit `translateY(-2px)`, glühender Accent-Border und Right-Arrow-Slide-In. `.hu-intercept__byline` auf den Sub-Pages als Pille mit Border und höherem Datums-Kontrast (0.55 → 0.75 Alpha), Sub-Page-Cards mit Hover-Lift und Underline-Animation auf Card-Titel-Links. `prefers-reduced-motion` durchgehend respektiert.
- Versionierte SEO-Title/Description sind repo-seitig auf Keyword-spitzer geschärft: `/solar-waermepumpen-leadgenerierung/` adressiert jetzt explizit `Leadgenerierung Photovoltaik & Wärmepumpe`, `/wordpress-agentur-hannover/` betont `WordPress Agentur Hannover` mit SEO/Tracking/CRO, `/server-side-tracking-b2b/` adressiert seit 2026-08-08 `Server-Side Tracking einrichten lassen` plus GA4/Ads/Consent — „Consent" steht bewusst im Title, weil die Seite laut `docs/seo/query-ownership.csv` vier DSGVO-Queries besitzt und CAPI in die Description ausweicht, `/b2b-solar-leads/` fokussiert ausschließlich Gewerbe-PV (`PV-Termine B2B: qualifizierte Gewerbe-PV-Anfragen`; das generische „Photovoltaik Leads" bleibt aus Title und H1 entfernt, weil es die Query der Solar-Money-Page hierher gezogen hat), `/solar-leads-kosten-studie/` fokussiert `Solar-Leads Kosten 2026` plus CPL/CPO und `/case-study-solar-leadgenerierung/` führt die CPL-Senkung `150 € auf 22 €` sowie die kanonischen E3-Kennzahlen im Title.
- Der historische Kategoriepfad `/category/owned-leads/` wird repo-seitig per 301 auf `/eigene-leadgenerierung-vs-portale/` geführt, weil der Pfad in Live-Logs weiterhin von Googlebot gecrawlt wurde, aktuell aber als 404 endete.
- Die Homepage-SERP-Signale sind repo-seitig auf die aktuelle Solar-/Wärmepumpen-Positionierung gezogen: Title und Description sprechen jetzt eigene Anfragesysteme, Marktcheck, Vorqualifizierung und Portal-Alternative an statt WordPress/B2B als Hauptpositionierung.
- `page-wgos.php` ist repo-seitig kein öffentlicher Sales-Funnel mehr, sondern ein internes `WGOS Client Dashboard` mit Login-/Capability-Schutz (`view_wgos_dashboard` oder `manage_options`), `noindex,nofollow`, Sprint-, Credit-, Aufgaben-, Asset-, Modul- und Dokumentations-Fallbacks. Nicht eingeloggte Nutzer gehen zum Login, eingeloggte Nutzer ohne Berechtigung zurück zur Startseite.
- Die aktiven Service-Clusterseiten für `GA4 Tracking Setup` und `Performance Marketing` sind als versionierte Routen im Theme hinterlegt. Die früheren SEO-, CRO-, Core-Web-Vitals- und Wartungs-Slugs sind nicht mehr öffentliche Primärziele; interne Links zeigen auf kanonische Routen, Assets oder Agentur-Anker, und die alten Service-Slugs liefern `410 Gone`.
- `inc/enqueue.php` nutzt für CSS- und JS-Assets in Live-Umgebungen die Version aus dem Header der deployten Child-Theme-`style.css` als verbindliche Cache-Busting-Version und fällt nur in `local`/`development` auf `filemtime()` zurück. Eine eventuell gesetzte `HU_THEME_VERSION`-Konstante greift nur noch, wenn der Theme-Header keine Version liefert, damit eine veraltete Server-Konstante neue Assets nicht festhält; der alte Instant-Results-Stack bleibt als Legacy-Code vorhanden, wird auf `/growth-audit/` aber durch den 301-Redirect nicht mehr öffentlich geladen.
- Die Service-Clusterseiten zeigen im Proof-Block jetzt bewusst belastbare System-Beweise (`100 % B2B-Fokus`, `48h Diagnosezeit`, `3 Proof-Routen`) statt uebergreifend wiederholter Leistungszahlen, die als generischer Platzhalter gelesen werden koennten.
- Die kanonischen Cluster-Routen und die Kontaktseite `/kontakt/` werden repo-seitig jetzt zusaetzlich als veroeffentlichte WordPress-Seiten mit zugewiesenem Template abgesichert, damit oeffentliche URL, native Sitemap, Canonical-Logik und Admin-Aufloesung konsistent bleiben.
- Repo-seitige Primary-Links auf aktive Cluster- und Service-Ziele zeigen jetzt direkt auf ihre kanonischen Routen, Asset-URLs oder Agentur-Anker und fallen nicht mehr auf kuerzere Legacy-Slugs wie `/seo/` zurueck.
- Ergebnisse laufen repo-seitig weiterhin kanonisch über `/ergebnisse/`; alte Proof-Slugs bleiben Legacy-Redirects. Für die Hauptnavigation ist `/case-study-solar-leadgenerierung/` der direkte Proof-Link.
- Der frühere `Growth Audit`-Shortcode-Flow bleibt repo-seitig als Legacy-Code vorhanden, ist aber nicht mehr der öffentliche Einstieg. Neue kalte Solar-/SHK-Besucher laufen in den Marktcheck auf der Solar-Seite; Agentur-Besucher laufen von der lokalen Money Page primär in den scoped Projektprüfungs-Flow auf `/kontakt/?type=project&focus=followup_scope` und nur bei explizitem Solar-/Wärmepumpen-Kontext in den unteren Marktcheck-Hinweis.
- Direkte Gespraechs-CTAs fuer Audit- und Kontakt-Kontexte nutzen theme-seitig zentral `https://cal.com/hasim-uener/30min?overlayCalendar=true`; öffentliche WGOS-/Asset-Hub-Sales-CTAs sind aus dem Funnel entfernt. Die Whitelabel-Seite nutzt fuer Partner- und Agentur-Fit zusaetzlich `https://cal.com/hasim-uener/whitelabel-fit-gesprach?overlayCalendar=true`.
- Diese Gespraechs-CTAs oeffnen bei verfuegbarem JavaScript ein eingebettetes Cal.com-Popup im Seitenkontext; der direkte Kalender-Link bleibt pro Event-Typ als robuster Fallback erhalten.
- Die Kontaktseite führt öffentlich weiterhin über die kaufnahen Anfragearten `Marktcheck`, `Umsetzung` und `Weiterentwicklung`; `Website-Analyse` und `Projektprüfung` werden zusätzlich als scoped Einstiege für `/kontakt/?type=analysis` und `/kontakt/?type=project` genutzt. Für die Agentur-Money-Page landet `type=project` als B2B-Projektanfrage im bestehenden Contact-REST-Vertrag und nicht als Solar-Marktcheck oder allgemeiner Kontaktpfad. Bestandskunden und allgemeine Rückfragen werden nicht mehr als Hauptauswahl gerendert. Die Frontend-Ausgabe von `/kontakt/` ist repo-seitig am nativen Theme-Template verankert und als kompakter Superflow reduziert. Neben-Routing, Social-Links und Erklär-Rail sind entfernt; Auswahlfragen springen automatisch weiter, `?type=implementation`, `?type=analysis` und `?type=project` starten direkt im passenden Thema und der bestehende REST-Submit-Vertrag bleibt rückwärtskompatibel.
- Der frühere warme Anfrage-Intake `/anfrage/` rendert kein eigenes Formular mehr. Repo-seitig ist `page-anfrage.php` nur noch ein Redirect-Template auf `/solar-waermepumpen-leadgenerierung/#marktcheck`; die alte `energy-intake.js`-Strecke und der dedizierte `/anfrage/`-CSS-Block sind aus dem Deploy-Code entfernt.
- Das Client Portal existiert technisch inklusive Login- und Upload-Logik.
- Repo-seitig existiert jetzt ein gemeinsames `Nexus CRM` fuer Audit-Anfragen, Projektanfragen und Blog-Abos; die Blog-Benachrichtigungen inkl. DOI, Abmeldung und manuell anstossbarem Artikelversand liegen im Theme.
- Repo-seitig existiert jetzt ein `SEO Cockpit` im WordPress-Admin: Search-Console-OAuth, Cache-Layer, Kernmetriken, Dashboard-Widget, Cron-Snapshot, optionaler Koko-Kontext und ein neuer Audit-Lead-Layer aus dem internen CRM sind im Theme vorbereitet.
- Das `SEO Cockpit` priorisiert repo-seitig jetzt nicht mehr nur nach Severity, sondern ueber eine business-aware Queue aus Page Role, Funnel-Naehe, Impressionen, Lead-Signal, Confidence und neuen Insight-Typen wie `Money Page Underperforming`, `Weak Funnel Bridge`, `Orphan Value Page` und `Indexing Mismatch`.
- Die Crawl-Signale für Search- und KI-Crawler werden repo-seitig jetzt über eigene Theme-Routen für `/robots.txt` und `/llms.txt` ausgeliefert: `robots.txt` antwortet als `text/plain` mit expliziten Regeln für OAI-SearchBot, GPTBot, ChatGPT-User, ClaudeBot und PerplexityBot und nennt `/llms.txt` sowie die native Sitemap als explizit erlaubte Crawl-Ziele; `llms.txt` bündelt Marktcheck-, Solar-Cluster-, Proof- und Kontaktpfade in einem erweiterten Markdown-Signal und dient Agenten als kompakter Routen-/Positionierungsindex.
- Der Solar-Subpage-Cluster umfasst repo-seitig jetzt auch `/solar-leads-kosten-studie/` in der zentralen Cluster-/Cockpit-Erkennung und in der sichtbaren Kontextlink-Registry. Public/Legacy-`noindex`-Seiten werden zentral als `noindex, follow` geführt, geschützte/private Routen bleiben `noindex, nofollow`; SEO-Cockpit und Frontend-Meta nutzen denselben Resolver.
- Technische SEO-Hygiene ist repo-seitig weiter konsolidiert: `/sitemap_index.xml` leitet per 301 auf `/wp-sitemap.xml`, nicht kuratierte Tag-/Custom-Taxonomy-/Author-/Date-Archive sind `noindex, follow`, die Taxonomy-Sitemap bleibt auf Kategorien beschränkt und das Theme bleibt ohne SEO-Plugin die zentrale Quelle für Meta, Canonical, Robots, Social Meta und Schema. Für Robots gilt das seit 2026-08-06 auch tatsächlich: `hu_silence_core_robots_tag()` schaltet den Core-Tag über `wp_robots` ab, weil sonst zwei `<meta name="robots">` im Head standen (Core mit `max-image-preview:large`, Theme mit der eigentlichen Direktive). Die Preview-Direktive läuft jetzt im Theme-Tag mit, ausgenommen auf `noindex`-Routen. Document-Title-Overrides laufen über eine zentrale Resolver-Funktion; OG-Bildmaße kommen aus Attachment-Metadaten statt aus festen Defaults; doppelte globale Breadcrumb-Schema-Ausgaben werden auf Seiten mit route-spezifischen Breadcrumbs vermieden.
- FAQPage-Schema aus editorgetriebenen Posts/Pages wird repo-seitig nicht mehr per Regex auf jedem Frontend-Request extrahiert. `save_post` berechnet FAQ-Entities einmalig aus `<details>` bzw. `.faq-trigger`/`.faq-content`-Mustern und speichert sie als JSON in Post-Meta; das Frontend liest nur noch den Cache. Template-eigene FAQ-Schema-Ausgaben bleiben ausgenommen.
- Nicht-kritische Theme-Skripte werden zentral ueber `inc/enqueue.php` mit `defer` ausgeliefert; `nexus-core-js` und `nexus-site-header-js` bleiben fuer unmittelbare Boot- und Header-Interaktionen ausgenommen.
- Zusaetzliche INP-Haertung: `nexus-core.js` verschiebt nicht-kritische Observer/TOC/Progress-Initialisierung auf `requestAnimationFrame` bzw. Idle-Zeit, der Header verarbeitet Scroll- und Pointer-Bewegungen nur noch RAF-gebuendelt und das Cal.com-Enhancement bindet Preload-Listener direkt an passende Buchungslinks statt global am gesamten Dokument.
- Zusaetzliche Homepage-Performance: die Font-Faces liegen jetzt direkt in `style.css` statt in einer separaten `fonts.css`-Anfrage, `related-content.css` und `footer-cta.css` laden nur noch auf Blog-Singles und die Homepage zieht die Core-Block-Library nicht mehr in den kritischen CSS-Pfad.
- Zusaetzliche Homepage-Font-Optimierung: auf der Startseite wird fuer Figtree jetzt gezielt `figtree-600.woff2` als kritischer Schnitt vorgeladen; gleichzeitig sind Floating-Nav, Hero-Badge, Hero-CTA, Hero-Textlink und der fruehe Proof-Strip typografisch auf leichtere Figtree-Gewichte reduziert, damit `figtree-700.woff2` nicht mehr fuer den ersten Viewport notwendig wird.
- Zusaetzliche Homepage-JS-Optimierung: die Startseite zwingt jetzt auch Parent-Theme- und Core-Handles (`ct-scripts`, `nexus-core-js`, `nexus-site-header-js`) in den `defer`-Pfad; `nexus-core.js` initialisiert Smart-Nav-Scroll-Spy spaeter und bevorzugt `IntersectionObserver`.
- Das globale Theme ist repo-seitig jetzt fest auf das dunkle Farbschema gesetzt; der fruehere Desktop-Hell/Dunkel-Toggle rendert nicht mehr und der Frontend-Boot setzt `data-theme` bzw. `data-nx-theme` konsistent auf `dark`.
- Repo-seitige Branding-Ausgabe fuer Logo- und Favicon-Signale priorisiert jetzt das im WordPress-Admin gesetzte `custom_logo` bzw. `site_icon`; die Theme-SVGs unter `assets/brand/` bleiben nur noch Fallback, damit Frontend-Head-Tags und Organization-Schema dieselbe Quelle nutzen.
- Schema-/Entity-Graph: Personenreferenzen laufen zentral auf `/hasim-uener/#person` (aufgeloest ueber `hu_person_profile_url()`, nicht mehr hart verdrahtet); `Person.sameAs` enthaelt `hasimuener.de`, LinkedIn, GitHub, `hasimuener.org` und Facebook, `Person.address` nennt Pattensen/DE. Die Person-Entity trägt jetzt zusätzlich JobTitle, Autorenbeschreibung, `alumniOf` und fachliche `knowsAbout`-Signale. `Organization.hasMap`, Unternehmensname, Koordinaten und Oeffnungszeiten sind an das Google Business Profile angeglichen. `/wordpress-agentur-hannover/` gibt neben Service-, WebPage- und FAQPage-Schema jetzt zusaetzlich Article-Schema mit Haşim Üner als Autor aus.
- Der frühere EnergieFahrplan-Showroom ist repo-seitig aus dem Funnel entfernt. Beweisführung läuft über E3, Methodik und die direkte Achse Landingpage -> Branchen-Seite -> Marktcheck.
- Die alte WGOS-Video-/Preview-Route `wgos-preview` und ihre statischen HTML-/Template-Artefakte sind aus dem deploybaren Code entfernt. Nicht runtime-referenzierte statische HTML-Referenzen wie das alte WGOS-Proof-Snippet und das Brand-Sheet sind ebenfalls entfernt. Das interne `page-wgos.php` bleibt davon unberührt und bleibt ein geschütztes Client-Dashboard.

## In Arbeit

- `/system-diagnose/` ist repo-seitig kein eigener öffentlicher Funnel mehr. Die Route bleibt als Legacy-Einstieg erhalten und leitet per 301 auf den Marktcheck der Branchen-Landingpage. Der frühere React-Funnel unter `blocksy-child/readiness/` bleibt als Legacy-Code im Repo, ist aber nicht mehr der aktive Solar-Lead-Pfad. Der aktive Marktcheck nutzt `/wp-json/nexus/v1/audit-request`, speichert im Audit-CRM und nutzt die bestehende Mail-/Brevo-Schicht.
- Das Repo wird gerade von einem Theme-Repo zu einem Website Operating System erweitert; WGOS bleibt dabei internes Delivery-/Client-System und ist nicht mehr öffentlicher Verkaufsrahmen.
- `page-wgos.php` bleibt template-driven als internes Dashboard; eine spaetere Auslagerung dynamischer Projektwerte muss serverseitig nach `client_user_id = current_user_id` filtern.
- Systembaustein-Detailseiten laufen weiter ueber den hierarchischen CPT `wgos_asset` mit SCF-Meta und Single-Template; die redaktionelle Befuellung der Spokes passiert weiter im WordPress-Admin.
- Systembausteine sind jetzt zusaetzlich als versionierte Registry im Theme definiert: 39 Assets stehen repo-seitig auf `publish`, inklusive 4 KI-/Automatisierungs-Assets im Kernbereich `Weiterentwicklung`.
- Die neue Systembaustein-Struktur rendert versionierte SEO-Meta, Related-Links und `Service`-Schema aus derselben Registry.
- Die frühere Systemlandkarte wird bei Bedarf weiter als Baustein-Übersicht angelegt, ist aber kein öffentlicher CTA- oder Sales-Pfad mehr.
- Die eigentliche Post-Erstellung und Aktualisierung fuer Systembausteine haengt aktuell noch am neuen Theme-Sync und damit am naechsten Deployment auf die Live-Umgebung.
- Teile der Homepage- und Navigationslogik haengen noch an manuellen WordPress-Admin-Schritten oder editorgetriebenen Default-Seiten ausserhalb der route-forced Clusterpages.
- Editorgetriebene Seitentitel, Excerpts, Karten und `the_content()`-Bereiche koennen weiter alte Proof-, Tonalitaets- oder Du/Sie-Brueche enthalten und muessen im WordPress-Admin separat verifiziert werden.
- Blog-Artikel koennen jetzt theme-seitig passende Systembausteine als Anschlussblock ausgeben; weitere Post-Mappings bleiben ausbaubar.
- Der frühere schnelle Intake und der Instant-Results-Flow für `/growth-audit/` liegen repo-seitig weiter als Legacy-Code vor, sind aber nicht mehr der aktive Renderpfad dieser Route.
- CRM-, Mail- und Follow-up-Logik fuer den Audit sind jetzt im Theme sichtbar; `wp_mail`-basierte Transaktionsmails koennen repo-seitig zentral ueber die Brevo API geroutet werden, sobald die Live-Credentials ausserhalb des Repos gesetzt sind.
- Neue Audit-Anfragen speichern repo-seitig zusätzlich Formular-Landingpage, ersten internen Einstieg, vorherige interne Seite und Referrer für Lead-Attribution im SEO-Cockpit. Der gemeinsame Frontend-Helper übernimmt innerhalb der Browser-Session außerdem Kampagnenquelle und Suchbegriff aus `utm_source`/`utm_term`; vorhandene `gclid`-/`fbclid`-Parameter werden nur als Google-/Meta-Quellensignal erkannt, nicht als rohe Klick-ID gespeichert. Der Solar-B2B-Marktcheck sendet absolute interne URLs; für bestehende Energie-Landing-Leads ohne historische URL-Meta ordnet das SEO-Cockpit defensiv per `energy_systems_landing`-Source-Fallback auf `/solar-waermepumpen-leadgenerierung/` zu, statt sie als komplett unmapped zu behandeln. Cockpit und CRM verwenden für die Qualifizierung denselben Vertrag `qualified|nurture` und das Cockpit aggregiert die qualifizierte Anzahl explizit.
- Der aktive Marktcheck-REST-Endpoint `/wp-json/nexus/v1/audit-request` ist repo-seitig auf Contract `2026-05-26.audit-request.v1` gehärtet: Clients können `contract_version` mitsenden, Antworten tragen `contractVersion`, `traceId`, `X-Nexus-Contract-Version` und `X-Nexus-Trace-Id`, und Fehler liefern maschinenlesbare `error_code`-/`error_details.field`-Signale.
- Die Energie-/Branchen-Landingpage bleibt unter `/solar-waermepumpen-leadgenerierung/` als SEO-Zweig, Kontextseite und primärer Marktcheck-Einstieg bestehen; der alte Pfad `/website-fuer-solar-und-waermepumpen-anbieter/` wird weiter per Redirect auf den kanonischen Slug gefuehrt. Die großen CTA-Flächen der Seite führen zum eigenen `#marktcheck` im Hero; kein separater `/system-diagnose/`-Funnel ist dort Default.
- Das neue Blog-Notify-System ist repo-seitig implementiert. Der initiale Subscribe-Request fuer `Neue Artikel per E-Mail` ist live verifiziert: der Endpoint speichert zunaechst einen Pending-DOI-Intent und versendet die Bestaetigungsmail; der CRM-Kontakt entsteht bzw. wird aktualisiert erst nach Klick auf den DOI-Link.
- Das SEO-Cockpit ist repo-seitig implementiert; Google-OAuth/API-Livezustand bleibt admin-/credential-owned und wird nicht im Repo gespeichert. Koko bleibt ein optionaler lokaler Traffic-Layer und muss getrennt vom Search-Console-API-Zugriff bewertet werden.
- Das SEO-Cockpit ist repo-seitig jetzt umbaufest interpretiert: historische Search-Console-Zeilen für konsolidierte Legacy-Slugs werden für Insight-Scoring auf das kanonische Ziel gemappt, WooCommerce-/Shop-Queries werden nicht mehr als kaufnahe Zielaufgaben priorisiert, und die Top-Pages-Tabelle markiert Redirect-Reste als `Legacy` statt sie wie unklare WordPress-Objekte aussehen zu lassen.
- Der SEO-Cockpit-CSV-Export führt aktuelle und vorherige Query×URL-Zeilen jetzt als Union, kennzeichnet `previous_only`-Verluste und ergänzt getrennte Page-Totals. Gap-Auswertungen ignorieren nur vorherige Zeilen, Drift-Auswertungen melden sie als `VERSCHWUNDEN`; fehlende Positionen werden nicht als Rang `0` ausgegeben.
- Das SEO-Cockpit zählt interne Kontextlinks jetzt nicht mehr nur aus `post_content`, sondern zusätzlich aus den relevanten Laufzeit-Templates für Cluster-Seiten, Blog-Bridges, die Agentur-Seite und den Ergebnisse-Hub; zugleich sind forcierte SEO-Snippets für `/ergebnisse/` und ein keyword-first Title für `/wordpress-agentur-hannover/` im Theme hinterlegt. Nicht-Zielqueries wie reine Shop-/E-Commerce-Intents werden bei Prioritäts-Queue, Kannibalisierung, Quick-Wins und Query-Movern aus Handlungsempfehlungen herausgefiltert.
- Der SEO-Cockpit-Linkgraph zählt jetzt auch die hardcodierten Themen-Hub-Links aus Homepage, Solar-Money-Page, E3-Case und Agentur-Seite in den Solar-/SHK-Subpage-Cluster. Dadurch fallen `/kunden-gewinnen-solarteure/` und `/server-side-tracking-b2b/` nicht mehr fälschlich als kontextlinklose Pages auf, sobald der neue Linkgraph nach Deploy/Sync neu aufgebaut ist.
- Der Solar-Hauptfunnel fuehrt repo-seitig nicht mehr ueber `/kontakt/`, `/wordpress-agentur-hannover/` oder `/whitelabel-retainer/`; diese Nebenpfade bleiben in isolierten Kontexten bzw. im Footer erreichbar, nicht in der primaeren CTA-Logik. `/whitelabel-retainer/` hat als Agentur-Einstieg zusaetzlich einen eigenen Header-Navigationspunkt „Für Agenturen“, ist aber kein Schritt im Solar-Funnel.
- `/whitelabel-retainer/` ist repo-seitig zum Partner-Funnel umgebaut: primaere Conversion-Aktion ist jetzt ein rein clientseitiger Fit-Check (3 Klickfragen, keine Speicherung, kein REST) in der Abschluss-Sektion `#fit-check`; die Antworten führen zu einer ehrlichen Einordnung für Test-Sprint, größeres Erstprojekt, Retainer nach Erstprojekt oder einen noch zu frühen Bedarf. Hero-, Einstiegs- und Sticky-CTAs ankern dorthin, während eine direkte Cal.com-Fast-Lane für Agenturen mit klarem Scope sowie der Mail-Weg erhalten bleiben; ohne JavaScript sind Buchung und Mail direkt sichtbar. Das Hero-KPI-Dashboard ist durch die statische Ablauf-Kette (Ihr Kunde → Eure Agentur → Umsetzung im Hintergrund → Ergebnis unter eurem Namen) ersetzt, die Proof-Sektion zeigt nur noch den CPL-Kontrast als offengelegte, vollständig anonymisierte Arbeitsprobe mit explizitem Nicht-Mandats-Hinweis. Kleinster Einstieg ist der einmalige WordPress-Test-Sprint zum Festpreis aus dem Pricing-Canon (`HU_WHITELABEL_TEST_SPRINT_PRICE`, maximal ein Arbeitstag, schriftlich fixierter Scope); groessere Erstprojekte werden nach der Umfangsklaerung als Festpreis angeboten, Retainer erst nach einem erfolgreichen Erstprojekt. Was der Test-Sprint nicht abdeckt, steht als eigene FAQ-Frage statt im oberen Seitenbereich. Vor dem Erstprojekt steht sichtbar das Pre-Sales-Scoping für die Angebotsphase. Service-Level werden vorab über Verfügbarkeit, Starttermin und Delivery-Fenster verbindlich vereinbart (genau eine Stelle: Kontrakt-Karte „Verbindlich“); die zugesagte Reaktionszeit kommt aus `hu_response_promise()` (`inc/canon/messaging-canon.php`) und lautet sitewide „Antwort innerhalb von 24 Stunden werktags“. Die Lieferfelder sind auf drei Bereiche reduziert — WordPress & Technical Delivery, Tracking & Attribution, CRM & Automation —, Landingpages laufen als Querschnitt statt als eigener Bereich. Der Block „Wer liefert“ nennt zusaetzlich eine Kapazitaets- und Abwesenheitsregel sowie eine Senior-Zeile (Erfahrung, Abschluss, Schwerpunkt, Standort); der Nav-Punkt „Ablauf“ ankert auf `#ablauf` (Ablauf-Kette im Hero) statt auf `#hero`. Auf der Route ersetzen ein seitenlokaler Sprungmarken-Header und ein reduzierter Footer mit zentralem Repository-Link sowie Impressum und Datenschutz den globalen Header samt Endkunden-Navigation und den globalen CRO-Footer; die Route ist als Agentur-Einstieg jetzt `index, follow`, steht in der Sitemap und ist ueber den Navigationspunkt „Für Agenturen“ erreichbar.
- Der Whitelabel-Partner-Funnel führt den Proof-Pfad jetzt über drei serverseitig gerenderte E3-Canon-Kennzahlen und einen Rücksprung in den Fit-Check. Seitenlokale Klicks aus Header, Inhalt und Footer werden ohne neue Analytics-Bibliothek als bestehende `data-track-action`-Events in einen vorhandenen `dataLayer` geschrieben; der Abschluss ergänzt genau ein `fitcheck_result` mit einem von fünf festen Outcome-Werten und überträgt weder Freitext noch die Mail-Vorlage. Das strukturierte Datenmodell beschreibt den sichtbaren White-Label-Partner-Funnel und emittiert genau einen FAQPage-Knoten aus demselben Neun-Fragen-Helper wie das Template; der Save-Time-FAQ-Cache ist für die Route ausgeschlossen.
- Seit 2026-08-14 bedient `/whitelabel-retainer/` als zweite Kernpositionierung zwei Zielgruppen und hat dafür repo-seitig einen zweiten, gleichrangigen Ausgang: neben dem Fit-Check steht an drei Entscheidungspunkten (Hero, Angebotskarten, Fit-Check-Block) der CTA „Aufgabe beschreiben — Einschätzung innerhalb von 4 Stunden werktags“. Er ist ein `mailto` mit vorbelegtem Betreff und Textgerüst, ohne Formular, REST-Endpunkt oder zusätzliches Blocking-Asset; die Events `cta_whitelabel_hero_task_brief`, `cta_whitelabel_entry_task_brief` und `cta_whitelabel_fitcheck_task_brief` (Kategorie `lead_gen`) laufen über den bestehenden delegierten Listener und bleiben von Fit-Check- und Marktcheck-Zahlen getrennt. Die Arbeitsprobe ist vom Hero-Ghost-Button zum Textlink in der Fineprint-Zeile geworden.
- Die Preisdarstellung beider indexierter Tracking-Seiten ist seit 2026-08-14 aufeinander abgestimmt. Die Beträge stehen ausschließlich in `inc/canon/pricing-canon.php` und werden hier bewusst nicht wiederholt — diese Datei ist keine zweite Preisquelle. Endkundenleiter auf `/server-side-tracking-b2b/`: `HU_TRACKING_STANDARD_SETUP`, `HU_TRACKING_PRO_SETUP` und `HU_TRACKING_CUSTOM_SETUP_MIN` sind angehoben; die Monatsbeiträge der Tracking Care, Leistungsumfänge, Paketnamen und Seitenstruktur bleiben unverändert. White-Label-Leiter auf `/whitelabel-retainer/`: `HU_WHITELABEL_TEST_SPRINT_PRICE` bleibt der einzige feste Betrag und wird auf der Karte als Festpreis ausgewiesen; `HU_WHITELABEL_TRACKING_AUDIT_MIN`, `HU_WHITELABEL_SERVER_SIDE_MIN` und `HU_WHITELABEL_LANDINGPAGE_MIN` sind neue Untergrenzen und liegen rund 30 % unter dem jeweils entsprechenden Endkundenpreis. Der Audit ist die günstigste Stufe, weil er als Türöffner das Setup verkauft. Die Reihenfolge ist bindend: sinken die Endkundenpreise, muss der White-Label-Block mitgeprüft werden, sonst zahlen Agenturen mehr als Endkunden. Kein Stundensatz auf der Seite. Die Angebots-FAQ liest dieselben Konstanten und geht so auch ins FAQPage-Schema.
- Die Proof-Sektion von `/whitelabel-retainer/` ergänzt die anonymisierte Zahlenkachel um drei prüfbare Live-Umsetzungen (civaka-azad.org, conversionengine.de, hasimuener.org) mit je einem Satz zur technischen Schwierigkeit — keine Kunden- und keine Inhaltsangaben. Die dritte Kennzahl zeigt nur noch `12 % · eigene Anfragen`; der Vorher-Wert „1 – 5 %“ ist aus der Route entfernt, weil er nicht zu den kanonischen Zahlen gehört. Der Schlüssel `sales_conversion_uplift` bleibt im E3-Canon für die Solar-Routen unverändert. Der Footer der Route trägt keinen Repository-Link mehr; er kehrt erst zurück, wenn ein vom Arbeits-Repo getrenntes öffentliches Showcase-Repo existiert, dann als CTA im Abschnitt „Technischer Beleg“.
- `/whitelabel-retainer/` ist kein geschlossener Kreis mehr: Die Wortmarke im seitenlokalen Header ist ein Link auf `/` (`nav_whitelabel_home`, Kategorie `navigation`), und der reduzierte Footer trägt über der Rechtszeile drei Rückwege in die Site — Startseite, `/wordpress-freelancer-hannover/` und `/solar-waermepumpen-leadgenerierung/` (`nav_whitelabel_footer_home`, `nav_whitelabel_footer_freelancer`, `nav_whitelabel_footer_energy`). Alle URLs kommen aus `hu_get_commercial_route_map()`, nicht aus hartkodierten Pfaden. Die Sprungmarken-Navigation, der Fit-Check als primäre Aktion und der zweite Ausgang „Aufgabe beschreiben“ bleiben unverändert; die Route behält damit ihren eigenen Header statt des globalen.
- Kannibalisierungs-Hinweise im SEO-Cockpit zeigen jetzt direkt die staerksten konkurrierenden URLs mit Impressionen und Position an, statt nur auf den normalen URL-Drilldown der Primaer-URL zu verweisen.
- Das Client Portal zeigt keine erfundenen KPI-/Roadmap-Daten mehr. Ressourcen, Kennzahlen und Roadmap werden nur ausgegeben, wenn sie pro Nutzer im Meta-Payload `nexus_client_portal` hinterlegt sind; sonst erscheinen explizite Empty-States. Admins können diesen Payload im Benutzerprofil pflegen. Login und Upload-Logik bleiben aktiv.
- KI-Erweiterung: 4 Systembausteine (KI-Assistent/Chatbot 30 Cr, KI-Lead-Qualifizierung 20 Cr, RAG-Wissenssuche 25 Cr, LLM-Workflow-Automatisierung 20 Cr) stehen repo-seitig jetzt auf `publish` und sind dem Weiterentwicklungs-Layer zugeordnet. Dachseite `/ki-integration-wordpress/` als versioniertes Page-Template (`page-ki-integration.php`) mit Service- und FAQPage-Schema bleibt im Repo; die Live-Synchronisation der Asset-Posts ueber den Theme-Sync ist noch nicht verifiziert.

## Geplant

- n8n nur bei konkretem Workflow-Bedarf weiterfuehren; jedes aktive Artefakt braucht JSON, Doku und Flow-Map
- Agenten-Layer weiter ueber kurze Root-Regeln, lokale Kontexte und Skills fuehren; keine neue Root-Planablage
- weitere Systemdoku fuer Tracking, CRM-Routing, Offer-Logik und CTA-Inventar
- Ausbau des SEO Cockpits um echte Koko-Daten, Page-Level-Korrelationen und spaetere URL-Inspection-Sichten
- Ausbau des Decision Logs fuer strukturelle Repo- und Systementscheidungen
- Nach Deploys: Theme-Deploy prüfen, SEO-Cockpit manuell synchronisieren, Koko-Status im gewählten Zeitraum lesen, Sitemap prüfen und alte WordPress-Seiten/Menüeinträge über den Admin-Legacy-Cleanup bereinigen.

## Deprecated

- Legacy-Slugs `/system-diagnose/`, `/anfrage-system-analyse/`, `/readiness-diagnose/`, `/anfrage/`, `/growth-audit/`, `/audit/`, `/customer-journey-audit/`, `/360-audit/` und `/wordpress-tech-audit/` werden auf `/solar-waermepumpen-leadgenerierung/#marktcheck` umgeleitet.
- Die früheren Tool-, ROI- und Service-Slugs `/wordpress-seo-hannover/`, `/core-web-vitals/`, `/conversion-rate-optimization/`, `/wordpress-wartung-hannover/`, `/kostenlose-tools/`, `/tools/`, `/website-performance-analyse/`, `/roi-rechner/`, `/audit-linkedin/` und `/seo/` sind keine erzwungenen 301-Pfade mehr und liefern `410 Gone`. Alte WGOS-Pfade wie `/wordpress-growth-operating-system/` und `/wgos-systemlandkarte/` bleiben noindex/access-protected bzw. noindex, solange sie intern noch als Dashboard-/Asset-Hub-Pfade existieren.
- Legacy-Slugs `/case-studies/` und `/case-studies-e-commerce/` werden auf `/ergebnisse/` umgeleitet.
- Der Legacy-Slug `/wordpress-agentur/` bleibt als Schutz-Redirect auf `/wordpress-agentur-hannover/` erhalten.
- Der Legacy-Pfad `/alle-loesungen-im-detail/` wird auf die aktuelle Loesungs-Uebersicht umgeleitet.
- Sitemap-/Index-Hygiene: interne Lösungsseiten (`/alle-loesungen/`, `/loesungen/`), Showroom-/Legacy-Demos (`/energie-fahrplan-demo/`), Tool-/Audit-Legacy-Routen, alte Proof-/Kontakt-/Agentur-/Solar-Pfade und retired Service-Slugs werden repo-seitig aus der nativen Sitemap ausgeschlossen und je nach Pfad noindex markiert oder als `410 Gone` ausgeliefert. Relevante Sitemap-Ziele bleiben kanonische Funnel-, Proof-, Money-, Blog-, Glossar-, Kontakt- und veröffentlichte Asset-/Content-Detailseiten.
- Die Clusterseiten `/ga4-tracking-setup/` und `/performance-marketing/` bleiben aktive versionierte Routen. `/wordpress-wartung-hannover/` ist als alte Seite wieder konsolidiert: eigenes Template und Cluster-Registry-Eintrag sind entfernt, der Slug liefert `410 Gone`; interne Primärlinks zeigen nicht mehr auf den alten Slug.
- Im WordPress-Admin erscheint fuer Admins ein Legacy-Cleanup-Hinweis; der Ein-Klick-Flow setzt gefundene Altseiten auf `draft` und entfernt passende Menue-Eintraege.
- Der `Growth Audit`, die System-Diagnose-Seite und die alten kostenlosen Tools sind nicht mehr öffentliche Funnel-Stufen. Nur geschützte Legacy-Einstiege mit wahrscheinlicher externer Nutzung leiten noch auf den Marktcheck; interne Tool-/ROI-/Service-Altlasten werden als `410 Gone` beendet statt als Redirect-Netz betrieben. Der kaufnahe Einstieg für kalten Solar-/SHK-Traffic läuft repo-seitig über den Marktcheck auf der Solar-Seite; die Agentur-Money-Page nutzt für allgemeine B2B-Besucher den scoped Projektprüfungs-Flow auf `/kontakt/?type=project&focus=followup_scope` und nur für Solar-/Wärmepumpen-Kontext den unteren Marktcheck-Hinweis. Vertiefte Folgeanalysen oder technische Integrationen ergeben sich erst danach.
- Oeffentliche Kontakt- und CTA-Texte mit `Pilotprojekt`, `Proof-of-Value`, `3.000+ Leads`, `34x ROAS` ohne Kontext oder `Retainer` als kaufnaher Standardbegriff sind nicht mehr Zielbild.
- Ein WordPress-Editor-Shell als Source of Truth fuer den Audit ist nicht mehr Zielbild.
- Lose Root-Ablage fuer Playbooks, Referenz-Snippets und Content-Drafts ist nicht mehr Zielstruktur.
