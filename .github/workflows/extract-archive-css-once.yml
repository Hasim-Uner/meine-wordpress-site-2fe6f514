name: Extract Archive CSS Once

on:
  push:
    branches:
      - refactor/extract-archive-css-2026-09
    paths:
      - '.github/workflows/extract-archive-css-once.yml'

permissions:
  contents: write

jobs:
  extract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/extract-archive-css-2026-09
      - name: Apply extraction
        run: python3 scripts/extract-archive-css.py
      - name: Verify architecture
        run: |
          python3 scripts/audit-css-architecture.py
          python3 scripts/audit-legacy-nx-css.py
          bash scripts/lint-css-spacing.sh
          bash scripts/lint-css-motion.sh
          php -l blocksy-child/inc/enqueue.php
          test -f blocksy-child/assets/css/archive.css
          ! grep -q -- '--nx-' blocksy-child/assets/css/archive.css
          ! grep -q '\.nexus-archive-' blocksy-child/style.css
          grep -q "nexus-archive-css" blocksy-child/inc/enqueue.php
          git diff --check
      - name: Commit product extraction
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add blocksy-child/style.css blocksy-child/assets/css/archive.css blocksy-child/inc/enqueue.php docs/architecture/CSS_ARCHITECTURE.md
          git commit -m 'Extract archive CSS from global stylesheet'
          git push origin HEAD:refactor/extract-archive-css-2026-09
